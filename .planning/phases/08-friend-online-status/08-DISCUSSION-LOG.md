---
phase: 8
discussion_status: completed
started_at: "2026-06-13T03:30:00.000Z"
completed_at: "2026-06-13T04:00:00.000Z"
supplementary_discussion: "好友状态推送细节"
---
# Phase 8: Friend & Online Status — 讨论日志

## 讨论摘要

共讨论 10 个灰区 + 6 个边界场景，全部完成决策。

## 已解决决策

### 好友模块

| D-编号 | 灰区 | 决策 | 理由 |
|--------|------|------|------|
| D-41 | G1 — 好友添加策略 | 单向申请+确认 | 与主流聊天工具一致，用户有控制权 |
| D-42 | G2 — 验证消息持久化 | 持久化 message 到 FriendRequestEntity | Flyway 迁移，保证请求列表完整性 |
| D-43 | G3 — 事务范围 | 单事务：好友+会话+成员+系统消息 | 强一致性，2 人会话锁时间短 |
| D-44 | G4 — 删除后会话行为 | 保留会话，禁止发送 | ROADMAP 明确要求 |
| D-45 | G5 — 重加后会话恢复 | 恢复原会话 | 保留聊天历史，用户体验好 |
| D-46 | G9 — 好友列表分页 | 游标分页（按 friendship_id） | 与项目其他分页一致 |

### 在线状态模块

| D-编号 | 灰区 | 决策 | 理由 |
|--------|------|------|------|
| D-47 | G6 — 在线判定 | 任意设备在线=在线 | 多设备直觉，UserStreamRegistry 已有设备列表 |
| D-48 | G7 — TTL 刷新策略 | 伪在线 + PING 刷新 | 精确反映在线 + 减少误切换 |
| D-49 | G8 — 伪在线时长 | 60 秒 | 覆盖大部分网络抖动 |
| D-50 | G10 — 推送范围 | 所有在线好友 | 扇出成本可接受，避免懒加载复杂度 |

### 错误/异常处理

| D-编号 | 场景 | 决策 |
|--------|------|------|
| D-51 | 重复申请 | 拒绝并提示已有待处理申请 |
| D-52 | 双向竞赛 | 自动成为好友 |
| D-53 | 拒绝后重申请 | 允许重新申请 |
| D-54 | 申请自己 | 直接拒绝 |
| D-55 | 好友上限 | 不设上限 |
| D-56 | 非好友消息 | 发送时检查并拒绝 |

## 设计约束

- 好友模块复用已有 `FriendRequestEntity`/`FriendshipEntity` 结构
- 在线状态集成到 `ChatService` 的连接生命周期
- 使用 `ConversationLockManager` + `TransactionTemplate` 事务模式
- 推送到使用 `PushService.pushEventToUser()` 扇出

### 状态推送细节（补充讨论）

| D-编号 | 场景 | 决策 | 理由 |
|--------|------|------|------|
| D-57 | S1 — 推送触发时机 | 全部触发（登录/下线/超时/隐藏切换） | 状态变化完整通知 |
| D-58 | S2 — 推送内容 | 通知+客户端拉取：PushEventType.STATUS_CHANGED 仅带 uid，客户端调用 batchGetStatus | 服务端轻量，客户端按需拉取 |
| D-59 | S3 — 隐私过滤 | 隐藏用户不推送 | 隐私优先 |
| D-60 | S4 — 离线补偿 | 上线时客户端主动拉取所有好友状态 | 无服务端补偿成本 |

---
phase: 11
plan: 11-03
type: implementation
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/DedupStep.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/ValidateStep.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/WriteStep.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/FriendCheckStep.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageStep.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendContext.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageException.kt
  - common/src/main/kotlin/com/nebula/common/exception/SequenceOverflowException.kt
  - gateway/src/main/kotlin/com/nebula/gateway/di/ChatHandlerModule.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/ChatHandlerCollector.kt
  - service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt
  - service/src/main/kotlin/com/nebula/service/chat/MessageService.kt
  - repository/src/main/kotlin/com/nebula/repository/impl/MessageRepositoryImpl.kt
  - repository/src/main/kotlin/com/nebula/repository/redis/MessageQueueRepository.kt
  - repository/src/main/kotlin/com/nebula/repository/redis/PrivacyRepository.kt
  - gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt
  - repository/src/main/kotlin/com/nebula/repository/repository/DeadLetterRepository.kt
  - service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt
autonomous: false
---

# Plan 11-03：数据完整性与错误处理

## 目标

修复 30 个 MEDIUM 严重性问题，覆盖 6 大领域：死代码清理（8 个文件）、数据丢失修复（payload 全路径）、N+1 查询消除、业务逻辑 Bug（分页/死查询/去重）、协程与线程安全、未读计数持久化。

## 覆盖的问题

| # | CQ | 文件 | 行号 | 修复描述 |
|---|-----|------|------|---------|
| M01 | CQ-01 | `gateway/.../send/DedupStep.kt` | 全文 | **删除**：SendMessageStep 链死代码 1/7 |
| M02 | CQ-01 | `gateway/.../send/ValidateStep.kt` | 全文 | **删除**：SendMessageStep 链死代码 2/7 |
| M03 | CQ-01 | `gateway/.../send/WriteStep.kt` | 全文 | **删除**：SendMessageStep 链死代码 3/7 |
| M04 | CQ-01 | `gateway/.../send/FriendCheckStep.kt` | 全文 | **删除**：SendMessageStep 链死代码 4/7 |
| M05 | CQ-01 | `gateway/.../send/SendMessageStep.kt` | 全文 | **删除**：SendMessageStep 链死代码 5/7 |
| M06 | CQ-01 | `gateway/.../send/SendContext.kt` | 全文 | **删除**：SendMessageStep 链死代码 6/7 |
| M07 | CQ-01 | `gateway/.../send/SendMessageException.kt` | 全文 | **删除**：SendMessageStep 链死代码 7/7 |
| M08 | CQ-01 | `common/.../exception/SequenceOverflowException.kt` | 9 | **删除**：未使用的异常类 |
| M11 | CQ-06 | `repository/.../impl/MessageRepositoryImpl.kt` | 87-91 | `DataIntegrityViolationException` 时批量 XACK 但无死信记录 → 创建死信 |
| M09 | CQ-06 | `service/.../admin/DeadLetterService.kt` | 142 | `compensate()` payload 硬编码空串 → 从 `DeadLetterEntity.payload` 恢复（Base64） |
| M10 | CQ-06 | `service/.../admin/DeadLetterService.kt` | 205 | `retry()` payload 硬编码空串 → 从 `DeadLetterEntity.payload` 恢复（Base64） |
| M12 | CQ-06 | `service/.../chat/MessageService.kt` | 122-131 | 消息异步路径 payload 序列化丢失 → 传入原始 payload |
| M26 | CQ-06 | `repository/.../redis/PrivacyRepository.kt` | 124-128 | `userRepository.save()` 失败静默吞掉 → 日志 + 抛异常 |
| M13 | CQ-07 | `service/.../conversation/ConversationService.kt` | 202-205 | `inviteMember` for 循环内逐个查询 → 批量 `findByConversationIdAndUserIds`（D-83） |
| M14 | CQ-07 | `repository/.../repository/ConversationRepository.kt` | 13-40 | 缺少按 ID 列表批量查询方法 — `findByConversationIdAndUserIds()` 已存在（行 95-102），修复方案为在 N+1 修复（任务 #16）中调用该方法 |
| M29 | CQ-07 | `gateway/.../send/SendMessageHandler.kt` | 58,88 | pushMessage 和 asyncUnread 分别查询成员列表 → 合并为一次查询复用 |
| M15 | CQ-08 | `service/.../admin/DeadLetterService.kt` | 239-244 | `query()` 按状态过滤时 total 取自未过滤的 `findAll()` → 新增 `countByStatus()`（D-75） |
| M16 | CQ-08 | `service/.../admin/DeadLetterService.kt` | 255-260 | `markPermanentFailed()` `failCount < 0` 死查询 → `failCount >= MAX_RETRIES`（D-75） |
| M17 | CQ-08 | `service/.../chat/MessageService.kt` | 103-105 | `clientMessageId` 去重注释声明但无代码执行 → Handler 入口调用 `checkAndSetDedup()` |
| M20 | CQ-13 | `repository/.../redis/MessageQueueRepository.kt` | 112-123 | `checkAndSetDedup()` 声明但发送路径未调用 → 在 `SendMessageHandler` 入口调用 |
| M21 | CQ-13 | `gateway/.../send/DedupStep.kt` | 23-25 | 去重 Step 退化为 no-op → 随 M01 删除 |
| M24 | CQ-08 | `gateway/.../send/SendMessageHandler.kt` | 88-94 | 未读计数仅存 Redis → 同步调用 `incrementUnreadCount()` 持久化到 DB |
| M25 | CQ-08 | `repository/.../repository/ConversationMemberRepository.kt` | 28-37 | `incrementUnreadCount()` 声明但未被使用 → 在 `SendMessageHandler` 中调用启用 |
| M27 | CQ-08 | `service/.../conversation/ConversationService.kt` | 199-236 | `inviteMember` 中已存在成员被跳过但 memberCount 正确更新（逻辑验证，随 Plan 11-02 事务修复后自动正确） |
| M28 | CQ-08 | `service/.../admin/DeadLetterService.kt` | 262-265 | 补偿全量加载 RETRYING 记录 → 改分页（`Pageable.ofSize(BATCH_SIZE)`） |
| M18 | CQ-11 | `gateway/.../push/PushService.kt` | 58,174 | `pushMessage()` 和 `pushConversationEvent()` 中 JPA 直接调用 → 包裹 `withContext(Dispatchers.IO)`（D-84） |
| M19 | CQ-11 | `repository/.../impl/MessageRepositoryImpl.kt` | 116 | 独立 `CoroutineScope` 创建但 `stop()` 不 cancel → 添加 `scope.cancel()`（D-85） |
| M30 | CQ-11 | `gateway/.../send/SendMessageHandler.kt` | 73 | `launch{}` 非结构化协程 → 确认使用注入的 `sendHandlerScope`（D-85） |
| M22 | CQ-13 | `service/.../chat/MessageService.kt` | 131 | payload 在 Redis Stream 写入时为 `""` → 传入实际序列化 payload |
| M23 | CQ-13 | `gateway/.../send/WriteStep.kt` | 68-71 | payload 写入 Redis Stream 缺失 → 随 M03 删除 |

## 任务

### 阶段 A：死代码清理（CQ-01，可并行）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 1 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/DedupStep.kt` | 删除文件（M01） | `git rm` 无残留引用 | 编译通过 + IDE 无未解析引用 |
| 2 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/ValidateStep.kt` | 删除文件（M02） | 同上 | 同上 |
| 3 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/WriteStep.kt` | 删除文件（M03） | 同上 | 同上 |
| 4 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/FriendCheckStep.kt` | 删除文件（M04） | 同上 | 同上 |
| 5 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageStep.kt` | 删除文件（M05） | 同上 | 同上 |
| 6 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendContext.kt` | 删除文件（M06） | 同上 | 同上 |
| 7 | delete | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageException.kt` | 删除文件（M07，注意与 11-04 L18 删除的 `common/.../SendMessageException.kt` 是不同文件 — 两者均需删除） | 同上 | 同上 |
| 8 | delete | `common/src/main/kotlin/com/nebula/common/exception/SequenceOverflowException.kt` | 删除文件（M08） | 同上 | 同上 |
| 9 | modify | `gateway/src/main/kotlin/com/nebula/gateway/di/ChatHandlerModule.kt` | 移除 Step 链 7 个类的 Koin `single {}` 注册 | Koin 启动不报 `NoSuchBeanDefinitionException` | `ApplicationContext.getBeansOfType(DedupStep::class.java)` 返回空 |
| 10 | modify | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/ChatHandlerCollector.kt` | 移除 Step 链相关 Handler 的注册引用（如有） | 同上 | HandlerCollector 中不含死代码类引用 |

### 阶段 B：数据丢失修复 — payload 全路径（CQ-06）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 11 | modify | `repository/src/main/kotlin/com/nebula/repository/impl/MessageRepositoryImpl.kt` | `catch (e: DataIntegrityViolationException)` 分支中添加 `entityManager.flush()` 前置于 `saveAll()`；catch 块中为每条冲突消息调用 `deadLetterService.create(...)` 创建死信记录，传入 `payload = msg.payload`；保留 `messageQueueRepository.xack(messages)` 清理 Redis（M11） | UK 冲突时死信表有记录 | `INSERT` 重复 `client_msg_id` 后 `SELECT * FROM dead_letters WHERE client_msg_id=?` 返回 1 行 |
| 12 | modify | `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` | `compensate()` 方法中 `streamFields["payload"]` 从 `""` 改为 `item.payload?.let { Base64.getEncoder().encodeToString(it) } ?: ""`（M09，D-75） | 死信补偿后 Redis Stream 中 payload 字段非空 | `XRANGE stream 0 +` 输出中 `payload` field 为 Base64 编码字符串（非空串） |
| 13 | modify | `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` | `retry()` 方法中 payload 同样从 `DeadLetterEntity.payload` 恢复（同 #12 模式）（M10） | 同上 | 同上 |
| 14 | modify | `service/src/main/kotlin/com/nebula/service/chat/MessageService.kt` | 异步路径中 `streamFields["payload"]` 从 `""` 改为传入实际的 `req.payload`（原始 ByteArray → Base64 编码）（M12, M22） | 新发送消息的 Redis Stream 中有 payload | `XREAD BLOCK 0 STREAMS stream 0-0` 返回的 payload 为 Base64 编码 |
| 15 | modify | `repository/src/main/kotlin/com/nebula/repository/redis/PrivacyRepository.kt` | `setPrivacy()` 中 `userRepository.save()` 失败 add `logger.error(e) { "Failed to persist privacy for uid=$uid" }` + 抛出异常而非吞掉（M26） | `save()` 失败时异常传播到调用方 | `userRepository.save()` 抛异常时 Service 层可捕获并返回错误响应 |

### 阶段 C：N+1 查询消除（CQ-07）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 16 | modify | `service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt` | `inviteMember()` 中 for 循环逐个查询 `findByConversationIdAndUserId(convId, uid)` 替换为前置批量查询 `findByConversationIdAndUserIds(convId, req.uidsList).associateBy { it.userId }`，循环内使用 `existingMap[uid]`（D-83, M13） | 邀请 5 个用户仅 2 次查询（1 次会话 + 1 次批量成员）vs 修复前 6 次 | Hibernate Statistics `queryExecutionCount == 2`；修复前后 `newMemberUids` 结果一致 |
| 17 | modify | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt` | `asyncUnreadAndPush()` 中合并 `pushMessage()` 和 `incrementUnreadCount()` 的两次 `findByConversationId()` 查询为一次，结果复用（M29） | 发送消息仅 1 次成员查询 | Hibernate Statistics 成员查询次数 == 1 |

### 阶段 D：业务逻辑 Bug 修复（CQ-08, CQ-13）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 18 | modify | `repository/src/main/kotlin/com/nebula/repository/repository/DeadLetterRepository.kt` | 新增 `@Query("SELECT COUNT(d) FROM DeadLetterEntity d WHERE d.status = :status") fun countByStatus(@Param("status") status: String): Long`（M15） | `countByStatus("retrying")` 返回精确计数 | 模拟 DB 有 3 条 retrying + 100 条 pending，`countByStatus("retrying")` 返回 3 |
| 19 | modify | `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` | `query()` 方法中 `val total = deadLetterRepository.findAll(pageable).totalElements` 替换为 `deadLetterRepository.countByStatus(status)`（M15） | 分页结果 total 与过滤状态一致 | 状态过滤后 `page.totalElements == countByStatus(status)` |
| 20 | modify | `repository/src/main/kotlin/com/nebula/repository/repository/DeadLetterRepository.kt` | 新增 `@Query("SELECT d FROM DeadLetterEntity d WHERE d.status = :status AND d.failCount >= :minFailCount") fun findByStatusAndFailCountGreaterThanEqual(...): List<DeadLetterEntity>`（M16） | `findByStatusAndFailCountGreaterThanEqual("retrying", 3, ...)` 返回 failCount >= 3 的记录 | `failCount < 0` 死查询修复，`failCount >= MAX_RETRIES` 正确筛选 |
| 21 | modify | `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` | `markPermanentFailed()` 中 `findByStatusAndFailCountLessThan(STATUS_RETRYING, 0, ...)` 替换为 `findByStatusAndFailCountGreaterThanEqual(STATUS_RETRYING, MAX_COMPENSATE_RETRIES, ...)`（M16） | 超过重试次数的死信被正确标记 | 3 条 retrying 记录 failCount 分别为 2/3/4，仅 3/4 被标记 permanent_failed |
| 22 | modify | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt` | `handle()` 入口（业务逻辑处理前）添加 `if (req.hasClientMessageId()) { val isNew = messageQueueRepository.checkAndSetDedup(req.clientMessageId, session.userId); if (!isNew) return buildDuplicateAck(req.clientMessageId) }`（M17, M20, M21） | 相同 `clientMessageId` 重复发送时返回幂等 ACK | 连续发送相同 `client_msg_id`，第 2 次收到 `"duplicate": true` 而非新 seq |
| 23 | modify | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt` | `asyncUnreadAndPush()` 中新增 `conversationMemberRepository.incrementUnreadCount(convId, session.userId)` 调用（启用 M25 已有方法）（M24） | 发送消息后 DB 中 `unread_count` 增加 | `SELECT unread_count FROM conversation_members WHERE conversation_id=? AND user_id<>?` 发送后 +1 |
| 24 | modify | `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` | `compensate()` 中全量加载 `findByStatus(STATUS_RETRYING)` 改为分页 `findByStatus(STATUS_RETRYING, Pageable.ofSize(BATCH_SIZE))` + 循环处理（M28） | 10000 条 retrying 记录时内存可控 | 分页加载每次最多 BATCH_SIZE 条进入内存 |

### 阶段 E：协程与线程安全修复（CQ-11）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 25 | modify | `gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt` | `pushMessage()` 中 `conversationMemberRepository.findByConversationId(convId)` 包裹 `withContext(Dispatchers.IO) { }`（D-84, M18） | 协程线程不再被 JPA 阻塞 | 日志无 "blocking call on IO dispatcher" 警告 |
| 26 | modify | `gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt` | `pushConversationEvent()` 中 JPA 调用同理包裹 `withContext(Dispatchers.IO)`（D-84, M18） | 同上 | 同上 |
| 27 | modify | `repository/src/main/kotlin/com/nebula/repository/impl/MessageRepositoryImpl.kt` | `stop()` 方法中添加 `scope.cancel()` 取消所有子协程（D-85, M19） | 调用 `stop()` 后 `scope.isActive == false` | `scope.coroutineContext[Job]!!.children.all { it.isCancelled }` |
| 28 | modify | `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt` | 确认 `scope.launch {}` 使用注入的 `sendHandlerScope`（通过 Koin `get(named("sendHandlerScope"))`），确保 `SupervisorJob()` 异常隔离（D-85, M30） | 异步推送失败不影响消息发送主流程 | `scope.launch { throw Exception() }` 后主流程正常返回 seq |

## 依赖

- **Plan 11-01**：依赖配置安全就绪（ConfigLoader 校验、SSL 预校验、Shutdown Hook），确保 11-03 运行时基础设施稳定
- **Plan 11-02**：依赖 TransactionTemplate 事务包裹 + Flyway V5 + JPQL `incrementMemberCount` + `uk_client_msg_id` UNIQUE 约束（任务 #23 的 `incrementUnreadCount` 引用、任务 #22 的去重依赖 V5 UNIQUE）
- **内部依赖链**：阶段 A → B ∥ C ∥ D ∥ E（阶段 B/C/D/E 可并行）

## 产出物

### 源码文件（修改）
- `common/src/main/kotlin/com/nebula/common/exception/SequenceOverflowException.kt` — **删除**
- `repository/src/main/kotlin/com/nebula/repository/impl/MessageRepositoryImpl.kt` — 死信创建 + scope.cancel()
- `repository/src/main/kotlin/com/nebula/repository/repository/DeadLetterRepository.kt` — 新增 countByStatus() + findByStatusAndFailCountGreaterThanEqual()
- `repository/src/main/kotlin/com/nebula/repository/redis/PrivacyRepository.kt` — save 失败异常传播
- `service/src/main/kotlin/com/nebula/service/admin/DeadLetterService.kt` — payload 恢复 + total bug 修复 + 死查询修复 + 分页加载
- `service/src/main/kotlin/com/nebula/service/chat/MessageService.kt` — async path payload 传入
- `service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt` — N+1 批量查询
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt` — 去重 + 未读计数持久化 + 查询合并 + scope
- `gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt` — withContext(Dispatchers.IO)
- `gateway/src/main/kotlin/com/nebula/gateway/di/ChatHandlerModule.kt` — 移除死代码注册

### 源码文件（删除）
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/DedupStep.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/ValidateStep.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/WriteStep.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/FriendCheckStep.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageStep.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendContext.kt`
- `gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageException.kt`

### 测试文件（已规划，在 Plan 11-04 补充）
- T02: Step 链死代码测试清理
- T05: DeadLetter payload 补偿路径测试

## 验证

1. **编译验证**：`./gradlew compileKotlin` — 删除死代码后无未解析引用
2. **Koin DI 验证**：服务启动无 `NoSuchBeanDefinitionException`
3. **单元测试**：现有死信/消息相关测试通过
4. **Payload 验证**：补偿路径后 Redis Stream 中 payload 非空（Base64 编码）
5. **N+1 验证**：Hibernate Statistics 查询次数对比（D-83 双重验证）
6. **去重验证**：相同 `client_msg_id` 重复发送仅处理一次
7. **分页验证**：`DeadLetterService.query()` 按状态过滤后 total 精确
8. **协程验证**：`MessageRepositoryImpl.stop()` 后 scope 为 cancelled 状态
9. **集成测试**：Wave 2 全量回归测试通过

## 风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 删除 Step 链文件后其他模块仍有引用 | 编译失败 | 执行前 `grep -r "DedupStep\|ValidateStep\|..."` 确认无残留引用 |
| Payload 为空时 Base64 编码输出空串（与修复前相同） | 无改进 | 在 `createDeadLetter()` 入口处校验 `payload != null && payload.isNotEmpty()` |
| `countByStatus()` 在高并发下计数可能不精确 | 分页显示微差 | 接受 REPEATABLE_READ 隔离级别下的近似计数（非金融场景） |
| `sendHandlerScope` 注入可能已正确但命名不一致 | 运行时 NPE | 检查 `ChatHandlerModule.kt:23` 确认 `named("sendHandlerScope")` 注册与注入一致 |

## PLANNING COMPLETE

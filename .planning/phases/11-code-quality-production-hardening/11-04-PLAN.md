---
phase: 11
plan: 11-04
type: implementation
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - repository/src/main/kotlin/com/nebula/repository/entity/ConversationEntity.kt
  - repository/src/main/kotlin/com/nebula/repository/entity/UserEntity.kt
  - repository/src/main/kotlin/com/nebula/repository/entity/FriendRequestEntity.kt
  - repository/src/main/kotlin/com/nebula/repository/entity/FriendshipEntity.kt
  - repository/src/main/kotlin/com/nebula/repository/entity/ConversationMemberEntity.kt
  - common/src/main/kotlin/com/nebula/common/exception/ChatException.kt
  - common/src/main/kotlin/com/nebula/common/exception/MessageException.kt
  - common/src/main/kotlin/com/nebula/common/idgen/SnowflakeIdGenerator.kt
  - service/src/main/kotlin/com/nebula/service/chat/MessageService.kt
  - service/src/main/kotlin/com/nebula/service/user/UserService.kt
  - service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt
  - gateway/src/main/kotlin/com/nebula/gateway/service/ChatService.kt
  - repository/src/main/kotlin/com/nebula/repository/config/JpaConfig.kt
  - common/src/main/kotlin/com/nebula/common/exception/SendMessageException.kt
  - common/src/test/kotlin/com/nebula/common/idgen/SnowflakeIdGeneratorTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send
  - service/src/test/kotlin/com/nebula/service/friend/FriendServiceTest.kt
  - service/src/test/kotlin/com/nebula/service/conversation/ConversationServiceTest.kt
  - service/src/test/kotlin/com/nebula/service/admin/DeadLetterServiceTest.kt
  - service/src/test/kotlin/com/nebula/service/sequence/SeqServiceTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/LoginHandlerTest.kt
autonomous: false
---

# Plan 11-04：代码质量与测试加固

## 目标

修复 27 个 LOW 严重性问题：消除魔法数字引入枚举、替换 `!!` 空断言、提取 DRY 扩展函数、精简异常类型、修正 Entity 注释与 SQL 矛盾、抽取 Clock 接口替代反射、补充 5 个关键场景测试。

## 覆盖的问题

| # | CQ | 文件 | 行号 | 修复描述 |
|---|-----|------|------|---------|
| L01 | CQ-12 | `repository/.../entity/ConversationEntity.kt` vs `V1__init_schema.sql` | 15 | Entity 注释 `0=私聊`，SQL COMMENT `1=私聊` — 值矛盾，修正为 `1=私聊, 2=群聊` |
| L20 | CQ-12 | `repository/.../entity/ConversationEntity.kt` | 17 | type 字段 KDoc 与 SQL 矛盾修正（同 L01） |
| L02 | CQ-12 | `repository/.../entity/ConversationEntity.kt` | 15,39 | `type` / `status` Int 魔法数字 → 枚举 `ConversationType` / `ConversationStatus` |
| L03 | CQ-12 | `repository/.../entity/UserEntity.kt` | 34 | `privacyStatus` Int 魔法数字 → 枚举 `PrivacyLevel` |
| L04 | CQ-12 | `repository/.../entity/FriendRequestEntity.kt` | 23 | `status` Int 魔法数字 → 枚举 `FriendRequestStatus` |
| L05 | CQ-12 | `repository/.../entity/FriendshipEntity.kt` | 31 | `deleted` Int 魔法数字 → `isActive()` 扩展属性 |
| L06 | CQ-12 | 6+ 文件 | 多处 | `member.deleted == 0/1` 无命名常量 → 提取 `ConversationMemberEntity.isActive()` 扩展 |
| L08 | CQ-15 | `service/.../chat/MessageService.kt` | 252 | `id!!` → `id ?: error("MessageEntity.id 不应为 null")` |
| L09 | CQ-15 | `service/.../user/UserService.kt` | 92,120,157,183,212 | 5 处 `user.id!!` → `requireNotNull(user.id) { "用户ID不能为null" }` |
| L10 | CQ-15 | `service/.../conversation/ConversationService.kt` | 151,164 | 2 处 `id!!` → `id ?: error("会话ID不能为null")` |
| L11 | CQ-15 | `gateway/.../service/ChatService.kt` | 253,312 | `merge().!!` → `requireNotNull(mergeResult) { "合并结果不能为null" }` 前置校验 |
| L12 | CQ-15 | `repository/.../config/JpaConfig.kt` | 47 | `emfBean.getObject()!!` → `?: error("EntityManagerFactory 未初始化")` |
| L13 | CQ-15 | 4+ 文件 | 多处 | `member == null \|\| member.deleted == 1` 重复 → 提取 `ConversationMemberEntity.isActive` 扩展 |
| L14 | CQ-15 | 4+ 文件 | 多处 | `findById().orElse(null)` 重复 9 次 → 提取 `findByIdOrNull()` 扩展函数 |
| L15 | CQ-15 | 5+ 文件 | 多处 | `friendship.deleted == 0/1` 重复 → 提取 `FriendshipEntity.isActive` 扩展 |
| L16 | CQ-15 | `gateway/.../send/DedupStep.kt` | 13-15 | `@Suppress("UNUSED_PARAMETER")` — 随 Plan 11-03 阶段 A 删除 |
| L17 | CQ-15 | `gateway/.../dispatcher/Dispatcher.kt` | 48 | `@Suppress("unused")` 减为保留 `scope`（实际被 ExceptionInterceptor 使用，移除标记） |
| L18 | CQ-15 | `common/.../exception/SendMessageException.kt` | 全文 | **删除**（死代码异常类，与 CQ-01 协调清理） |
| L19 | CQ-15 | `common/.../exception/ChatException.kt` + `MessageException.kt` | 全文 | 同一 Service 分裂两个异常类 → 合并为 `ChatException` |
| T01 | CQ-14 | `common/.../idgen/SnowflakeIdGeneratorTest.kt` | 123-126 | 反射操作 private 字段 → 抽取 `Clock` 接口（`fun millis(): Long`） |
| T02 | CQ-14 | `gateway/.../send/*Step*Test.kt` | 全文 | Step 链死代码对应的测试文件清理 |
| T03 | CQ-14 | `service/.../friend/FriendServiceTest.kt` | 全文 | 补充好友双向竞赛并发测试 |
| T04 | CQ-14 | `service/.../conversation/ConversationServiceTest.kt` | 全文 | 补充 memberCount 并发更新测试 |
| T05 | CQ-14 | `service/.../admin/DeadLetterServiceTest.kt` | 全文 | 补充 payload 补偿路径测试 |
| T06 | CQ-14 | `service/.../sequence/SeqServiceTest.kt` | 全文 | 补充 Redis 重启恢复测试 |
| T07 | CQ-14 | `gateway/.../handler/user/LoginHandlerTest.kt` | 全文 | 补充审计日志验证测试 |

## 任务

### 阶段 A：状态管理 — 魔法数字 → 枚举（CQ-12）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 1 | create | `common/src/main/kotlin/com/nebula/common/enum/ConversationType.kt` | 新建枚举 `ConversationType(val code: Int)`：`PRIVATE(1)`, `GROUP(2)`，含 `companion object { fun fromCode(code: Int) = entries.first { it.code == code } }`（L02, L20） | 编译通过 + `fromCode(1) == PRIVATE` | SQL COMMENT `1=私聊` 与枚举一致 |
| 2 | create | `common/src/main/kotlin/com/nebula/common/enum/ConversationStatus.kt` | 新建枚举 `ConversationStatus(val code: Int)`：`ACTIVE(0)`, `DISMISSED(1)`（L02） | 编译通过 | 值定义与 SQL DDL 一致 |
| 3 | create | `common/src/main/kotlin/com/nebula/common/enum/PrivacyLevel.kt` | 新建枚举 `PrivacyLevel(val code: Int)`：`PUBLIC(0)`, `FRIENDS_ONLY(1)`, `PRIVATE(2)`（L03） | 编译通过 | 值定义与 `UserEntity.privacyStatus` 使用一致 |
| 4 | create | `common/src/main/kotlin/com/nebula/common/enum/FriendRequestStatus.kt` | 新建枚举 `FriendRequestStatus(val code: Int)`：`PENDING(0)`, `ACCEPTED(1)`, `REJECTED(2)`（L04） | 编译通过 | 值定义与 `FriendRequestEntity.status` 使用一致 |
| 5 | modify | `repository/src/main/kotlin/com/nebula/repository/entity/ConversationEntity.kt` | KDoc `/** 会话类型：0=私聊, 2=群聊 */` → `/** 会话类型：1=私聊, 2=群聊（与 V1__init_schema.sql COMMENT 一致） */`（L01, L20）；如需批量修正 Service 层 `CONV_TYPE_PRIVATE` 常量，新增任务 #6 | Entity 注释与 SQL 一致 | `grep "0=私聊"` 无结果，`grep "1=私聊"` 有结果 |
| 6a | modify | `service/.../ConversationService.kt` | 检查 `CONV_TYPE_PRIVATE` / `CONV_TYPE_GROUP` 常量值，若 DB 存 `1=私聊` 则修正常量为 `ConversationType.PRIVATE.code`（L01） | Service 层常量值与 DB 一致 | `ConversationType.PRIVATE.code == 1`，创建私聊时 `conversation.type = ConversationType.PRIVATE.code` |
| 6b | modify | `service/.../FriendService.kt` | 同上检查 `CONV_TYPE_PRIVATE` 常量，修正为 `ConversationType.PRIVATE.code`（L01） | 同上 | 同上 |
| 6c | modify | `service/.../MessageService.kt` | 同上检查 `CONV_TYPE_PRIVATE` 常量，修正为 `ConversationType.PRIVATE.code`（L01） | 同上 | 同上 |

### 阶段 B：代码质量 — !! 空断言替换（CQ-15）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 7 | modify | `service/src/main/kotlin/com/nebula/service/chat/MessageService.kt` | `id!!`（行 252）→ `id ?: error("MessageEntity.id 不应为 null")`（D-86, L08） | 异常消息清晰 | NPE 被替换为 `IllegalStateException("MessageEntity.id 不应为 null")` |
| 8 | modify | `service/src/main/kotlin/com/nebula/service/user/UserService.kt` | 5 处 `user.id!!`（行 92,120,157,183,212）→ `requireNotNull(user.id) { "用户ID不能为null" }` 或 `user.id ?: error("用户ID不能为null")`（D-86, L09） | `user.id` 参数校验语义正确 | `IllegalArgumentException` / `IllegalStateException` 替代 KotlinNullPointerException |
| 9 | modify | `service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt` | 2 处 `id!!`（行 151,164）→ `it.id ?: error("会话ID不能为null")`（D-86, L10） | 同上 | 同上 |
| 10 | modify | `gateway/src/main/kotlin/com/nebula/gateway/service/ChatService.kt` | `merge().!!`（行 253,312）→ 前置 `val mergeResult = merge(); requireNotNull(mergeResult) { "gRPC 流合并结果不能为null" }`（D-86, L11） | 空值在方法调用前被检测 | 异常消息包含上下文信息 |
| 11 | modify | `repository/src/main/kotlin/com/nebula/repository/config/JpaConfig.kt` | `emfBean.getObject()!!`（行 47）→ `emfBean.getObject() ?: error("EntityManagerFactory 未初始化")`（D-86, L12） | 启动阶段异常消息有意义 | `IllegalStateException("EntityManagerFactory 未初始化")` vs `KotlinNullPointerException` |

### 阶段 C：代码质量 — DRY 重构（CQ-15）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 12 | create | `repository/src/main/kotlin/com/nebula/repository/entity/ConversationMemberEntity.kt`（同文件追加） | 在 `ConversationMemberEntity.kt` 文件底部新增扩展属性：`/** 成员是否为活跃状态（未软删除）（D-86）。 */ val ConversationMemberEntity.isActive: Boolean get() = deleted == 0`（L06, L13） | 所有 `member.deleted == 0` 可替换为 `member.isActive` | `member.deleted == 0` 搜索在非 entity 文件外为 0 |
| 13 | modify | 4+ 处使用点（`ConversationService.kt`、`FriendService.kt` 等） | 将 `member == null \|\| member.deleted == 1` 替换为 `member == null \|\| !member.isActive`；将 `member.deleted == 0` 替换为 `member.isActive`（L06, L13） | 行为语义不变 | 替换后所有相关测试通过 |
| 14 | create | `repository/src/main/kotlin/com/nebula/repository/entity/FriendshipEntity.kt`（同文件追加） | 新增扩展属性：`/** 好友关系是否有效（未软删除）（D-86）。 */ val FriendshipEntity.isActive: Boolean get() = deleted == 0`（L05, L15） | 所有 `friendship.deleted == 0` 可替换为 `friendship.isActive` | 同上 |
| 15 | modify | 5+ 处使用点（`FriendService.kt` 等） | 将 `friendship.deleted == 0/1` 替换为 `friendship.isActive` / `!friendship.isActive`（L15） | 行为语义不变 | 同上 |
| 16 | create | `repository/src/main/kotlin/com/nebula/repository/repository/ConversationRepository.kt`（同文件追加） | 新增 suspend 扩展函数：`suspend fun ConversationRepository.findByIdOrNull(id: String): ConversationEntity? = withContext(Dispatchers.IO) { findById(id).orElse(null) }`（L14） | 重复 9 次的 `findById().orElse(null)` 模式收拢 | 搜索 `findById(.*).orElse(null)` 仅出现在扩展函数定义中 |
| 17 | modify | 9 处使用点（`ConversationService.kt`、`FriendService.kt`、`MessageService.kt` 等） | 将 `withContext(Dispatchers.IO) { conversationRepository.findById(convId).orElse(null) }` 替换为 `conversationRepository.findByIdOrNull(convId)`（L14） | 行为语义不变 | 替换后所有相关测试通过 |

### 阶段 D：异常类型精简 + 死代码标记清理（CQ-15）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 18 | modify | `common/src/main/kotlin/com/nebula/common/exception/MessageException.kt` | 将 `MessageException` 合并到 `ChatException`：`MessageException` 调用点替换为 `ChatException`，保留 `MessageException` 文件为 typealias `typealias MessageException = ChatException`（过渡期）或直接删除（L19） | 编译通过 + 异常捕获不丢失 | `catch (e: ChatException)` 可捕获原 `MessageException` 抛出的异常 |
| 19 | modify | `common/src/main/kotlin/com/nebula/common/exception/SendMessageException.kt` | **删除**文件（L18，`common` 层死代码异常类，与 11-03 M07 删除的 `gateway` 层 `SendMessageException.kt` 是不同文件 — 两者均需删除） | 编译通过 | `SendMessageException` 无引用 |
| 20 | modify | `gateway/src/main/kotlin/com/nebula/gateway/dispatcher/Dispatcher.kt` | 移除 `@Suppress("unused")` 标记（行 48），`scope` 已被 `ExceptionInterceptor` 使用（L17） | Lint 检查通过 | IDE 无 "unused" 警告（因为 scope 实际被使用） |

### 阶段 E：测试加固 — Clock 接口抽取（CQ-14 — T01）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 21 | create | `common/src/main/kotlin/com/nebula/common/idgen/Clock.kt` | 新建接口 `interface Clock { fun millis(): Long }` + `class SystemClock : Clock { override fun millis() = System.currentTimeMillis() }`（T01） | 编译通过 | `SystemClock().millis()` 返回值与 `System.currentTimeMillis()` 一致 |
| 22 | modify | `common/src/main/kotlin/com/nebula/common/idgen/SnowflakeIdGenerator.kt` | 构造参数新增 `private val clock: Clock = SystemClock()`（默认参数保持向后兼容）（T01） | 现有测试不修改仍然通过 | `SnowflakeIdGenerator(workerId = 1)` 行为保持不变 |
| 23 | modify | `common/src/test/kotlin/com/nebula/common/idgen/SnowflakeIdGeneratorTest.kt` | 删除反射操作 private 字段 `lastTimestamp` 的代码（行 123-126）；新建 `FakeClock(var current: Long) : Clock`；测试用 `SnowflakeIdGenerator(workerId = 1, clock = FakeClock(...))` 注入可控时钟（T01） | 时钟回拨测试无需反射 | `testClockBackwardShouldThrow()` 使用 `FakeClock` 而非反射 |

### 阶段 F：测试补充 — 5 个关键场景（CQ-14 — T02-T07）

| # | 类型 | 文件 | 操作 | 验证 | 验收标准 |
|---|------|------|------|------|---------|
| 24 | test | `gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/` | 删除 Step 链死代码对应的测试文件（`*Step*Test.kt`），确认无遗漏（T02） | 编译通过 | `find . -name "*Step*Test*"` 无结果 |
| 25 | test | `service/src/test/kotlin/com/nebula/service/friend/FriendServiceTest.kt` | 新增并发测试 `addFriend 双向竞赛只应创建一对好友关系`：使用 `runBlocking` + `coroutineScope { launch { service.addFriend(A, B) }; launch { service.addFriend(B, A) } }` 并发调用，验证 `friendshipRepository.count()` == 2（A→B + B→A）（T03, D-80） | 并发执行 10 次，每次结果一致 | friendships 表无重复对 (A,B) 和 (B,A) |
| 26 | test | `service/src/test/kotlin/com/nebula/service/conversation/ConversationServiceTest.kt` | 新增并发测试 `memberCount 并发更新应保持一致性`：使用 `coroutineScope { repeat(10) { launch { service.inviteMember(...) }; launch { service.leaveGroup(...) } } }`，验证最终 `member_count = SELECT COUNT(*) WHERE deleted=0`（T04, D-82） | 并发执行后 memberCount 一致 | `conversation.member_count == countActiveMembers()` |
| 27 | test | `service/src/test/kotlin/com/nebula/service/admin/DeadLetterServiceTest.kt` | 新增测试 `compensate 应从 DeadLetterEntity.payload 恢复而非写入空串`：创建带 payload 的死信 → 调用 `compensate()` → Mock `MessageQueueRepository.enqueue()` 验证 `fields["payload"]` 为 Base64 编码（T05, D-75） | payload 非空 | `fields["payload"]` 长度 > 0 且可 Base64 解码回原始 bytes |
| 28 | test | `service/src/test/kotlin/com/nebula/service/sequence/SeqServiceTest.kt` | 新增测试 `Redis 重启后序列号应从 MySQL 恢复`：写入 5 条消息（seq 1-5）→ `FLUSHALL` Redis → 调用恢复逻辑 → 验证下一条消息 seq=6（T06, D-81） | Redis 清空后恢复正确 | `seqService.nextSeq(convId)` 返回 6 |
| 29 | test | `gateway/src/test/kotlin/com/nebula/gateway/handler/user/LoginHandlerTest.kt` | 新增测试 `登录成功应写入审计日志`：Mock `ListAppender` 附加到 `AUDIT_LOGIN` Marker logger → 调用 `handler.handle(loginReq)` → 验证日志事件包含 `"user_login"`, `"success=true"`（T07） | 审计日志有记录 | `listAppender.list.any { it.marker?.name == "AUDIT_LOGIN" && it.formattedMessage.contains("user_login") }` |

## 依赖

- **Plan 11-01**：依赖 H10 审计日志（T07 测试审计 Marker 存在）
- **Plan 11-02**：依赖 D-80/D-82（T03/T04 测试验证事务和原子更新修复）
- **Plan 11-03**：依赖 M09/M10 payload 修复（T05 测试验证）；依赖阶段 A 死代码清理（T02 测试清理 + L16/L18）
- **内部依赖链**：A ∥ B ∥ D ∥ E → C → F
  - 阶段 C（DRY）依赖 A（枚举就绪后才能统一替换）
  - 阶段 F（测试补充）依赖所有前序修复完成

## 产出物

### 新建文件
- `common/src/main/kotlin/com/nebula/common/enum/ConversationType.kt`
- `common/src/main/kotlin/com/nebula/common/enum/ConversationStatus.kt`
- `common/src/main/kotlin/com/nebula/common/enum/PrivacyLevel.kt`
- `common/src/main/kotlin/com/nebula/common/enum/FriendRequestStatus.kt`
- `common/src/main/kotlin/com/nebula/common/idgen/Clock.kt`

### 修改文件
- `repository/src/main/kotlin/com/nebula/repository/entity/ConversationEntity.kt` — KDoc 修正
- `repository/src/main/kotlin/com/nebula/repository/entity/ConversationMemberEntity.kt` — `isActive` 扩展
- `repository/src/main/kotlin/com/nebula/repository/entity/FriendshipEntity.kt` — `isActive` 扩展
- `repository/src/main/kotlin/com/nebula/repository/repository/ConversationRepository.kt` — `findByIdOrNull` 扩展
- `common/src/main/kotlin/com/nebula/common/idgen/SnowflakeIdGenerator.kt` — Clock 接口注入
- `common/src/main/kotlin/com/nebula/common/exception/MessageException.kt` — 合并到 ChatException
- `service/src/main/kotlin/com/nebula/service/chat/MessageService.kt` — !! 替换 + 常量修正
- `service/src/main/kotlin/com/nebula/service/user/UserService.kt` — 5 处 !! 替换
- `service/src/main/kotlin/com/nebula/service/conversation/ConversationService.kt` — !! 替换 + DRY + 常量修正
- `service/src/main/kotlin/com/nebula/service/friend/FriendService.kt` — DRY + 常量修正
- `gateway/src/main/kotlin/com/nebula/gateway/service/ChatService.kt` — !! 替换
- `gateway/src/main/kotlin/com/nebula/gateway/dispatcher/Dispatcher.kt` — 移除 @Suppress
- `repository/src/main/kotlin/com/nebula/repository/config/JpaConfig.kt` — !! 替换

### 删除文件
- `common/src/main/kotlin/com/nebula/common/exception/SendMessageException.kt`

### 测试文件（修改/新建）
- `common/src/test/kotlin/com/nebula/common/idgen/SnowflakeIdGeneratorTest.kt` — Clock 注入替代反射
- `service/src/test/kotlin/com/nebula/service/friend/FriendServiceTest.kt` — 并发测试
- `service/src/test/kotlin/com/nebula/service/conversation/ConversationServiceTest.kt` — 并发测试
- `service/src/test/kotlin/com/nebula/service/admin/DeadLetterServiceTest.kt` — payload 测试
- `service/src/test/kotlin/com/nebula/service/sequence/SeqServiceTest.kt` — 恢复测试
- `gateway/src/test/kotlin/com/nebula/gateway/handler/user/LoginHandlerTest.kt` — 审计日志测试
- `gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/*Step*Test.kt` — **删除**

## 验证

1. **编译验证**：`./gradlew compileKotlin` — 所有模块编译通过
2. **枚举验证**：`ConversationType.PRIVATE.code == 1` 与 SQL DDL 一致
3. **空断言验证**：`grep "!!"` 在主源码中减少 12 处，仅保留合理使用（如 `readOnly()!!`）
4. **DRY 验证**：`grep "deleted == 0"` 仅在 entity 定义和扩展属性中出现，`grep "findById(.*).orElse(null)"` 仅出现在扩展函数中
5. **测试验证**：`./gradlew test` — 所有 257+ 测试通过（含新增 6 个测试），无 Step 链残留测试
6. **审计日志验证**：`T07` 测试验证 `AUDIT_LOGIN` Marker 日志事件
7. **集成测试**：Wave 3 全量回归测试通过（`./gradlew test`）

## 风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| `CONV_TYPE_PRIVATE` 常量值修正（0→1）导致现有数据不兼容 | 现有会话 type 字段值错误 | 先确认 DB 实际存储值：若 DB 存 1 则代码 0 是 Bug 必须修；若 DB 存 0 则需 DataGrip 修正 + Flyway `UPDATE conversations SET type=1 WHERE type=0` |
| `MessageException` 合并为 `ChatException` 破坏外部 catch | 调用方异常捕获失败 | `MessageException` 保留为 `typealias` 过渡期，确认无外部 catch 后移除 |
| 枚举引入后 JSON 序列化行为变化 | API 返回格式变化 | 若使用 Jackson，添加 `@JsonValue fun toCode() = code` 确保序列化为数字 |
| DRY 提取函数引入的 import 变更范围大 | 编译错误（未 import 扩展函数） | IDE auto-import 辅助；编译前 `grep` 所有使用点确认 import |

## PLANNING COMPLETE

---
phase: 01-project-scaffolding-proto-definitions
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - proto/src/main/proto/nebula/envelope.proto
  - proto/src/main/proto/nebula/common/common.proto
  - proto/src/main/proto/nebula/message_type.proto
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-06
  - PROTO-07
user_setup: []
must_haves:
  truths:
    - "envelope.proto 定义了 Direction 枚举（REQUEST/RESPONSE/PUSH/PING/PONG 5 个变体）"
    - "Envelope 消息包含 direction、request_id（UUIDv4）、protocol_version、oneof payload（Request/Response/Message）"
    - "Request 消息包含 method 字符串和 bytes params"
    - "Response 消息包含 code、msg、method、bytes result"
    - "Message（推送）消息包含 MessageType、content、bytes payload"
    - "common.proto 定义了 DeviceType 枚举（MOBILE/DESKTOP/WEB）"
    - "message_type.proto 定义了 MessageType 枚举，包含 14 个条目（TEXT、TEXT_AND_IMAGE、FRIEND_REQUEST、FRIEND_ACCEPTED、LOGOUT、GROUP_CREATED、GROUP_INVITED、GROUP_DISSOLVED、GROUP_UPDATED、MEMBER_JOINED、MEMBER_LEFT、MEMBER_KICKED、READ_RECEIPT、DELIVERY_ACK）"
  artifacts:
    - path: "proto/src/main/proto/nebula/envelope.proto"
      provides: "信封协议：Direction、Envelope、Request、Response、Message"
      min_lines: 60
    - path: "proto/src/main/proto/nebula/common/common.proto"
      provides: "公共类型：DeviceType 枚举"
      min_lines: 60
    - path: "proto/src/main/proto/nebula/message_type.proto"
      provides: "MessageType 枚举（14 个推送事件类型）"
      min_lines: 50
  key_links:
    - from: "envelope.proto"
      to: "common.proto"
      via: "import 路径"
      pattern: "import.*common"
    - from: "envelope.proto"
      to: "message_type.proto"
      via: "Message.messageType 字段引用 MessageType 枚举"
      pattern: "MessageType"
    - from: "common.proto"
      to: "conversation/conversation.proto, group/group.proto, user/user.proto, friend/friend.proto"
      via: "import 路径（Plan C 的领域文件引用 common 类型）"
      pattern: "import.*common"
---

<objective>
定义 Nebula Chat Server 的核心协议契约 —— envelope.proto（信封协议）、common.proto（公共类型）、message_type.proto（推送事件类型枚举）。

Purpose: 这三个文件是协议层的基础。envelope.proto 定义所有 gRPC 双向流消息的统一信封格式；common.proto 定义被多个领域复用的类型；message_type.proto 独立成文件（D-04），使后续扩展推送类型不碰信封结构。生成的 Java/Kotlin 桩代码被所有 6 个模块依赖。
Output: 3 个 .proto 文件，位于 proto/src/main/proto/nebula/ 目录
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.1-MessageType枚举.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.2-Payload结构体.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.3-请求响应消息/common.proto.md
@/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md

<interfaces>
<!-- Design doc proto definitions to implement -->

From 3.3.1-MessageType枚举.md:
proto 结构：
- message Request: method(string), params(bytes)
- message Response: code(int32), msg(string), method(string), result(bytes)
- message Message: messageType(MessageType), content(string), payload(bytes)
- enum MessageType: TEXT=0, TEXT_AND_IMAGE=1, FRIEND_REQUEST=3, FRIEND_ACCEPTED=5, LOGOUT=4, GROUP_CREATED=6, GROUP_INVITED=7, GROUP_DISSOLVED=8, GROUP_UPDATED=9, MEMBER_JOINED=10, MEMBER_LEFT=11, MEMBER_KICKED=12, READ_RECEIPT=13, DELIVERY_ACK=14

From 3.3.2-Payload结构体.md:
Message.payload 对应的 10 个具体结构体（MessageType TEXT/LOGOUT 无 payload）
Payload 结构体在 domain proto 文件中定义（Plan C），不在本文件

From common.proto.md:
公共类型在 common/common.proto 中定义：
- DeviceType: DEVICE_TYPE_UNSPECIFIED=0, MOBILE=1, DESKTOP=2, WEB=3
- UserBrief: uid(int64), username(string), display_name(string), avatar_url(string)
- ConversationBrief: conversation_id(string), type(string), name(string), avatar_url(string), last_message_id(int64), last_message_preview(string), last_message_ts(int64), last_updated_at(int64), last_read_msg_id(int64)
- GroupMember: uid(int64), username(string), display_name(string), avatar_url(string), role(string), joined_at(int64)
- UserOnlineStatus: uid(int64), status(int32), last_active_at(int64)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nebula/envelope.proto — Direction 枚举 + Envelope/Request/Response/Message 消息定义</name>
  <files>proto/src/main/proto/nebula/envelope.proto</files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.1-MessageType枚举.md — Request/Response/Message 结构体定义
    @/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md — Common Operation 5 (envelope.proto 完整模板)
  </read_first>
  <action>
    D-01: Proto package 声明为 `com.nebula.chat`。
    D-02: envelope.proto 放在根包（即 java_package = "com.nebula.chat"）。
    D-04: MessageType 不在 envelope.proto 中定义。
    D-09: Request.params / Response.result / Message.payload 保持 bytes 类型。
    PROTO-06: Direction 枚举包含 PING=4 和 PONG=5 变体（心跳方向定义）。
    PROTO-07: 路由机制在 Proto 层的体现是 Request.method 字符串字段。

    创建 proto/src/main/proto/nebula/envelope.proto：

    前置：确保 proto/src/main/proto/nebula/ 和 proto/src/main/proto/nebula/common/ 目录已存在（由 01-01 Task 2 创建，此处做防御性创建）。运行 `mkdir -p proto/src/main/proto/nebula proto/src/main/proto/nebula/common`。

    1. syntax = "proto3"
    2. package = "com.nebula.chat"
    3. option java_multiple_files = true
    4. option java_package = "com.nebula.chat"
    5. import 声明留空（envelope.proto 不依赖其他 proto）
    
    6. **enum Direction**: 必须包含 6 个变体：
       - DIRECTION_UNSPECIFIED = 0
       - REQUEST = 1
       - RESPONSE = 2
       - PUSH = 3
       - PING = 4  (per PROTO-06 心跳方向)
       - PONG = 5  (per PROTO-06 心跳方向)
    
    7. **message Envelope**: 字段号分配如下：
       - Direction direction = 1
       - string request_id = 2 (UUIDv4, 36 字符)
       - int32 protocol_version = 3 (初始值 1，运行时由服务器设置)
       - oneof payload { Request request = 10; Response response = 11; Message message = 12; }
    
    8. **message Request**: 字段号：
       - string method = 1 ("user/login" 等格式)
       - bytes params = 2 (D-09 bytes 类型)
    
    9. **message Response**: 字段号：
       - int32 code = 1 (200 成功，其余业务错误码)
       - string msg = 2
       - string method = 3 (原样回显请求 method)
       - bytes result = 4 (D-09 bytes 类型)
    
    10. **message Message**: 字段号：
       - MessageType messageType = 1 (此处仅字段声明，类型在 message_type.proto)
       - string content = 2
       - bytes payload = 3 (D-09 bytes 类型)

    注意：虽然 Message 引用了 MessageType，但它不在本文件定义。proto 编译器允许 forward reference 到导入文件的类型。MessageType 的 import 在 Task 3 创建的 message_type.proto 中定义，但实际 Message 对 MessageType 的引用在 envelope.proto 内——所以必须在 envelope.proto 头部添加 `import "nebula/message_type.proto"`。
  </action>
  <verify>
    <automated>grep -c "enum Direction" proto/src/main/proto/nebula/envelope.proto && grep -c "message Envelope" proto/src/main/proto/nebula/envelope.proto && grep -c "message Request" proto/src/main/proto/nebula/envelope.proto && grep -c "Response" proto/src/main/proto/nebula/envelope.proto && grep -c "MessageType" proto/src/main/proto/nebula/envelope.proto</automated>
  </verify>
  <acceptance_criteria>
    - envelope.proto 存在于 proto/src/main/proto/nebula/ 目录
    - 包含 Direction 枚举（DIRECTION_UNSPECIFIED=0, REQUEST=1, RESPONSE=2, PUSH=3, PING=4, PONG=5）
    - 包含 Envelope 消息（direction, request_id, protocol_version, oneof payload {request, response, message}）
    - 包含 Request 消息（method string, bytes params）
    - 包含 Response 消息（code int32, msg string, method string, bytes result）
    - 包含 Message 消息（MessageType messageType, content string, bytes payload）
    - 包含 import "nebula/message_type.proto"
    - java_package = "com.nebula.chat"
  </acceptance_criteria>
  <done>envelope.proto 创建完毕，包含 Direction 枚举 + 4 个消息定义（Envelope/Request/Response/Message），符合 PROTO-01~04 和 PROTO-06</done>
</task>

<task type="auto">
  <name>Task 2: Create common/common.proto — DeviceType、UserBrief、UserOnlineStatus、ConversationBrief、GroupMember</name>
  <files>proto/src/main/proto/nebula/common/common.proto</files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.3-请求响应消息/common.proto.md — 5 个公共类型完整结构体定义
  </read_first>
  <action>
    D-02: common.proto 在 `com.nebula.chat.common` 包（子包 common）。
    D-05: UserBrief/UserOnlineStatus → user/user.proto（不在此文件），ConversationBrief → conversation/conversation.proto（不在此文件），GroupMember → group/group.proto（不在此文件）。但 common.proto.md 设计文档中 UserBrief、UserOnlineStatus、ConversationBrief、GroupMember 都在 common.proto 中——然而 D-05 明确说这些类型分别移到各自的领域文件。所以本文件只定义 DeviceType。
    
    等等，让我重新检查 D-05 的描述。
    D-05: "`UserBrief`/`UserOnlineStatus` → `user/user.proto`，`ConversationBrief` → `conversation/conversation.proto`，`GroupMember` → `group/group.proto`。`DeviceType`/`ErrorCode` 留在 `common.proto`"
    
    所以 D-05 将 UserBrief 和 UserOnlineStatus 移到了 user/user.proto，ConversationBrief 移到了 conversation/conversation.proto，GroupMember 移到了 group/group.proto。common.proto 只保留 DeviceType 枚举。
    
    但是 common.proto.md 设计文档中这些类型都在 common.proto 里定义。D-05 是用户决策，覆盖了设计文档分类。
    
    等等不对，再看一下——re-read common.proto.md: "UserBrief, UserOnlineStatus, ConversationBrief, GroupMember" 都在 common.proto 里。但 D-05 把它们移走了。所以 common.proto 本阶段只需要 DeviceType。
    
    不过，所有的领域 proto 文件都会 import "nebula/common/common.proto" 来引用 DeviceType。那些被移走的类型会在各自的 domain proto 中定义（Plan C）。
    
    所以 common.proto 包含：
    1. enum DeviceType: DEVICE_TYPE_UNSPECIFIED=0, MOBILE=1, DESKTOP=2, WEB=3
    
    syntax = "proto3"
    package = "com.nebula.chat.common"
    option java_multiple_files = true
    option java_package = "com.nebula.chat.common"
  </action>
  <verify>
    <automated>grep -c "DeviceType" proto/src/main/proto/nebula/common/common.proto</automated>
  </verify>
  <acceptance_criteria>
    - common.proto 存在于 proto/src/main/proto/nebula/common/ 目录
    - package = "com.nebula.chat.common"
    - java_package = "com.nebula.chat.common"
    - 包含 DeviceType 枚举（DEVICE_TYPE_UNSPECIFIED=0, MOBILE=1, DESKTOP=2, WEB=3）
    - 按 D-05 不包含 UserBrief/UserOnlineStatus/ConversationBrief/GroupMember
  </acceptance_criteria>
  <done>common.proto 创建完毕，包含 DeviceType 枚举</done>
</task>

<task type="auto">
  <name>Task 3: Create message_type.proto — MessageType 枚举（14 个推送事件类型）</name>
  <files>proto/src/main/proto/nebula/message_type.proto</files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/03-通信协议/3.3-内层消息与Payload/3.3.1-MessageType枚举.md — MessageType 枚举完整定义（14 个枚举值）
  </read_first>
  <action>
    D-04: MessageType 独立成文件 message_type.proto，后续扩展新增消息类型时不需修改 envelope.proto。
    
    package = "com.nebula.chat"（根包），java_package = "com.nebula.chat"。
    
    创建 nebula/message_type.proto，包含全部 14 个 MessageType 枚举值（from 3.3.1 设计文档）：
    
    enum MessageType {
      TEXT = 0;
      TEXT_AND_IMAGE = 1;
      FRIEND_REQUEST = 3;
      LOGOUT = 4;
      FRIEND_ACCEPTED = 5;
      GROUP_CREATED = 6;
      GROUP_INVITED = 7;
      GROUP_DISSOLVED = 8;
      GROUP_UPDATED = 9;
      MEMBER_JOINED = 10;
      MEMBER_LEFT = 11;
      MEMBER_KICKED = 12;
      READ_RECEIPT = 13;
      DELIVERY_ACK = 14;
    }
    
    注意字段编号：TEXT=0, TEXT_AND_IMAGE=1, 然后跳跃到 FRIEND_REQUEST=3（2 被跳过，可能预留）。
    设计文档中对应值编号准确。
    
    java_multiple_files = true。
  </action>
  <verify>
    <automated>grep -c "TEXT = 0" proto/src/main/proto/nebula/message_type.proto && grep -c "DELIVERY_ACK = 14" proto/src/main/proto/nebula/message_type.proto</automated>
  </verify>
  <acceptance_criteria>
    - message_type.proto 存在于 proto/src/main/proto/nebula/ 目录
    - package = "com.nebula.chat"
    - java_package = "com.nebula.chat"
    - 包含 enum MessageType，14 个枚举值（TEXT=0 到 DELIVERY_ACK=14）
    - java_multiple_files = true
  </acceptance_criteria>
  <done>message_type.proto 创建完毕，MessageType 枚举包含全部 14 个推送事件类型</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .proto 源文件 → Git | proto 文件通过 Git 版本控制，本地篡改可通过 git diff 检测 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | .proto 源文件 | mitigate | 所有 .proto 文件受 Git 追踪，PR code review 时通过 git diff 审查变更 |
</threat_model>

<verification>
- [ ] Task 1: envelope.proto 包含 Direction、Envelope、Request、Response、Message
- [ ] Task 2: common.proto 包含 DeviceType（不含 D-05 移走的类型）
- [ ] Task 3: message_type.proto 包含全部 14 个 MessageType 枚举值
</verification>

<success_criteria>
1. 3 个 .proto 文件存在于 proto/src/main/proto/nebula/ 目录
2. envelope.proto 定义完整信封协议 + 心跳方向
3. common.proto 定义 DeviceType（按 D-05 约束）
4. message_type.proto 独立定义 14 个推送事件类型（D-04）
5. 所有文件语法正确，package 声明按 D-01/D-02 规范
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffolding-proto-definitions/01-02-SUMMARY.md` when done
</output>

---
phase: 01-project-scaffolding-proto-definitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - build.gradle.kts
  - gradle/libs.versions.toml
  - gradle/wrapper/gradle-wrapper.properties
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.jar
  - proto/build.gradle.kts
  - common/build.gradle.kts
  - repository/build.gradle.kts
  - service/build.gradle.kts
  - gateway/build.gradle.kts
  - server/build.gradle.kts
  - .gitignore
  - .editorconfig
  - .gitattributes
  - README.md
  - LICENSE
  - .idea/codeStyles/Project.xml
  - .idea/codeStyles/codeStyleConfig.xml
  - .idea/runConfigurations/
  - .idea/vcs.xml
  - .idea/misc.xml
  - .idea/compiler.xml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-06
user_setup: []
must_haves:
  truths:
    - "6 个 Gradle 子模块被 settings.gradle.kts 正确 include"
    - "libs.versions.toml 定义了 protobuf、kotlin-logging、logback 等版本"
    - "gradle wrapper 可用，/gradlew build 能触发构建"
    - "6 个模块的 build.gradle.kts 依赖方向正确：proto ← common ← repository ← service ← gateway ← server"
    - "项目根目录存在 .gitignore、.editorconfig、.gitattributes、README.md、LICENSE（MIT）"
    - ".idea/ 目录选择性提交：codeStyles/、runConfigurations/、vcs.xml、misc.xml、compiler.xml 提交；workspace.xml、tasks.xml 忽略"
    - "所有.proto 源文件最终位于 proto/src/main/proto/ 目录"
  artifacts:
    - path: "settings.gradle.kts"
      provides: "6 模块注册 + pluginManagement + dependencyResolutionManagement"
      contains: "include(\":proto\", \":common\", \":repository\", \":service\", \":gateway\", \":server\")"
    - path: "gradle/libs.versions.toml"
      provides: "集中版本声明"
      contains: "protobuf = \"4.29.3\""
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 8.10 wrapper"
      contains: "gradle-8.10"
    - path: "proto/build.gradle.kts"
      provides: "proto 模块骨架（不含 protobuf 插件 — Plan D 添加）"
      contains: "plugins { id(\"org.jetbrains.kotlin.jvm\") }"
    - path: "common/build.gradle.kts"
      provides: "common 模块骨架"
      contains: "implementation(project(\":proto\"))"
    - path: "repository/build.gradle.kts"
      provides: "repository 模块骨架"
      contains: "implementation(project(\":common\"))"
    - path: "service/build.gradle.kts"
      provides: "service 模块骨架"
      contains: "implementation(project(\":repository\"))"
    - path: "gateway/build.gradle.kts"
      provides: "gateway 模块骨架"
      contains: "implementation(project(\":service\"))"
    - path: "server/build.gradle.kts"
      provides: "server 模块骨架（入口模块、依赖 gateway）"
      contains: "implementation(project(\":gateway\"))"
    - path: ".gitignore"
      provides: "标准 Git 忽略规则"
      contains: ".gradle/"
    - path: "LICENSE"
      provides: "MIT 许可证文件"
      contains: "MIT"
  key_links:
    - from: "settings.gradle.kts"
      to: "root build.gradle.kts"
      via: "pluginManagement 注册插件"
      pattern: "pluginManagement"
    - from: "gradle/libs.versions.toml"
      to: "6 个 build.gradle.kts"
      via: "libs.* 版本引用"
      pattern: "libs\\."
    - from: "common/build.gradle.kts"
      to: "proto/build.gradle.kts"
      via: "implementation(project(\":proto\"))"
      pattern: "project.*:proto"
---

<objective>
搭建 Nebula Chat Server 的 Gradle 多模块工程骨架——包含 6 个子模块的构建系统、依赖版本目录、Gradle wrapper、以及标准工程脚手架文件。

Purpose: 为所有后续阶段提供可编译的构建基础。6 个模块的依赖方向（proto ← common ← repository ← service ← gateway ← server）通过 Gradle implementation project() 配置强制实施，阻止反向引用。
Output: settings.gradle.kts、根 build.gradle.kts、gradle/libs.versions.toml、Gradle wrapper、6 个模块的 build.gradle.kts 骨架、.gitignore、.editorconfig、.gitattributes、README.md、LICENSE、.idea/ 选择性提交配置
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/09-基础设施设计/9.1-Gradle多模块.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/02-项目目录结构.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/13-编码与规范.md

<interfaces>
<!-- Greenfield project — no existing code. All patterns from design docs and RESEARCH.md. -->

From 设计文档/9.1-Gradle多模块.md:
- 6 modules: proto, common, repository, service, gateway, server
- Dependency chain: proto ← common ← repository ← service ← gateway ← server
- All modules depend on proto's generated stubs

From RESEARCH.md:
- Gradle 8.10, Kotlin 2.1.20, protobuf 4.29.3, protobuf-plugin 0.10.0
- kotlin-logging 8.0.4, logback 1.5.34, SLF4J 2.0.18
- libs.versions.toml manages all version declarations
- settings.gradle.kts: rootProject.name = "nebula-server"
</interfaces>

From CONTEXT.md (D-11 through D-15):
- D-11: Standard init files: Gradle wrapper, .gitignore, README.md, .editorconfig, .gitattributes
- D-12: MIT license
- D-13: libs.versions.toml for version catalog
- D-15: .idea/ selective commit policy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gradle build system — settings.gradle.kts、root build.gradle.kts、libs.versions.toml、wrapper</name>
  <files>
    settings.gradle.kts
    build.gradle.kts
    gradle/libs.versions.toml
    gradle/wrapper/gradle-wrapper.properties
    gradlew
    gradlew.bat
    gradle/wrapper/gradle-wrapper.jar
  </files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/09-基础设施设计/9.1-Gradle多模块.md — 模块结构和命名规范
    @/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md — 完整的 Common Operation 1~4 代码模板
  </read_first>
  <action>
    D-13 要求通过 libs.versions.toml 集中管理版本。按以下配置创建构建系统：

    1. **settings.gradle.kts**: pluginManagement 中配置 gradlePluginPortal + mavenCentral，dependencyResolutionManagement 设置为 PREFER_PROJECT，repositories 配置 mavenCentral。rootProject.name = "nebula-server"。include 6 个模块 sequence：:proto, :common, :repository, :service, :gateway, :server（顺序无关但保持一致）。
    
    2. **根 build.gradle.kts**: 声明 `kotlin("jvm") version "2.1.20" apply false` 和 `id("com.google.protobuf") version "0.10.0" apply false` 两个插件（apply false —— 子模块按需启用）。repositories { mavenCentral() }。`group = "com.nebula"`，`version = "1.0.0-SNAPSHOT"`。
    
    3. **gradle/libs.versions.toml**: 严格遵循 RESEARCH.md Common Operation 4 模板。必须包含以下 versions：kotlin = "2.1.20", protobuf = "4.29.3", protobuf-plugin = "0.10.0", kotlin-logging = "8.0.4", logback = "1.5.34", slf4j = "2.0.18"。libraries 包含 protobuf-java、protobuf-kotlin、protoc、kotlin-logging、logback-classic、slf4j-api。plugins 包含 kotlin-jvm 和 protobuf。
    
    4. **Gradle Wrapper**: 若 gradle wrapper 未安装，执行 `gradle wrapper --gradle-version 8.10` 生成。若已存在，修改 gradle/wrapper/gradle-wrapper.properties 中 distributionUrl 指向 `gradle-8.10-bin.zip`。确保 gradlew 可执行（`chmod +x gradlew`）。
  </action>
  <verify>
    <automated>./gradlew --version 2>&1 | grep -q "8.10"</automated>
  </verify>
  <acceptance_criteria>
    - `settings.gradle.kts` 包含 6 个模块的 include 声明且 rootProject.name = "nebula-server"
    - `build.gradle.kts` 声明 kotlin-jvm 和 protobuf 插件（apply false），group = "com.nebula"，version = "1.0.0-SNAPSHOT"
    - `gradle/libs.versions.toml` 定义 kotlin、protobuf、protobuf-plugin 三个版本的 versions 条目，以及对应的 libraries 和 plugins
    - `gradle/wrapper/gradle-wrapper.properties` 的 distributionUrl 指向 gradle-8.10
    - `./gradlew --version` 输出 Gradle 版本为 8.10
  </acceptance_criteria>
  <done>settings.gradle.kts、root build.gradle.kts、libs.versions.toml、Gradle wrapper 全部就绪，./gradlew --version 显示 Gradle 8.10</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 module build.gradle.kts with dependency direction enforcement</name>
  <files>
    proto/build.gradle.kts
    common/build.gradle.kts
    repository/build.gradle.kts
    service/build.gradle.kts
    gateway/build.gradle.kts
    server/build.gradle.kts
  </files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/nebula_server/settings.gradle.kts（Task 1 产物）
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/09-基础设施设计/9.1-Gradle多模块.md
    @/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md — Common Operation 3 (proto/build.gradle.kts prototype)
  </read_first>
  <action>
    D-05 要求 ConversationBrief/GroupMember 等类型定义在对应 proto 文件中。D-08 要求 proto 模块通过 git submodule 集成 nebula-proto（Plan D 添加 submodule 配置，本任务创建模块骨架）。

    为每个模块创建 build.gradle.kts，依赖方向严格遵循 INFRA-06：proto ← common ← repository ← service ← gateway ← server。每个模块只声明指向下层模块的 implementation project()，禁止反向引用。

    **proto/build.gradle.kts**: plugins = `kotlin("jvm")`（不含 protobuf plugin — Plan D 添加）。jvmToolchain(21)。依赖留空（Plan D 添加 protobuf 依赖）。在文件末尾添加注释标记 `// PROTO-PLUGIN-CONFIG-HERE` 供 Plan D 定位替换。

    **common/build.gradle.kts**: plugins = `kotlin("jvm")`。dependencies = `implementation(project(":proto"))` + `implementation(libs.kotlin.logging)` + `implementation(libs.slf4j.api)` + `implementation(libs.logback.classic)`。jvmToolchain(21)。

    **repository/build.gradle.kts**: plugins = `kotlin("jvm")`。dependencies = `implementation(project(":common"))`。jvmToolchain(21)。

    **service/build.gradle.kts**: plugins = `kotlin("jvm")`。dependencies = `implementation(project(":repository"))`。jvmToolchain(21)。

    **gateway/build.gradle.kts**: plugins = `kotlin("jvm")`。dependencies = `implementation(project(":service"))` + `implementation(project(":proto"))`。jvmToolchain(21)。

    **server/build.gradle.kts**: plugins = `kotlin("jvm")`。dependencies = `implementation(project(":gateway"))` + `implementation(project(":proto"))`。jvmToolchain(21)。application 插件配置 mainClass = "com.nebula.server.NebulaServerKt"。

    所有模块的 kotlin { jvmToolchain(21) } 和 java { sourceCompatibility = JavaVersion.VERSION_21; targetCompatibility = JavaVersion.VERSION_21 } 一致配置。

    同时创建模块的源目录结构：
    ```bash
    mkdir -p proto/src/main/proto
    mkdir -p common/src/main/kotlin
    mkdir -p repository/src/main/kotlin/com/nebula/repository
    mkdir -p service/src/main/kotlin/com/nebula/service
    mkdir -p gateway/src/main/kotlin/com/nebula/gateway
    mkdir -p server/src/main/kotlin/com/nebula/server
    ```
  </action>
  <verify>
    <automated>grep -c "implementation(project" */build.gradle.kts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 6 个 build.gradle.kts 文件全部存在在对应子目录
    - common/build.gradle.kts 包含 `implementation(project(":proto"))`
    - repository/build.gradle.kts 包含 `implementation(project(":common"))`
    - service/build.gradle.kts 包含 `implementation(project(":repository"))`
    - gateway/build.gradle.kts 包含 `implementation(project(":service"))` 和 `implementation(project(":proto"))`
    - server/build.gradle.kts 包含 `implementation(project(":gateway"))` 和 `implementation(project(":proto"))`，且配置了 application 插件的 mainClass
    - 6 个模块的源目录全部存在
    - proto/build.gradle.kts 末尾有 `// PROTO-PLUGIN-CONFIG-HERE` 标记
    - kotlin jvmToolchain(21) 在每个模块中配置
  </acceptance_criteria>
  <done>6 个 build.gradle.kts 创建完毕，目录结构就绪，依赖方向正确（检查无反向引用）</done>
</task>

<task type="auto">
  <name>Task 3: Create project scaffolding files — .gitignore、.editorconfig、.gitattributes、README.md、LICENSE、.idea/ config</name>
  <files>
    .gitignore
    .editorconfig
    .gitattributes
    README.md
    LICENSE
    .idea/codeStyles/Project.xml
    .idea/codeStyles/codeStyleConfig.xml
    .idea/vcs.xml
    .idea/misc.xml
    .idea/compiler.xml
    .idea/.gitignore
  </files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md — 推荐目录结构
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/13-编码与规范.md
  </read_first>
  <action>
    D-11 要求标准的工程初始化文件。
    D-12 要求 MIT 许可证。
    D-15 要求 .idea/ 选择性提交：codeStyles/、runConfigurations/、vcs.xml、misc.xml、compiler.xml 提交；workspace.xml、tasks.xml 忽略。

    1. **.gitignore**: 必须包含 .gradle/、build/、.idea/workspace.xml、.idea/tasks.xml、*.iml、.DS_Store、local.properties、*.log、.env、out/、*.class。
    
    2. **.editorconfig**: root = true，[*] indent_style = space，indent_size = 4，end_of_line = lf，charset = utf-8，trim_trailing_whitespace = true，insert_final_newline = true。[*.{kt,kts}] indent_size = 4。 [{*.yml,*.yaml}] indent_size = 2。
    
    3. **.gitattributes**: text=auto 设置。*.bat text eol=crlf。*.sh text eol=lf。*.proto linguist-language=Protobuf。
    
    4. **README.md**: 项目名 "Nebula Chat Server"，简短描述 "A real-time gRPC bidirectional streaming chat server built with Kotlin"。模块依赖图 ASCII 示意。构建命令 `./gradlew build`。许可证 MIT。
    
    5. **LICENSE**: 完整的 MIT 许可证文本。copyright 为 "Copyright (c) 2026 Nebula Chat Server"。
    
    6. **.idea/ 配置**: 按照 D-15 规则创建受版本控制的 .idea 配置文件：
       - .idea/vcs.xml: 指向项目根目录
       - .idea/misc.xml: 配置 JDK 21
       - .idea/compiler.xml: Kotlin 编译器配置
       - .idea/codeStyles/Project.xml: Kotlin 风格（PascalCase 类名，camelCase 函数/变量，4 空格缩进）
       - .idea/codeStyles/codeStyleConfig.xml: 引用 Project.xml
       - .idea/.gitignore: 内容为 `workspace.xml` 和 `tasks.xml`（D-15 忽略规则）
       - 不创建 runtime 生成的 .idea 文件（modules.xml、name 文件由 IDE 自动生成）
  </action>
  <verify>
    <automated>ls -la .gitignore .editorconfig .gitattributes README.md LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` 包含 .gradle/、build/、.idea/workspace.xml、.idea/tasks.xml
    - `.editorconfig` 有 root = true 和 [*]、[*.{kt,kts}] 两个 section
    - `.gitattributes` 有 text=auto，.bat 和 .sh 的 eol 规则，.proto 的 linguist-language
    - `README.md` 包含项目名 "Nebula Chat Server"、模块 ASCII 依赖图、构建命令、MIT 声明
    - `LICENSE` 是完整的 MIT 许可证，包含 "Copyright (c) 2026 Nebula Chat Server"
    - `.idea/.gitignore` 包含 workspace.xml 和 tasks.xml
    - `.idea/vcs.xml`、`.idea/misc.xml`、`.idea/compiler.xml` 存在
    - `.idea/codeStyles/Project.xml` 和 `.idea/codeStyles/codeStyleConfig.xml` 存在
  </acceptance_criteria>
  <done>所有工程脚手架文件创建完毕，.idea/ 选择性提交配置按 D-15 规则就绪</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地文件系统 → Git 仓库 | .proto 源文件、Gradle 配置文件通过 Git 管理，提交前可能被本地篡改 |
| Gradle Plugin Portal / Maven Central | 构建时从远程仓库下载依赖，可能引入恶意包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .proto 源文件 | mitigate | Git 版本控制 + code review 流程。所有 .proto 变更通过 PR 审查 |
| T-01-02 | Spoofing | Git submodule URL | mitigate | 在 .gitmodules 中指定可信 HTTPS/SSH URL，禁止 file:// 协议（Plan D 实施） |
| T-01-03 | Tampering | Maven Central 依赖 | accept | 本阶段使用的 protobuf-java、kotlin-logging 等均为长期稳定、高下载量的 Maven 中央仓库包，供应链风险极低。Gradle 依赖锁定通过版本目录管理 |
</threat_model>

<verification>
- [ ] Task 1: `./gradlew --version` 显示 Gradle 8.10
- [ ] Task 2: 6 个 build.gradle.kts 存在，依赖方向正确（grep 确认无反向引用）
- [ ] Task 3: .gitignore、.editorconfig、.gitattributes、README.md、LICENSE 存在
- [ ] 各模块源目录存在（proto/src/main/proto/ 等）
</verification>

<success_criteria>
1. `ls settings.gradle.kts build.gradle.kts gradle/libs.versions.toml gradlew` 全部存在
2. `ls proto/build.gradle.kts common/build.gradle.kts repository/build.gradle.kts service/build.gradle.kts gateway/build.gradle.kts server/build.gradle.kts` 全部存在
3. `ls .gitignore .editorconfig .gitattributes README.md LICENSE` 全部存在
4. `ls .idea/vcs.xml .idea/misc.xml .idea/compiler.xml .idea/codeStyles/Project.xml` 存在
5. `ls proto/src/main/proto/ common/src/main/kotlin/ repository/src/main/kotlin/ service/src/main/kotlin/ gateway/src/main/kotlin/ server/src/main/kotlin/` 目录就绪
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffolding-proto-definitions/01-01-SUMMARY.md` when done
</output>

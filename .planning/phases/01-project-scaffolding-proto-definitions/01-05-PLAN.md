---
phase: 01-project-scaffolding-proto-definitions
plan: 05
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - common/build.gradle.kts
  - common/src/main/kotlin/com/nebula/common/BizCode.kt
autonomous: true
requirements:
  - INFRA-01
user_setup: []
must_haves:
  truths:
    - "common/build.gradle.kts 包含 kotlin-logging + SLF4J + Logback 依赖"
    - "BizCode.kt 定义了 Kotlin 枚举类 BizCode，包含 30 个错误码常量（code: Int + msg: String）"
    - "所有错误码与设计文档 6.2 完全一致"
    - "BizCode 枚举的 code 值覆盖 200（成功）和 6 个错误码区间（1000~1004, 1100~1103, 1200~1201, 1300~1305, 1400~1405, 1500~1503, 9000~9002）"
  artifacts:
    - path: "common/build.gradle.kts"
      provides: "common 模块完整依赖配置"
      contains: "libs.kotlin.logging"
    - path: "common/src/main/kotlin/com/nebula/common/BizCode.kt"
      provides: "30 个业务错误码枚举"
      contains: "enum class BizCode"
      min_lines: 50
  key_links:
    - from: "BizCode.kt"
      to: "Response.code（envelope.proto）"
      via: "BizCode.code → Response.code 字段"
      pattern: "BizCode\\.\\w+\\.code"
---

<objective>
在 `:common` 模块中创建 Kotlin BizCode 错误码枚举（30 个错误码），配置 kotlin-logging + SLF4J 日志框架。

Purpose: D-10 要求 ErrorCode 不在 Proto 中定义，而是使用 Kotlin BizCode 枚举，便于在 Kotlin when 表达式中 exhaustive 匹配。D-14 要求使用 kotlin-logging + SLF4J。本计划在 common 模块完成这两项基础设施。
Output: BizCode.kt 枚举文件、common/build.gradle.kts 日志依赖配置
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/06-接口方法列表/6.2-错误码.md
@/Users/linyu/project/personal/Nebula/nebula_server/.planning/phases/01-project-scaffolding-proto-definitions/01-RESEARCH.md
@/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/13-编码与规范.md

<interfaces>
From 6.2 错误码.md — 完整 BizCode 枚举定义：

```kotlin
enum class BizCode(val code: Int, val msg: String) {
    OK(200, "ok"),
    INVALID_PARAM(1000, "invalid param"),
    UNAUTHORIZED(1001, "unauthorized"),
    FORBIDDEN(1002, "forbidden"),
    NOT_FOUND(1003, "not found"),
    RATE_LIMITED(1004, "rate limited"),
    TOKEN_EXPIRED(1100, "token expired"),
    TOKEN_INVALID(1101, "token invalid"),
    AUTH_FAILED(1102, "auth failed"),
    DEVICE_UNSUPPORTED(1103, "device unsupported"),
    USER_NOT_FOUND(1200, "user not found"),
    USERNAME_EXISTS(1201, "username exists"),
    REQUEST_NOT_FOUND(1300, "friend request not found"),
    REQUEST_HANDLED(1301, "friend request already handled"),
    SELF_FRIEND(1302, "cannot add self as friend"),
    ALREADY_FRIEND(1303, "already friends"),
    NOT_FRIEND(1304, "not friend"),
    FRIEND_NOT_FOUND(1305, "friend not found"),
    CONV_NOT_FOUND(1400, "conversation not found"),
    GROUP_FULL(1401, "group is full"),
    GROUP_DISSOLVED(1402, "group dissolved"),
    NOT_MEMBER(1403, "not a group member"),
    GROUP_PERM_DENIED(1404, "group permission denied"),
    ALREADY_IN_GROUP(1405, "user already in group"),
    MSG_NOT_FOUND(1500, "message not found"),
    SEND_FAILED(1501, "message send failed"),
    UNSUPPORTED_MSG_TYPE(1502, "unsupported message type"),
    CONTENT_VIOLATION(1503, "content violation"),
    INTERNAL_ERROR(9000, "internal error"),
    DB_ERROR(9001, "database error"),
    CACHE_ERROR(9002, "cache error"),
}
```

D-10: ErrorCode 不在 Proto 中定义，用 Kotlin BizCode 枚举放在 :common 模块

D-14: kotlin-logging + SLF4J

Research 错误码区间：
- 200: 成功
- 1000-1099: 通用错误（5 个）
- 1100-1199: 认证错误（4 个）
- 1200-1299: 用户错误（2 个）
- 1300-1399: 好友错误（6 个）
- 1400-1499: 会话/群聊错误（6 个）
- 1500-1599: 消息错误（4 个）
- 9000-9099: 系统错误（3 个）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update common/build.gradle.kts — add kotlin-logging + SLF4J + Logback dependencies</name>
  <files>common/build.gradle.kts</files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/nebula_server/common/build.gradle.kts（Plan A 创建的基础骨架）
    @/Users/linyu/project/personal/Nebula/nebula_server/gradle/libs.versions.toml（确认 kotlin-logging 版本声明）
  </read_first>
  <action>
    Plan A Task 2 创建了 common/build.gradle.kts 基础骨架，已包含：
    - plugins { kotlin("jvm") }
    - dependencies { implementation(project(":proto")) }
    - jvmToolchain(21)

    D-14 锁定 kotlin-logging + SLF4J。确保 common/build.gradle.kts 的 dependencies 块包含以下全部依赖（如果 Plan A 创建的骨架已包含则验证，缺少则补全）：

    ```kotlin
    dependencies {
        implementation(project(":proto"))
        implementation(libs.kotlin.logging)
        implementation(libs.slf4j.api)
        implementation(libs.logback.classic)
    }
    ```

    注意 libs.versions.toml 中对应的 TOML key：
    - libs.kotlin.logging → [libraries] kotlin-logging = { module = "io.github.oshai:kotlin-logging-jvm" }
    - libs.slf4j.api → [libraries] slf4j-api = { module = "org.slf4j:slf4j-api" }
    - libs.logback.classic → [libraries] logback-classic = { module = "ch.qos.logback:logback-classic" }

    这些已在 Plan A Task 1 的 libs.versions.toml 中声明，此处只需在 common/build.gradle.kts 的 dependencies 中引用。

    确保不将 proto 相关依赖（protobuf-java、protobuf-kotlin）放在 common/build.gradle.kts 中——它们通过 `implementation(project(":proto"))` 传递依赖。
  </action>
  <verify>
    <automated>grep -c "libs\\.kotlin\\.logging" common/build.gradle.kts && grep -c "libs\\.slf4j\\.api" common/build.gradle.kts && grep -c "libs\\.logback\\.classic" common/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - common/build.gradle.kts 的 dependencies 包含 libs.kotlin.logging、libs.slf4j.api、libs.logback.classic
    - 不包含 protobuf-java 或 protobuf-kotlin 直接依赖（通过 :proto 传递）
  </acceptance_criteria>
  <done>common 模块日志依赖配置就绪</done>
</task>

<task type="auto">
  <name>Task 2: Create BizCode.kt — 30 个 Kotlin 错误码枚举</name>
  <files>common/src/main/kotlin/com/nebula/common/BizCode.kt</files>
  <read_first>
    @/Users/linyu/project/personal/Nebula/设计文档/后端架构设计v1.2/06-接口方法列表/6.2-错误码.md — 完整 30 个错误码定义 + Kotlin 枚举代码
  </read_first>
  <action>
    D-10: ErrorCode 不在 Proto 中定义，使用 Kotlin BizCode 枚举。
    严格按 6.2 架构文档的 Kotlin 枚举定义创建。

    文件路径：common/src/main/kotlin/com/nebula/common/BizCode.kt
    package: com.nebula.common

    ```kotlin
    package com.nebula.common

    enum class BizCode(val code: Int, val msg: String) {
        OK(200, "ok"),
        INVALID_PARAM(1000, "invalid param"),
        // ... 全部 30 个枚举值
    }
    ```

    必须包含全部 30 个枚举常量，准确对应用户设计文档 6.2 的定义：

    **通用 (200, 1000-1004):** OK, INVALID_PARAM, UNAUTHORIZED, FORBIDDEN, NOT_FOUND, RATE_LIMITED

    **认证 (1100-1103):** TOKEN_EXPIRED, TOKEN_INVALID, AUTH_FAILED, DEVICE_UNSUPPORTED

    **用户 (1200-1201):** USER_NOT_FOUND, USERNAME_EXISTS

    **好友 (1300-1305):** REQUEST_NOT_FOUND, REQUEST_HANDLED, SELF_FRIEND, ALREADY_FRIEND, NOT_FRIEND, FRIEND_NOT_FOUND

    **会话/群聊 (1400-1405):** CONV_NOT_FOUND, GROUP_FULL, GROUP_DISSOLVED, NOT_MEMBER, GROUP_PERM_DENIED, ALREADY_IN_GROUP

    **消息 (1500-1503):** MSG_NOT_FOUND, SEND_FAILED, UNSUPPORTED_MSG_TYPE, CONTENT_VIOLATION

    **系统 (9000-9002):** INTERNAL_ERROR, DB_ERROR, CACHE_ERROR

    msg 字段值严格匹配 6.2 设计文档的"说明"列——使用设计文档中的英文描述，非中文。
    
    可选扩展：添加 companion object 提供根据 code 值查找 BizCode 的工厂方法（非必须，Phase 4 Handler 层使用）。但如果添加，保证不破坏枚举结构：
    ```kotlin
    companion object {
        fun fromCode(code: Int): BizCode? = entries.find { it.code == code }
    }
    ```

    文件必须通过 `./gradlew :common:compileKotlin` 编译。
  </action>
  <verify>
    <automated>grep -c "enum class BizCode" common/src/main/kotlin/com/nebula/common/BizCode.kt && grep -c "CACHE_ERROR" common/src/main/kotlin/com/nebula/common/BizCode.kt && ./gradlew :common:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BizCode.kt 包含 30 个枚举常量
    - OK(200, "ok") 存在
    - CACHE_ERROR(9002, "cache error") 存在
    - `./gradlew :common:compileKotlin` 编译通过
  </acceptance_criteria>
  <done>BizCode.kt 创建完毕，30 个错误码全部定义且编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 错误码枚举为纯 Kotlin 逻辑，无安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | 本计划无运行时安全边界。错误码为纯枚举定义，不涉及用户输入或外部调用 |
</threat_model>

<verification>
- [ ] Task 1: common/build.gradle.kts 包含 kotlin-logging + SLF4J + Logback 依赖
- [ ] Task 2: BizCode.kt 30 个错误码枚举 + `./gradlew :common:compileKotlin` 编译通过
</verification>

<success_criteria>
1. common/build.gradle.kts 包含日志依赖
2. BizCode.kt 30 个错误码枚举编译通过
3. `./gradlew build` 在 common 模块级别成功
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffolding-proto-definitions/01-05-SUMMARY.md` when done
</output>

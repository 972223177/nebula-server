---
phase: 06-chat-message
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageStep.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendContext.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/ValidateStep.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/DedupStep.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/WriteStep.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/service/ChatService.kt"
autonomous: true
requirements:
  - BIZ-CHAT-01
  - BIZ-CHAT-02

must_haves:
  truths:
    - "chat/send 请求经由 Validate → Dedup → Write 三个 Step 顺序同步执行"
    - "ValidateStep 校验消息内容非空、client_message_id 非空、发送者是会话成员"
    - "DedupStep 通过 Redis SETNX 检测重复消息，重复时返回 SEND_FAILED"
    - "WriteStep 用 Snowflake 生成 msg_id，写入 Redis Stream，更新会话元信息"
    - "推送和未读计数异步执行——WriteStep 完成后立即返回 SendMessageResp（D-04 per REVIEW）"
    - "未读计数递增不在 WriteStep 中，移至异步推送阶段处理（REVIEW-MEDIUM-5）"
    - "去重键值在 WriteStep 中更新为实际 msg_id（REVIEW-MEDIUM-6）"
    - "SendMessageHandler 使用 try-catch 包裹 Step 链执行，非预期异常转化为 SendMessageException（REVIEW-HIGH-2）"
    - "ChatService 在 handleLoginSuccess 中注册 StreamObserver 到 UserStreamRegistry"
    - "ChatService 在 cleanupConnection 中解除 UserStreamRegistry 注册"
    - "ChatStreamObserver.userId 通过显式类型检查设置，而非 `as?` 静默转换（REVIEW-MEDIUM-7）"
  artifacts:
    - path: "gateway/.../handler/chat/send/SendMessageStep.kt"
      provides: "Step 链接口"
      contains: "interface SendMessageStep"
    - path: "gateway/.../handler/chat/send/SendContext.kt"
      provides: "Step 链共享上下文"
      contains: "data class SendContext"
    - path: "gateway/.../handler/chat/send/ValidateStep.kt"
      provides: "成员验证 + 参数校验"
      contains: "class ValidateStep : SendMessageStep"
    - path: "gateway/.../handler/chat/send/DedupStep.kt"
      provides: "Redis SETNX 消息去重"
      contains: "class DedupStep : SendMessageStep"
    - path: "gateway/.../handler/chat/send/WriteStep.kt"
      provides: "Snowflake ID → Redis Stream → 会话元更新"
      contains: "class WriteStep : SendMessageStep"
    - path: "gateway/.../handler/chat/send/SendMessageHandler.kt"
      provides: "Step 链编排 + 异步推送 + chat/send 入口"
      contains: "class SendMessageHandler : Handler<SendMessageReq, SendMessageResp>"
  key_links:
    - from: "SendMessageHandler.handle"
      to: "SendMessageStep.execute (x3)"
      via: "for (step in steps) { if (!step.execute(context)) break }"
    - from: "WriteStep"
      to: "SnowflakeIdGenerator.nextId()"
      via: "msg_id 生成"
    - from: "WriteStep"
      to: "MessageQueueRepository.enqueue()"
      via: "Redis Stream 写入"
    - from: "SendMessageHandler.handle (async)"
      to: "PushService.pushMessage()"
      via: "fire-and-forget 异步扇出推送"
    - from: "SendMessageHandler.handle (async)"
      to: "ConversationMemberRepository.findByConversationId"
      via: "异步未读计数递增"
    - from: "ChatService.handleLoginSuccess"
      to: "UserStreamRegistry.register()"
      via: "登录后注册 StreamObserver"
---

<objective>
实现 chat/send 的 Step 链编排（D-13）— 创建 SendMessageStep 接口、SendContext 共享上下文、3 个同步 Step（Validate → Dedup → Write）、SendMessageHandler 编排器（含 fire-and-forget 异步推送 + 未读计数），以及 ChatService 集成 UserStreamRegistry 的连接生命周期管理。

**关键设计变更（源自 REVIEW 反馈）：**
1. **D-04 严格遵从** — Push 不再作为同步 Step；WriteStep 完成后立即返回 SendMessageResp，推送和未读计数在独立协程中 fire-and-forget 异步执行
2. **未读计数从 WriteStep 剥离**（REVIEW-MEDIUM-5）— WriteStep 不再处理未读计数 INCR，移至异步后处理阶段
3. **去重键值更新**（REVIEW-MEDIUM-6）— DedupStep 写入 "pending"，WriteStep 完成 msg_id 生成后更新为实际 msg_id
4. **catch-all 异常处理**（REVIEW-HIGH-2）— SendMessageHandler.handle() 包裹 Step 链 try-catch，非预期异常转化为 SendMessageException
5. **ChatStreamObserver.userId 显式类型检查**（REVIEW-MEDIUM-7）— 使用 require(responseObserver is ChatStreamObserver) 替代 `as?` 静默转换

Purpose: chat/send 是 Phase 6 核心入口，Step 链模式将验证→去重→持久化分离为独立可测试单元，异步推送确保 ACK 及时返回。
Output: 6 个新文件 + 1 个修改文件 + 单元测试。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-message/06-CONTEXT.md
@.planning/phases/06-chat-message/06-RESEARCH.md
@.planning/phases/06-chat-message/06-PATTERNS.md
@.planning/phases/06-chat-message/06-01-PLAN.md
@.planning/phases/06-chat-message/06-REVIEWS.md

<interfaces>
<!-- 执行本计划需要使用的已有接口 -->

**SendMessageReq** (proto: chat.proto):
- `fun getConversationId(): String` — 会话 ID
- `fun getMessageType(): ChatContentType` — 消息类型
- `fun getContent(): String` — 消息文本
- `fun getPayload(): ByteString` — 附加数据
- `fun getClientTs(): Long` — 客户端时间戳
- `fun getClientMessageId(): String` — 客户端幂等标识

**SendMessageResp** (proto: chat.proto):
- `newBuilder().setMsgId(long).setServerTs(long).build()`

**Handler<Req, Resp>** (gateway/.../handler/Handler.kt):
- `val method: String` — 路由字符串
- `suspend fun handle(req: Req): Resp`
- Session 从 `coroutineContext.requireSession()` 获取

**SnowflakeIdGenerator** (common/.../idgen/):
- `fun nextId(): Long` — 生成唯一 ID

**ConversationMemberRepository** (repository/.../repository/):
- `fun findByConversationId(conversationId: String): List<ConversationMemberEntity>`
- `fun findByConversationIdAndUserId(conversationId: String, userId: Long): ConversationMemberEntity?`

**MessageQueueRepository** (repository/.../redis/):
- `suspend fun enqueue(message: Map<String, String>): String?`

**UserStreamRegistry** (gateway/.../session/ — 06-01):
- `fun register(userId: Long, observer: StreamObserver<Envelope>)`
- `fun removeStream(userId: Long, observer: StreamObserver<Envelope>)`
- `fun removeUser(userId: Long)`
- `fun getStreams(userId: Long): List<StreamObserver<Envelope>>`

**PushService** (gateway/.../push/ — 06-01):
- `suspend fun pushMessage(convId: String, chatMessage: ChatMessage, excludeUid: Long)`

**SessionRegistry** (gateway/.../session/):
- `fun onEviction(callback: (token: String) -> Unit)` — 注册驱逐回调

**existing methods on ChatService:**
- `private val tokenToObserver` — token→observer 映射
- `private suspend fun handleLoginSuccess(response: Response, responseObserver: StreamObserver<Envelope>)`
- `private inner class ChatStreamObserver(responseObserver)` — cleanupConnection

**Redis SETNX pattern** (per OnlineStatusRepository):
- `redis.setnx(key, value)` — true=键不存在(set成功), false=键已存在
- `redis.expire(key, ttl)` — 设置 TTL

**BizCode 相关常量**:
- `BizCode.OK` (200)
- `BizCode.INVALID_PARAM` (1000)
- `BizCode.NOT_MEMBER` (1403)
- `BizCode.SEND_FAILED` (1501)
- `BizCode.CACHE_ERROR` (9002)
- `BizCode.INTERNAL_ERROR` (9000)

**Lettuce Redis 操作模式** (per OnlineStatusRepository.kt):
- `@OptIn(ExperimentalLettuceCoroutinesApi::class)`
- `private val redis = RedisCoroutinesCommandsImpl(connection.reactive())`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Step 链接口 + SendContext + 3 个同步 Step（Validate / Dedup / Write）</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageStep.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendContext.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/ValidateStep.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/DedupStep.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/WriteStep.kt
  </files>
  <read_first>
    gateway/.../handler/Handler.kt（interface 定义模式）
    gateway/.../interceptor/Interceptor.kt（链式接口类比）
    proto/.../chat/chat.proto（SendMessageReq 定义）
    proto/.../message/message.proto（ChatMessage 定义）
    repository/.../entity/MessageEntity.kt（消息实体字段）
    repository/.../redis/MessageQueueRepository.kt（enqueue 方法）
    repository/.../repository/ConversationMemberRepository.kt（findByConversationIdAndUserId）
    gateway/.../session/Session.kt（data class 模式）
  </read_first>
  <action>
    **SendMessageStep.kt**（Step 链接口，D-13）：
    package: `com.nebula.gateway.handler.chat.send`
    ```kotlin
    interface SendMessageStep {
        /** 执行 Step，返回是否继续下一步。false 终止链（如去重命中）。 */
        suspend fun execute(context: SendContext): Boolean
    }
    ```
    KDoc 中文注释说明职责：Step 链提供单一职责的可测试单元，通过 SendContext 传递共享状态。

    **SendContext.kt**（Step 链共享上下文，D-13）：
    package: `com.nebula.gateway.handler.chat.send`
    ```kotlin
    data class SendContext(
        val req: SendMessageReq,                           // 原始请求（只读）
        val senderUid: Long,                               // 发送者用户 ID（来自 Session）
        var conversationId: String = req.conversationId,   // 会话 ID
        var msgId: Long? = null,                           // Snowflake msg_id（WriteStep 设置）
        var chatMessage: ChatMessage? = null,               // 构建的 ChatMessage proto（WriteStep 设置）
        var bizCode: BizCode = BizCode.OK                   // 业务处理结果码
    )
    ```
    KDoc 中文注释说明各字段用途，注明 msgId 和 chatMessage 在 WriteStep 后才非空。

    **ValidateStep.kt**（D-08, D-13, D-14）：
    package: `com.nebula.gateway.handler.chat.send`
    构造注入: `ConversationMemberRepository`
    execute():
    1. req.content.isBlank() → throw SendMessageException(BizCode.INVALID_PARAM, "消息内容不能为空")（D-14）
    2. req.clientMessageId.isBlank() → throw SendMessageException(BizCode.INVALID_PARAM, "client_message_id 不能为空")（D-14）
    3. conversationMemberRepository.findByConversationIdAndUserId(req.conversationId, senderUid) == null → throw SendMessageException(BizCode.NOT_MEMBER)（D-08）
    4. return true
    KDoc 引用 D-08, D-13, D-14。

    **DedupStep.kt**（D-07, D-13）：
    package: `com.nebula.gateway.handler.chat.send`
    构造注入: `StatefulRedisConnection<String, String>`
    使用 `@OptIn(ExperimentalLettuceCoroutinesApi::class)` 和 `RedisCoroutinesCommandsImpl(connection.reactive())`
    execute():
    1. `val dedupKey = "chat:dedup:${context.req.clientMessageId}"`
    2. `val isNew = redis.setnx(dedupKey, "pending")` — Redis SETNX 语义：true=设置成功（新消息），false=键已存在（重复消息）
    3. 如果 !isNew（重复消息）→ `throw SendMessageException(BizCode.SEND_FAILED, "重复消息")`（D-07 忽略重复）
    4. `redis.expire(dedupKey, 7 * 24 * 3600L)` — 设置 7 天 TTL
    5. return true
    **去重键值说明**：初始值写为 "pending"（非最终 msg_id，因为 msg_id 尚未生成）。
    WriteStep 在生成 msg_id 后会将去重键值更新为实际 msg_id（REVIEW-MEDIUM-6）。
    KDoc 引用 D-07, D-13。

    **WriteStep.kt**（D-04, D-10, D-13）：
    package: `com.nebula.gateway.handler.chat.send`
    构造注入: `SnowflakeIdGenerator`, `MessageQueueRepository`, `StatefulRedisConnection<String, String>`
    使用 `@OptIn(ExperimentalLettuceCoroutinesApi::class)` 和 `RedisCoroutinesCommandsImpl(connection.reactive())`
    execute():
    1. `val msgId = idGenerator.nextId()` — Snowflake 生成 msg_id
    2. `context.msgId = msgId`
    3. 构建 ChatMessage proto:
       - msgId, conversationId, senderUid
       - messageType 从 req.messageType
       - content 从 req.content
       - payload 从 req.payload
       - clientTs 从 req.clientTs
       - serverTs = `System.currentTimeMillis()`
    4. `context.chatMessage = chatMessage`
    5. `messageQueueRepository.enqueue(mapOf("msg_id" to msgId.toString(), "conversation_id" to context.req.conversationId))` — Redis Stream 写入（D-04 ACK 时机）
    6. 更新会话元信息（D-10）：通过 Redis 更新会话元信息
       - `redis.set("conversation:${convId}:last_message_id", msgId.toString())`
       - `redis.set("conversation:${convId}:last_message_preview", context.req.content.take(50))`
       - `redis.set("conversation:${convId}:last_updated_at", System.currentTimeMillis().toString())`
    7. **更新去重键值为实际 msg_id**（REVIEW-MEDIUM-6）：
       - `val dedupKey = "chat:dedup:${context.req.clientMessageId}"`
       - `redis.setex(dedupKey, 7 * 24 * 3600L, msgId.toString())` — 覆盖 "pending" 为实际 msg_id
    8. return true
    **设计说明（REVIEW-MEDIUM-5 未读计数剥离）**：未读计数 INCR 不再在 WriteStep 中处理，已移至 SendMessageHandler 的异步后处理阶段。WriteStep 仅负责消息持久化和会话元更新。
    KDoc 引用 D-04, D-10, D-13。

    所有文件添加 `companion object { private val logger = KotlinLogging.logger {} }`。
    所有文件添加完整 KDoc 中文注释，引用对应 D-编号。
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - SendMessageStep 接口含 `suspend fun execute(context: SendContext): Boolean`
    - SendContext 含 req/senderUid/conversationId/msgId/chatMessage/bizCode 字段
    - ValidateStep 检查 content 非空、clientMessageId 非空、成员身份
    - DedupStep 使用 setnx + expire 组合执行去重
    - WriteStep 调用 SnowflakeIdGenerator.nextId() + MessageQueueRepository.enqueue() + 会话元更新
    - WriteStep 不处理未读计数（REVIEW-MEDIUM-5）
    - WriteStep 更新去重键值为实际 msg_id（REVIEW-MEDIUM-6）
  </acceptance_criteria>
  <done>Step 链全部 5 个文件编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 SendMessageHandler（Step 链编排 + 异步推送/未读计数）</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandler.kt
  </files>
  <read_first>
    gateway/.../handler/Handler.kt（Handler 接口）
    gateway/.../handler/chat/send/SendMessageStep.kt（本计划 Task 1 创建 — Step 接口）
    gateway/.../handler/chat/send/SendContext.kt（本计划 Task 1 创建 — 上下文）
    gateway/.../push/PushService.kt（06-01 创建）
    repository/.../repository/ConversationMemberRepository.kt（findByConversationId）
  </read_first>
  <action>
    创建 SendMessageHandler（D-04, D-05, D-06, D-09, D-11, D-13）：
    package: `com.nebula.gateway.handler.chat.send`
    构造注入: 
    - `List<SendMessageStep>` — Step 链（Validate → Dedup → Write）
    - `PushService` — 用于 fire-and-forget 异步推送
    - `ConversationMemberRepository` — 用于异步未读计数
    - `StatefulRedisConnection<String, String>` — 用于异步未读计数 INCR
    - `CoroutineScope` — 用于 fire-and-forget 协程启动（Dispatchers.IO + SupervisorJob）
    
    使用 `@OptIn(ExperimentalLettuceCoroutinesApi::class)` 和 `RedisCoroutinesCommandsImpl(connection.reactive())`
    实现: `Handler<SendMessageReq, SendMessageResp>`
    - `override val method: String = "chat/send"`

    handle() 实现:
    ```kotlin
    override suspend fun handle(req: SendMessageReq): SendMessageResp {
        val session = coroutineContext.requireSession()
        val context = SendContext(req = req, senderUid = session.userId, conversationId = req.conversationId)

        // REVIEW-HIGH-2: Step 链包裹 try-catch，非预期异常转化为 SendMessageException
        try {
            for (step in steps) {
                if (!step.execute(context)) break
            }
        } catch (e: SendMessageException) {
            throw e  // 预期异常直接传播
        } catch (e: Exception) {
            // 非预期异常（如 Redis 连接超时）转化为带上下文的 SendMessageException
            throw SendMessageException(
                BizCode.INTERNAL_ERROR,
                "SendMessage Step chain failed at step ${e.stackTrace.firstOrNull()?.methodName ?: "unknown"}: ${e.message}",
                e
            )
        }

        requireNotNull(context.msgId) { "WriteStep must execute before building response" }
        val response = SendMessageResp.newBuilder()
            .setMsgId(context.msgId)
            .setServerTs(System.currentTimeMillis())
            .build()

        // D-04 per REVIEW: 写入后立即返回响应，推送和未读计数异步执行
        scope.launch {
            asyncUnreadAndPush(context)
        }

        return response
    }

    private suspend fun asyncUnreadAndPush(context: SendContext) {
        // D-06, D-16: 未读计数递增（REVIEW-MEDIUM-5 — 从 WriteStep 移出）
        try {
            val members = conversationMemberRepository
                .findByConversationId(context.conversationId)
                .filter { it.userId != context.senderUid }
            for (member in members) {
                try {
                    redis.incr("conversation:${context.conversationId}:unread:${member.userId}")
                } catch (e: Exception) {
                    logger.error(e) { "INCR unread failed for userId=${member.userId}" }
                }
            }
        } catch (e: Exception) {
            logger.error(e) { "Unread count batch increment failed for conv=${context.conversationId}" }
        }

        // D-04 async, D-05 best-effort: 推送
        val msg = context.chatMessage ?: return  // requireNotNull 已在上层检查
        try {
            pushService.pushMessage(context.conversationId, msg, context.senderUid)
        } catch (e: Exception) {
            logger.error(e) { "Async push failed for conv=${context.conversationId}" }
        }
    }
    ```

    KDoc 注释说明：
    - D-04: WriteStep 完成后立即返回响应，推送异步执行
    - REVIEW-HIGH-2: catch-all 异常处理机制
    - REVIEW-MEDIUM-5: 未读计数在异步后处理阶段处理
    - REVIEW-MEDIUM-6: 去重键值在 WriteStep 中更新为 msg_id

    添加 `companion object { private val logger = KotlinLogging.logger {} }`
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - SendMessageHandler.method == "chat/send"
    - 构造函数接收 5 个参数：List&lt;SendMessageStep&gt;, PushService, ConversationMemberRepository, StatefulRedisConnection, CoroutineScope
    - handle() 中 Step 链包裹 try-catch，非预期异常 → SendMessageException(BizCode.INTERNAL_ERROR, ...)
    - handle() 在 Step 链完成后立即返回 SendMessageResp，不等待推送
    - handle() 使用 scope.launch 启动异步后处理
    - 异步后处理同时处理未读计数 INCR 和推送
  </acceptance_criteria>
  <done>SendMessageHandler 编译通过，异步推送逻辑完整</done>
</task>

<task type="auto">
  <name>Task 3: ChatService 集成 UserStreamRegistry（注册 + 清理 + 类型安全 userId）</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/service/ChatService.kt
  </files>
  <read_first>
    gateway/.../service/ChatService.kt（当前版本 — 需修改集成点）
    gateway/.../session/UserStreamRegistry.kt（06-01 创建 — 需注入）
  </read_first>
  <action>
    修改 ChatService.kt 以集成 UserStreamRegistry（D-01, D-02）：

    1. **构造参数新增** `private val userStreamRegistry: UserStreamRegistry`
       （Koin 注入在 06-04 处理；ChatService 在 NebulaServer.kt 中实例化，需同步更新构造参数）

    2. **ChatStreamObserver 添加 userId 字段**（REVIEW-MEDIUM-7）：
       ```kotlin
       private inner class ChatStreamObserver(
           private val responseObserver: StreamObserver<Envelope>
       ) : StreamObserver<Envelope> {
           var userId: Long? = null  // REVIEW-MEDIUM-7: 显式声明可空字段，清理时检查非空
           
           // ... 现有方法 ...
           
           private fun cleanupConnection() {
               tokenToObserver.entries.removeIf { it.value == responseObserver }
               // D-01: 移除当前设备 StreamObserver（不调 removeUser 以免移除其他设备的流）
               userId?.let { uid ->
                   userStreamRegistry.removeStream(uid, responseObserver)
               }
           }
       }
       ```

    3. **handleLoginSuccess 中设置 ChatStreamObserver.userId**（REVIEW-MEDIUM-7 — 显式类型检查）：
       ```kotlin
       private suspend fun handleLoginSuccess(
           response: Response,
           responseObserver: StreamObserver<Envelope>
       ) {
           val loginResp = extractLoginResp(response)
           val session = buildSession(loginResp)
           val evictedToken = sessionRegistry.registerWithDeviceType(session)
           
           // D-01: 注册 StreamObserver（REVIEW-MEDIUM-7: 使用 require 替代 as? 静默转换）
           // responseObserver 实际上是 ChatStreamObserver，通过 require 显式断言
           require(responseObserver is ChatStreamObserver) {
               "responseObserver must be ChatStreamObserver"
           }
           responseObserver.userId = loginResp.userId
           userStreamRegistry.register(loginResp.userId, responseObserver)
           
           // ... 现有 tokenToObserver 逻辑 ...
       }
       ```

    4. **添加 import**：`import com.nebula.gateway.session.UserStreamRegistry`

    更新 ChatService 类 KDoc 注释添加 UserStreamRegistry 集成说明，引用 D-01。
    cleanupConnection KDoc 添加 UserStreamRegistry 清理说明。
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - ChatService 构造参数新增 `UserStreamRegistry`
    - ChatStreamObserver 含有 `var userId: Long? = null` 字段
    - cleanupConnection 中使用 `userId?.let { userStreamRegistry.removeStream(it, responseObserver) }`
    - handleLoginSuccess 中使用 `require(responseObserver is ChatStreamObserver)` 显式类型检查（非 `as?`）
    - handleLoginSuccess 中调用 `userStreamRegistry.register(loginResp.userId, responseObserver)`
  </acceptance_criteria>
  <done>ChatService 集成 UserStreamRegistry 编译通过，userId 类型安全</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 4: SendMessage Step 链 + Handler 单元测试</name>
  <files>
    gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/ValidateStepTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/DedupStepTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/WriteStepTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/chat/send/SendMessageHandlerTest.kt
  </files>
  <read_first>
    gateway/.../handler/chat/send/ValidateStep.kt（本计划 Task 1 创建）
    gateway/.../handler/chat/send/DedupStep.kt（本计划 Task 1 创建）
    gateway/.../handler/chat/send/WriteStep.kt（本计划 Task 1 创建）
    gateway/.../handler/chat/send/SendMessageHandler.kt（本计划 Task 2 创建）
    gateway/.../handler/chat/send/SendContext.kt（本计划 Task 1 创建）
  </read_first>
  <behavior>
    **ValidateStepTest:**
    - content 为空 → 抛出 SendMessageException(BizCode.INVALID_PARAM)
    - clientMessageId 为空 → 抛出 SendMessageException(BizCode.INVALID_PARAM)
    - 非会话成员 senderUid → 抛出 SendMessageException(BizCode.NOT_MEMBER)
    - 合法请求 → 返回 true

    **DedupStepTest:**
    - 首次 client_message_id → SETNX 成功 → 返回 true
    - 重复 client_message_id → SETNX 失败 → 抛出 SendMessageException(BizCode.SEND_FAILED)

    **WriteStepTest:**
    - 生成 msg_id → 设置到 context.msgId
    - 构建 ChatMessage → 设置到 context.chatMessage
    - 调用 MessageQueueRepository.enqueue
    - 更新会话元 Redis key
    - 更新去重键值为实际 msg_id

    **SendMessageHandlerTest:**
    - Step 链全部成功 → 返回 SendMessageResp（含 msgId + serverTs）
    - Step 链中 SendMessageException → 直接传播
    - 非预期异常（如模拟 RuntimeException）→ 包装为 SendMessageException(BizCode.INTERNAL_ERROR)
    - 验证响应在 Step 链完成后立即返回（不等待异步推送）
  </behavior>
  <action>
    ValidateStepTest:
    - 使用 MockK mock ConversationMemberRepository
    - 使用 runTest { } 测试 suspend execute()
    - 用 assertThrows 验证异常类型和 BizCode

    DedupStepTest:
    - Mock StatefulRedisConnection + coroutine commands
    - 模拟 setnx 返回 true/false 验证两种路径

    WriteStepTest:
    - Mock SnowflakeIdGenerator、MessageQueueRepository、Redis connection
    - 验证 context.msgId 被设置、context.chatMessage 被构建
    - 验证 enqueue 被调用、会话元 Redis 更新、去重键更新

    SendMessageHandlerTest:
    - Mock Step 链（模拟成功/失败路径）
    - Mock PushService、ConversationMemberRepository、Redis connection、CoroutineScope
    - 测试正常流程返回 SendMessageResp
    - 测试 Step 异常传播路径
    - 测试 catch-all 异常包装路径

    测试文件添加 `/** ... */` 中文注释说明测试目的。
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.handler.chat.send.*"</automated>
  </verify>
  <acceptance_criteria>
    - ValidateStepTest 全部通过（4 个测试用例）
    - DedupStepTest 全部通过（2 个测试用例）
    - WriteStepTest 全部通过（4 个测试用例）
    - SendMessageHandlerTest 全部通过（3 个测试用例）
    - 测试使用 runTest { } + MockK coEvery/coVerify
  </acceptance_criteria>
  <done>SendMessage Step 链和 Handler 单元测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ValidateStep | 未经验证的消息内容进入 Step 链 |
| DedupStep → Redis | 消息去重操作需原子性保证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | ValidateStep | mitigate | content 非空和 client_message_id 非空校验（D-14）；成员身份查询（D-08） |
| T-06-05 | Spoofing | DedupStep | mitigate | Redis SETNX 是原子操作（D-07），7 天 TTL 限制重放窗口；客户端必须有唯一 client_message_id |
| T-06-06 | Elevation of Privilege | WriteStep | mitigate | 仅写入已验证成员发起的消息（ValidateStep 已前置检查成员身份） |
| T-06-07 | Denial of Service | PushService (async) | mitigate | 异步推送包裹 try-catch（D-05）；单个推送异常不阻断主流程 |
| T-06-14 | Tampering | SendMessageHandler | mitigate | Step 链执行包裹 try-catch（REVIEW-HIGH-2），非预期异常转化为 SendMessageException(BizCode.INTERNAL_ERROR)，防止未处理异常暴露内部细节 |
</threat_model>

<verification>
1. `./gradlew :gateway:compileKotlin` 通过
2. Step 链接口和 3 个同步 Step 实现完整
3. SendMessageHandler 正确编排 Step 链 + 异步推送 + 未读计数
4. SendMessageHandler 包含 catch-all 异常处理（REVIEW-HIGH-2）
5. ChatService 正确集成 UserStreamRegistry（注册 + 清理 + 显式类型检查）
6. 单元测试覆盖 Step 链各组件和 Handler
</verification>

<success_criteria>
- 所有新建和修改文件编译通过
- SendMessageHandler 可接收 SendMessageReq → 同步执行 Validate → Dedup → Write → 立即返回 SendMessageResp → 异步执行未读计数递增 + 推送
- 非预期异常被 catch-all 包裹为 SendMessageException
- ChatService 在登录成功后注册 StreamObserver，连接关闭时解除注册
- ChatStreamObserver.userId 通过显式类型检查设置
</success_criteria>

<output>
创建 `.planning/phases/06-chat-message/06-02-SUMMARY.md` 完成时
</output>

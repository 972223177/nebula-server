---
phase: 06-chat-message
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - "gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt"
autonomous: false
requirements:
  - BIZ-CHAT-01
  - BIZ-CHAT-02
  - BIZ-MSG-01
  - BIZ-MSG-02

must_haves:
  truths:
    - "所有 Phase 6 组件在 Koin 容器中可解析（single { } 声明）"
    - "UserStreamRegistry 和 PushService 已注册为单例"
    - "SendMessageStep 列表可通过 Koin 的 get() 获取（4 个 Step 全部注册）"
    - "SendMessageHandler / PullMessagesHandler / ReadReportHandler 已注册到 HandlerRegistry"
    - "ChatService 构造参数包含 UserStreamRegistry（始于 06-02 修改）"
    - "NebulaServer 初始化时 registerHandlers() 可正确调用（参数列表已包含新 Handler）"
  artifacts:
    - path: "gateway/.../di/GatewayModule.kt"
      provides: "Koin DI + Handler 注册入口"
      contains: "handlerModule 包含 Phase 6 组件 / registerHandlers 包含 Phase 6 Handler"
  key_links:
    - from: "GatewayModule.handlerModule"
      to: "SendMessageHandler"
      via: "single { SendMessageHandler(get()) }"
    - from: "GatewayModule.registerHandlers"
      to: "HandlerRegistry.register (inline)"
      via: "registry.register(sendMessageHandler)"
    - from: "GatewayModule.registerHandlers 调用处（NebulaServer）"
      to: "ChatService"
      via: "ChatService 构造参数含 UserStreamRegistry"
---

<objective>
将所有 Phase 6 组件注册到 Koin 依赖注入容器，更新 HandlerRegistry 注册入口和 NebulaServer 调用处。

Purpose: 确保 Phase 6 所有组件可在运行时被 Koin 解析，Handler 可被 Dispatcher 发现和路由。
Output: 1 个修改文件（GatewayModule.kt — handlerModule + registerHandlers）。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-chat-message/06-CONTEXT.md
@.planning/phases/06-chat-message/06-RESEARCH.md
@.planning/phases/06-chat-message/06-PATTERNS.md
@.planning/phases/06-chat-message/06-01-PLAN.md
@.planning/phases/06-chat-message/06-02-PLAN.md
@.planning/phases/06-chat-message/06-03-PLAN.md

<interfaces>
<!-- 需要引用的现有代码和新建代码的接口 -->

**现有 GatewayModule.kt** (gateway/.../di/GatewayModule.kt):
- `val frameworkModule = module { ... }` — HandlerRegistry, ProtoCodec, SessionRegistry, 4 个 Interceptor
- `val handlerModule = module { ... }` — 9 个 Handler single { }
- `private inline fun <reified ReqT, RespT> HandlerRegistry.register(handler)` — inline 扩展函数
- `fun registerHandlers(registry, protoCodec, pingHandler, loginHandler, ... , getPrivacyHandler)` — 显式参数列表 + 逐个 registry.register()

**NebulaServer.kt** (server/.../ — registerHandlers 调用处):
- `registerHandlers(registry, protoCodec, pingHandler, loginHandler, ...)` — 需要追加 Phase 6 参数

**需要注册的新组件**:
- `UserStreamRegistry` — `single { UserStreamRegistry() }`
- `PushService` — `single { PushService(get(), get()) }`
- `SendMessageStep` 列表 — 4 个 Step 使用 `single<SendMessageStep> { ... }` 通过接口类型注册
- `SendMessageHandler` — `single { SendMessageHandler(get()) }`（get() 获取 List<SendMessageStep>）
- `PullMessagesHandler` — `single { PullMessagesHandler(get()) }`
- `ReadReportHandler` — `single { ReadReportHandler(get(), get(), get(), get()) }`

**ChatService 构造变更**（06-02 Task 2）:
- 从 `ChatService(dispatcher, sessionRegistry, registry)` 变为 `ChatService(dispatcher, sessionRegistry, registry, userStreamRegistry)`
- Koin 中 ChatService 的注册在 NebulaServer.kt（server 模块），不在 GatewayModule.kt
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: 更新 GatewayModule.kt — handlerModule 追加 Phase 6 组件</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt
  </files>
  <read_first>
    gateway/.../di/GatewayModule.kt（当前版本 — handlerModule 和 registerHandlers 结构）
    gateway/.../handler/chat/send/SendMessageHandler.kt（06-02 创建）
    gateway/.../handler/chat/send/SendMessageStep.kt（06-02 创建 — 需按接口类型注册）
    gateway/.../handler/chat/send/ValidateStep.kt（06-02 创建）
    gateway/.../handler/chat/send/DedupStep.kt（06-02 创建）
    gateway/.../handler/chat/send/WriteStep.kt（06-02 创建）
    gateway/.../handler/chat/send/PushStep.kt（06-02 创建）
    gateway/.../handler/message/PullMessagesHandler.kt（06-03 创建）
    gateway/.../handler/message/ReadReportHandler.kt（06-03 创建）
    gateway/.../session/UserStreamRegistry.kt（06-01 创建）
    gateway/.../push/PushService.kt（06-01 创建）
  </read_first>
  <action>
    修改 `handlerModule`（追加在已有 Handler 声明之后）：

    **新增 import：**
    ```kotlin
    import com.nebula.gateway.handler.chat.send.DedupStep
    import com.nebula.gateway.handler.chat.send.PushStep
    import com.nebula.gateway.handler.chat.send.SendMessageHandler
    import com.nebula.gateway.handler.chat.send.SendMessageStep
    import com.nebula.gateway.handler.chat.send.ValidateStep
    import com.nebula.gateway.handler.chat.send.WriteStep
    import com.nebula.gateway.handler.message.PullMessagesHandler
    import com.nebula.gateway.handler.message.ReadReportHandler
    import com.nebula.gateway.push.PushService
    import com.nebula.gateway.session.UserStreamRegistry
    ```

    **handlerModule 内追加（在 `single { GetPrivacyHandler(get()) }` 之后）：**
    ```kotlin
    // Phase 6: Chat & Message（D-13 Step 链 + PushService）
    single { UserStreamRegistry() }                          // D-01
    single { PushService(get(), get()) }                      // PushService(UserStreamRegistry, ConversationMemberRepository)
    // Step 链注册（D-13 通过接口类型注入 List<SendMessageStep>）
    single<SendMessageStep> { ValidateStep(get()) }           // ValidateStep(ConversationMemberRepository)
    single<SendMessageStep> { DedupStep(get()) }              // DedupStep(RedisConnection)
    single<SendMessageStep> { WriteStep(get(), get(), get()) } // WriteStep(SnowflakeIdGenerator, MessageQueueRepository, RedisConnection)
    single<SendMessageStep> { PushStep(get()) }               // PushStep(PushService)
    single { SendMessageHandler(get()) }                      // SendMessageHandler(List<SendMessageStep>)
    single { PullMessagesHandler(get()) }                     // PullMessagesHandler(MessageRepository)
    single { ReadReportHandler(get(), get(), get(), get()) }  // ReadReportHandler(ConversationRepo, ConvMemberRepo, PushService, RedisConnection)
    ```

    KDoc 注释引用 D-01, D-13。
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - UserStreamRegistry、PushService、4 个 SendMessageStep、3 个 Handler 均在 handlerModule 中注册
    - Koin 泛型无歧义（SendMessageStep 使用 single&lt;SendMessageStep&gt; 避免 KClass 冲突）
  </acceptance_criteria>
  <done>handlerModule 追加完毕，编译通过</done>
  <what-built>GatewayModule.kt 的 handlerModule 新增 Phase 6 组件注册</what-built>
  <how-to-verify>
    1. `./gradlew :gateway:compileKotlin` 通过
    2. 检查 handlerModule 是否包含 8 个新 single 声明
  </how-to-verify>
  <resume-signal>确认编译通过后继续</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: 更新 GatewayModule.kt — registerHandlers 追加 Phase 6 Handler + NebulaServer 调用处</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt
    server/src/main/kotlin/com/nebula/server/NebulaServer.kt
  </files>
  <read_first>
    gateway/.../di/GatewayModule.kt（registerHandlers 函数 — 当前 Phase 5 参数列表）
    server/.../NebulaServer.kt（registerHandlers 调用处）
    gateway/.../handler/chat/send/SendMessageHandler.kt（06-02 — method="chat/send"）
    gateway/.../handler/message/PullMessagesHandler.kt（06-03 — method="message/pull"）
    gateway/.../handler/message/ReadReportHandler.kt（06-03 — method="message/read"）
  </read_first>
  <action>
    **修改 registerHandlers 函数**（GatewayModule.kt）：

    1. 新增 import:
       ```kotlin
       import com.nebula.gateway.handler.chat.send.SendMessageHandler
       import com.nebula.gateway.handler.message.PullMessagesHandler
       import com.nebula.gateway.handler.message.ReadReportHandler
       ```

    2. 在函数参数列表末尾追加 3 个参数（要求 KDoc 同步更新 @param）:
       ```kotlin
       fun registerHandlers(
           // ... 现有 Phase 5 参数 ...
           getPrivacyHandler: GetPrivacyHandler,
           // 新增 Phase 6 参数
           sendMessageHandler: SendMessageHandler,
           pullMessagesHandler: PullMessagesHandler,
           readReportHandler: ReadReportHandler
       ) { ... }
       ```

    3. 在函数体末尾追加 3 行注册调用:
       ```kotlin
       registry.register(sendMessageHandler)   // chat/send: SendMessageReq → SendMessageResp
       registry.register(pullMessagesHandler)  // message/pull: PullMessagesReq → PullMessagesResp
       registry.register(readReportHandler)    // message/read: ReadReportReq → Response
       ```

    4. 更新 registerHandlers KDoc 注释，在 `@param getPrivacyHandler` 后追加 3 个新 @param。

    **修改 NebulaServer.kt 调用处**：
    1. 查找 registerHandlers 调用位置
    2. 在调用参数列表末尾追加 3 个新参数:
       ```kotlin
       get<SendMessageHandler>(),  // Phase 6: chat/send
       get<PullMessagesHandler>(), // Phase 6: message/pull
       get<ReadReportHandler>()    // Phase 6: message/read
       ```
    3. 如果 NebulaServer.startKoin() 中也包含 handlerModule（而非仅在 GatewayModule 中定义），确保 server 模块 import 了所有 Phase 6 Handler 的包路径

    **注意**：NebulaServer.kt 可能位于 server 模块，不在 gateway 模块。如果 NebulaServer 使用 `loadKoinModules(handlerModule)` 方式加载 handlerModule，则无需修改调用处（registerHandlers 调用已在 GatewayModule 中）。需先确认 NebulaServer 的实现在执行时根据实际代码判断。
  </action>
  <verify>
    <automated>./gradlew :server:compileKotlin && ./gradlew build</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew build` 退出码 0（全部模块编译通过）
    - registerHandlers 函数参数列表包含 3 个 Phase 6 Handler 参数
    - registerHandlers 函数体包含 3 行 Phase 6 Handler 注册调用
    - registerHandlers 的调用处（NebulaServer.kt）传递了 3 个新实参
    - KDoc 注释覆盖新增的 @param
  </acceptance_criteria>
  <done>registerHandlers 和调用处全部更新，完整编译通过</done>
  <what-built>registerHandlers 函数和 NebulaServer 调用处追加 Phase 6 Handler 注册</what-built>
  <how-to-verify>
    1. `./gradlew :gateway:compileKotlin` 通过
    2. `./gradlew :server:compileKotlin` 通过
    3. `./gradlew build` 通过
  </how-to-verify>
  <resume-signal>确认全部编译通过后完成</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (无新增)—DI 注册不引入新的信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering | Koin module registration | mitigate | 编译期类型安全 — 所有 single { } 注册通过 Kotlin 编译器验证类型兼容性 |
| T-06-13 | Tampering | HandlerRegistry.register | mitigate | registerHandlers 使用 inline reified 泛型，在编译期绑定 Req/Resp 类型，运行期零反射（现有 D-12 模式） |
</threat_model>

<verification>
1. `./gradlew :gateway:compileKotlin` 通过
2. `./gradlew :server:compileKotlin` 通过（如有 server 模块）
3. `./gradlew build` 通过
</verification>

<success_criteria>
- GatewayModule.kt 的 handlerModule 包含所有 Phase 6 组件注册
- registerHandlers 函数包含所有 Phase 6 Handler 注册
- 调用 registerHandlers 的代码传递了 Phase 6 Handler 参数
- 完整构建通过
</success_criteria>

<output>
创建 `.planning/phases/06-chat-message/06-04-SUMMARY.md` 完成时
</output>

---
phase: 06-chat-message
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "proto/src/main/proto/nebula/message/message.proto"
  - "gateway/src/main/kotlin/com/nebula/gateway/session/UserStreamRegistry.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageException.kt"
  - "gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt"
autonomous: true
requirements:
  - BIZ-CHAT-01
  - BIZ-CHAT-02
  - BIZ-MSG-01
  - BIZ-MSG-02

must_haves:
  truths:
    - "UserStreamRegistry 可注册/查询/移除 userId→StreamObserver 映射"
    - "PushService 可通过 UserStreamRegistry 查找在线成员并推送 ChatMessage Envelope"
    - "PushService 可向私聊发送者推送 ReadReceiptPayload Envelope"
    - "message.proto 的 ChatMessage 不包含 sender_username/sender_avatar 字段，包含 receiver_uid 字段"
    - "SendMessageException 作为消息领域异常可由 ExceptionInterceptor 捕获"
  artifacts:
    - path: "gateway/.../session/UserStreamRegistry.kt"
      provides: "userId→StreamObserver 注册中心"
      contains: "class UserStreamRegistry"
    - path: "gateway/.../push/PushService.kt"
      provides: "消息推送服务"
      contains: "fun pushMessage / fun pushReadReceipt"
    - path: "gateway/.../handler/chat/send/SendMessageException.kt"
      provides: "消息领域异常"
      contains: "class SendMessageException"
  key_links:
    - from: "PushService.pushMessage"
      to: "UserStreamRegistry.getStreams"
      via: "userId → observer 查找"
    - from: "PushService.pushMessage"
      to: "ConversationMemberRepository.findByConversationId"
      via: "获取会话成员列表"
    - from: "PushService.pushReadReceipt"
      to: "UserStreamRegistry.getStreams"
      via: "senderUid → observer 查找"
---

<objective>
创建 Phase 6 的推送基础设施：UserStreamRegistry（userId→StreamObserver 映射）、PushService（构建 PUSH Envelope 并逐一推送）、SendMessageException（消息领域异常），以及重新生成 protobuf 以应用 message.proto 变更（D-22）。

Purpose: 为后续 SendMessageHandler 和 ReadReportHandler 提供推送依赖。
Output: 4 个基础组件 + protobuf 重新生成。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-chat-message/06-CONTEXT.md
@.planning/phases/06-chat-message/06-RESEARCH.md
@.planning/phases/06-chat-message/06-PATTERNS.md

<interfaces>
<!-- 执行者需要了解的现有 Key Types -->

**SessionRegistry** (gateway/.../session/SessionRegistry.kt — ConcurrentHashMap 注册中心类比):
- `ConcurrentHashMap<Long, MutableSet<String>> userIdIndex` — userId → token 集合
- `CopyOnWriteArrayList<(String) -> Unit> evictionCallbacks` — 回调列表
- `fun onEviction(callback: (token: String) -> Unit)` — 注册驱逐回调

**ChatService** (gateway/.../service/ChatService.kt — gRPC 流管理):
- 构造参数: `(dispatcher, sessionRegistry, registry)`
- `private val tokenToObserver = ConcurrentHashMap<String, StreamObserver<Envelope>>()`
- `private val scope = CoroutineScope(Dispatchers.IO + SupervisorJob())`
- `private suspend fun handleLoginSuccess(response: Response, responseObserver: StreamObserver<Envelope>)`
- `private inner class ChatStreamObserver(responseObserver)` — onCompleted/onError → `cleanupConnection()`
- `private fun cleanupConnection()` — `tokenToObserver.entries.removeIf { it.value == responseObserver }`
- `ensureEvictionCallbackRegistered()` — 注册 sessionRegistry.onEviction 回调

**MessageQueueRepository** (repository/.../redis/MessageQueueRepository.kt):
- `suspend fun enqueue(message: Map<String, String>): String?` — XADD 到 `queue:messages`

**ConversationMemberRepository** (repository/.../repository/ConversationMemberRepository.kt — JPA):
- `fun findByConversationId(conversationId: String): List<ConversationMemberEntity>` — 查询会话成员
- `fun findByConversationIdAndUserId(...): ConversationMemberEntity?` — 单一成员查询

**StreamObserver<Envelope>** — gRPC 类型:
- `fun onNext(envelope: Envelope)` — 推送 Envelope
- `fun onCompleted()` — 关闭流
- `fun onError(t: Throwable)` — 异常关闭

**Proto 构建模式** (ChatService.kt LOGOUT 推送):
```kotlin
Envelope.newBuilder()
    .setDirection(Direction.PUSH)
    .setMessage(Message.newBuilder()
        .setEventType(PushEventType.CHAT_MESSAGE)
        .setContent("")
        .setPayload(chatMessage.toByteString()))
    .build()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重新生成 protobuf（message.proto ChatMessage 变更 D-22）</name>
  <files>
    proto/src/main/proto/nebula/message/message.proto
    （gradle 重新生成到 build/generated/source/proto/）
  </files>
  <read_first>
    proto/src/main/proto/nebula/message/message.proto（当前版本）
    proto/src/main/proto/nebula/chat/chat.proto
  </read_first>
  <action>
    验证 message.proto 已包含 D-22 变更：ChatMessage 字段号 4(sender_username) 和 5(sender_avatar) 已移除，字段号 11(receiver_uid) 已添加。
    运行 `./gradlew :proto:generateProto` 重新生成 Java 代码。
    如果 build 失败，检查 message.proto 的 ChatMessage 字段定义是否与 SendMessageReq/SendMessageResp 兼容。

    验证点：
    - `grep -c "sender_username" message.proto` 返回 0
    - `grep -c "sender_avatar" message.proto` 返回 0
    - `grep -c "receiver_uid" message.proto` 返回 1（字段号 11）
    - `./gradlew :proto:generateProto` 成功
    - `./gradlew :gateway:compileKotlin` 通过（不因 proto 变更产生编译错误）
  </action>
  <verify>
    <automated>./gradlew :proto:generateProto && ./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :proto:generateProto` 退出码 0
    - `./gradlew :gateway:compileKotlin` 退出码 0（已知 ChatMessage 变更有其他模块引用）
    - ChatMessage Java 生成类不包含 `getSenderUsername()`/`getSenderAvatar()` 方法
    - ChatMessage Java 生成类包含 `getReceiverUid()` 方法
  </acceptance_criteria>
  <done>protobuf 重新生成完毕，gateway 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 UserStreamRegistry + SendMessageException</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/session/UserStreamRegistry.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/chat/send/SendMessageException.kt
  </files>
  <read_first>
    gateway/.../session/SessionRegistry.kt（ConcurrentHashMap 注册中心模式类比）
    gateway/.../Handler.kt（Handler 接口定义）
    proto/.../envelope.proto（Direction/Envelope 定义）
  </read_first>
  <action>
    创建 UserStreamRegistry（D-01，D-02）：
    package: `com.nebula.gateway.session`
    - `private val userStreams = ConcurrentHashMap<Long, CopyOnWriteArrayList<StreamObserver<Envelope>>>()`
    - `fun register(userId: Long, observer: StreamObserver<Envelope>)` — compute 追加到列表（D-02 多设备）
    - `fun removeStream(userId: Long, observer: StreamObserver<Envelope>)` — computeIfPresent 移除单个 observer，列表为空时删除键
    - `fun removeUser(userId: Long)` — remove 整个键（D-01 连接清理）
    - `fun getStreams(userId: Long): List<StreamObserver<Envelope>>` — 返回拷贝或空列表

    创建 SendMessageException（消息领域异常）：
    package: `com.nebula.gateway.handler.chat.send`
    继承 `BizException`，与 ConversationException 模式一致：
    `class SendMessageException(bizCode: BizCode, msg: String = bizCode.msg) : BizException(bizCode, msg)`
    添加 `companion object { private val logger = KotlinLogging.logger {} }`

    两个文件均添加完整 KDoc 中文注释，设计决策引用 D-编号。
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - UserStreamRegistry 包含 register/removeStream/removeUser/getStreams 四个方法
    - UserStreamRegistry.userStreams 类型为 `ConcurrentHashMap<Long, CopyOnWriteArrayList<StreamObserver<Envelope>>>`
    - SendMessageException 可被 `throw SendMessageException(BizCode.INVALID_PARAM, "msg")` 调用
  </acceptance_criteria>
  <done>UserStreamRegistry 和 SendMessageException 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 PushService（推送服务）</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/push/PushService.kt
  </files>
  <read_first>
    proto/.../envelope.proto（Direction.PUSH 定义）
    proto/.../message_type.proto（PushEventType.CHAT_MESSAGE / READ_RECEIPT 定义）
    proto/.../message/message.proto（ChatMessage / ReadReceiptPayload 定义）
    gateway/.../service/ChatService.kt（Envelope 构建模式 — LOGOUT 推送 lines 229-236）
    gateway/.../session/UserStreamRegistry.kt（本计划 Task 2 创建）
    repository/.../repository/ConversationMemberRepository.kt（findByConversationId 方法）
  </read_first>
  <action>
    创建 PushService（D-11, D-15, D-12, D-05, D-09）：
    package: `com.nebula.gateway.push`
    构造注入：`UserStreamRegistry`, `ConversationMemberRepository`

    **pushMessage** (suspend fun):
    参数: `convId: String`, `chatMessage: ChatMessage`, `excludeUid: Long`
    1. conversationMemberRepository.findByConversationId(convId) 获取成员列表
    2. filter { it.userId != excludeUid }（D-09 排除发送者）
    3. 逐个遍历成员:
       - userStreamRegistry.getStreams(member.userId) 获取该成员所有在线设备
       - 对每个 observer 构建 Envelope:
         - Direction.PUSH
         - Message(eventType=CHAT_MESSAGE, content="", payload=chatMessage.toByteString())
       - observer.onNext(envelope)
       - 对每个 observer 包裹 try-catch（D-05 容错），catch 时 log error 并调用 userStreamRegistry.removeStream

    **pushReadReceipt** (fun, non-suspend — 无 I/O):
    参数: `senderUid: Long`, `payload: ReadReceiptPayload`
    1. userStreamRegistry.getStreams(senderUid)
    2. 逐个遍历:
       - 构建 Envelope: Direction.PUSH, Message(eventType=READ_RECEIPT, content="", payload=payload.toByteString())
       - observer.onNext(envelope)
       - 包裹 try-catch，catch 时 log + removeStream

    两个方法均不依赖 Session（Pitfall 5 预防 — 不从 CoroutineContext 获取 Session）。
    添加完整 KDoc 中文注释，引用 D-05, D-09, D-11, D-12, D-15。
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :gateway:compileKotlin` 退出码 0
    - PushService 包含 pushMessage(suspend) 和 pushReadReceipt(non-suspend) 两个方法
    - pushMessage 调用 conversationMemberRepository.findByConversationId
    - pushMessage 在推送到每个 observer 时包裹 try-catch
    - pushMessage 排除 excludeUid（filter）
    - pushReadReceipt 发送 PushEventType.READ_RECEIPT Envelope
  </acceptance_criteria>
  <done>PushService 编译通过，推送逻辑完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PushService → StreamObserver | 消息从服务端推送到客户端 gRPC 流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | PushService.pushMessage | mitigate | 每个 receiver 独立 try-catch；单个 observer.onNext 异常不阻断其他 receiver（D-05） |
| T-06-02 | Elevation of Privilege | PushService | mitigate | PushService 不自行判断推送权限；由调用方（PushStep/ReadReportHandler）保证仅推送给验证过的成员 |
| T-06-03 | Information Disclosure | UserStreamRegistry | mitigate | 仅通过 getStreams(userId) 暴露接口，不提供全量遍历方法；PushService 只推已通过成员验证的 convId |
</threat_model>

<verification>
1. `./gradlew :proto:generateProto` 通过
2. `./gradlew :gateway:compileKotlin` 通过
3. UserStreamRegistry 四方法接口完整
4. PushService 两个推送方法接口完整
5. SendMessageException 可编译
</verification>

<success_criteria>
- 所有新建文件编译通过
- UserStreamRegistry 可管理 userId→observer 映射
- PushService 可推送 ChatMessage 和 ReadReceiptPayload Envelope
- SendMessageException 可作为领域异常被 ExceptionInterceptor 捕获
</success_criteria>

<output>
创建 `.planning/phases/06-chat-message/06-01-SUMMARY.md` 完成时
</output>

---
phase: 05-user-authentication
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01                   # 需要 PrivacyRepository（含 batchGetHideOnlineStatus）、SessionKey
files_modified:
  - gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetProfileHandler.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetUserHandler.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetStatusHandler.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/user/SetPrivacyHandler.kt
  - gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetPrivacyHandler.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/GetProfileHandlerTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/BatchGetUserHandlerTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/BatchGetStatusHandlerTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/SetPrivacyHandlerTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/user/GetPrivacyHandlerTest.kt
autonomous: true
requirements:
  - BIZ-USER-02
  - BIZ-USER-03
  - BIZ-USER-04
  - BIZ-USER-05
  - BIZ-USER-06

must_haves:
  truths:
    - "用户可通过 user/getProfile 获取指定用户的详细资料"
    - "用户可通过 user/batchGet 批量查询用户简要信息（缺失 ID 静默跳过）"
    - "D-10: 用户可通过 user/batchGetStatus 批量查询在线状态（隐藏用户被批量过滤）"
    - "D-09: 用户可通过 user/setPrivacy 设置在线状态可见性（Redis 立即生效 + 异步 MySQL best-effort）"
    - "D-11: 用户可通过 user/getPrivacy 读取当前隐私设置（Redis 优先，MySQL 兜底）"
  artifacts:
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetProfileHandler.kt"
      provides: "user/getProfile — 用户详细资料"
      exports: ["GetProfileHandler"]
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetUserHandler.kt"
      provides: "user/batchGet — 批量用户摘要"
      exports: ["BatchGetUserHandler"]
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetStatusHandler.kt"
      provides: "user/batchGetStatus — 批量在线状态（MGET 批量隐私过滤，非 N+1）"
      exports: ["BatchGetStatusHandler"]
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/user/SetPrivacyHandler.kt"
      provides: "user/setPrivacy — 设置隐私（Redis + 异步 MySQL best-effort）"
      exports: ["SetPrivacyHandler"]
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetPrivacyHandler.kt"
      provides: "user/getPrivacy — 读取隐私设置"
      exports: ["GetPrivacyHandler"]
  key_links:
    - from: "BatchGetStatusHandler"
      to: "PrivacyRepository.batchGetHideOnlineStatus()"
      via: "一次 MGET 批量查询隐私设置，非 N 次 GET（Review 修复 N+1）"
      pattern: "batchGetHideOnlineStatus"
    - from: "SetPrivacyHandler"
      to: "PrivacyRepository.setHideOnlineStatus()"
      via: "写 Redis 立即生效 + 异步刷 MySQL（D-09, D-11）"
      pattern: "setHideOnlineStatus"
    - from: "GetPrivacyHandler"
      to: "PrivacyRepository.getHideOnlineStatus()"
      via: "读 Redis，MySQL 回退（D-11）"
      pattern: "getHideOnlineStatus"
---

<objective>
**实现用户业务 API Handler** — getProfile、batchGet、batchGetStatus（MGET 批量隐私过滤修复）、setPrivacy、getPrivacy

**Purpose:** 完成 Phase 5 剩余的用户 CRUD API，包括个人资料读取、批量查询、在线状态查询（含 MGET 批量隐私过滤——修复 Review 反馈 N+1 问题）、隐私设置读写。

**Output:**
- GetProfileHandler: user/getProfile — 返回用户详细资料
- BatchGetUserHandler: user/batchGet — 批量用户摘要（缺失 ID 静默跳过已文档化）
- BatchGetStatusHandler: user/batchGetStatus — 批量在线状态（**使用 batchGetHideOnlineStatus MGET 批量查询，非逐用户查询**）
- SetPrivacyHandler: user/setPrivacy — 设置在线状态可见性（Redis 立即生效 + 异步 MySQL best-effort 已文档化）
- GetPrivacyHandler: user/getPrivacy — 读取隐私设置
- 各 Handler 单元测试
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-authentication/05-RESEARCH.md
@.planning/phases/05-user-authentication/05-CONTEXT.md

**Phase 5 Plan 01 产出：**
- PrivacyRepository: getHideOnlineStatus() / setHideOnlineStatus() / **batchGetHideOnlineStatus()**（MGET 批量查询）
- AuthInterceptor: extractToken() 实现
- SessionRegistry: deviceTypeIndex + registerWithDeviceType()

**Review 反馈（05-REVIEWS.md）本计划已修复的问题：**
- [REVIEW-高] N+1 Redis 查询 → BatchGetStatusHandler 使用 PrivacyRepository.batchGetHideOnlineStatus()（一次 MGET），不再对每个用户逐次调用 getHideOnlineStatus()
- [REVIEW-中] 缺失 ID 静默跳过 → BatchGetUserHandler KDoc 中明确记录设计权衡
- [REVIEW-中] 异步 MySQL 失败 → SetPrivacyHandler KDoc 中明确标注 best-effort 和 Pitfall 4

**关键接口提取：**

From `user.proto` (enhanced in Plan 01):
```protobuf
message GetProfileReq { int64 uid = 1; }
message GetProfileResp { int64 uid = 1; string username = 2; string display_name = 3;
  string avatar_url = 4; int32 gender = 5; string bio = 6; int64 created_at = 7; }
message BatchIdRequest { repeated int64 uids = 1; }
message BatchGetUserResp { repeated UserBrief users = 1; }
message BatchGetStatusResp { repeated UserOnlineStatus statuses = 1; }
message SetPrivacyReq { bool hide_online_status = 1; }
message GetPrivacyReq {}   // 新增于 Plan 01，空消息，userId 从 Session 获取
message GetPrivacyResp { bool hide_online_status = 1; }
message UserBrief { int64 uid = 1; string username = 2; string display_name = 3;
  string avatar_url = 4; int64 created_at = 5; }
message UserOnlineStatus { int64 uid = 1; int32 status = 2; int64 last_active_at = 3; }
```

From `SessionKey.kt`:
```kotlin
suspend fun CoroutineContext.requireSession(): Session
```

From `PrivacyRepository.kt` (created in Plan 01):
```kotlin
class PrivacyRepository(
    private val connection: StatefulRedisConnection<String, String>,
    private val userRepository: UserRepository
) {
    suspend fun getHideOnlineStatus(userId: Long): Boolean
    suspend fun setHideOnlineStatus(userId: Long, hide: Boolean)
    suspend fun batchGetHideOnlineStatus(userIds: List<Long>): Set<Long>  // 新增——MGET 批量查询
}
```

From `OnlineStatusRepository.kt`:
```kotlin
class OnlineStatusRepository(private val connection: StatefulRedisConnection<String, String>) {
    suspend fun isOnline(userId: Long): Boolean
    suspend fun setOnline(userId: Long, statusData: String)
    suspend fun setOffline(userId: Long)
}
```

**D-10 隐私过滤规则：**
- batchGetStatus 查询时，对 hide_online_status=true 的用户跳过（不返回其在线状态）
- **Review 修复：** 使用 MGET 一次批量查询所有用户的隐私设置，避免 N+1 Redis 查询

**D-09/D-11 隐私读写策略：**
- setPrivacy: 写 Redis 立即生效 + 异步 CoroutineScope(Dispatchers.IO).launch 刷 MySQL（best-effort，接受宕机写入丢失）
- getPrivacy: 优先读 Redis，未命中则从 MySQL 读取并写回 Redis
</context>

<tasks>

<task type="auto">
  <name>Task 1: GetProfileHandler + BatchGetUserHandler（含缺失 ID 文档化）</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetProfileHandler.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetUserHandler.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/user/GetProfileHandlerTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/user/BatchGetUserHandlerTest.kt
  </files>
  <action>
    1.1 **创建 GetProfileHandler** — method = "user/getProfile"（BIZ-USER-02）：

    ```kotlin
    class GetProfileHandler(
        private val userRepository: UserRepository
    ) : Handler<GetProfileReq, GetProfileResp> {
        override val method: String = "user/getProfile"

        override suspend fun handle(req: GetProfileReq): GetProfileResp {
            val user = userRepository.findById(req.uid)
                ?: throw UserException(BizCode.USER_NOT_FOUND)

            return GetProfileResp.newBuilder()
                .setUid(user.id!!)
                .setUsername(user.username)
                .setDisplayName(user.nickname)
                .setAvatarUrl(user.avatar)
                .setCreatedAt(user.createdAt?.toEpochMilli() ?: 0)
                .build()
        }
    }
    ```

    KDoc 说明：
    - 当前所有用户资料公开可查（Phase 8 可能增加好友/非好友可见范围）
    - GetProfileResp 的 gender(5) 和 bio(6) 在 v1 中暂不填充（UserEntity 尚无相应字段）

    1.2 **创建 BatchGetUserHandler** — method = "user/batchGet"（BIZ-USER-03）：

    ```kotlin
    class BatchGetUserHandler(
        private val userRepository: UserRepository
    ) : Handler<BatchIdRequest, BatchGetUserResp> {
        override val method: String = "user/batchGet"

        override suspend fun handle(req: BatchIdRequest): BatchGetUserResp {
            val users = userRepository.findAllById(req.uidsList.map { it })
            val builder = BatchGetUserResp.newBuilder()

            users.forEach { entity ->
                builder.addUsers(UserBrief.newBuilder()
                    .setUid(entity.id!!)
                    .setUsername(entity.username)
                    .setDisplayName(entity.nickname)
                    .setAvatarUrl(entity.avatar)
                    .build())
            }
            return builder.build()
        }
    }
    ```

    **KDoc 中明确记录（Review 修复#缺失 ID 文档化）：**
    - `findAllById()` 对数据库中不存在的 ID 静默跳过，不返回对应条目
    - 这是 JPA 的预期行为：低延迟批量查询，不因无效 ID 中断
    - 客户端应通过结果中缺失的 UID 判断该用户不存在

    1.3 **单元测试：**

    **GetProfileHandlerTest.kt** (JUnit5 + MockK + runTest)：
    - `获取已有用户资料` — mock userRepository.findById() → UserEntity；验证 GetProfileResp uid/username/displayName 正确
    - `用户不存在` — mock findById() → null；验证抛出 UserException(USER_NOT_FOUND)

    **BatchGetUserHandlerTest.kt**：
    - `批量获取用户` — mock findAllById() → 2 个 UserEntity；验证返回 2 条 UserBrief
    - `空列表` — mock 返回空列表；验证 users 列表为空
    - `部分 ID 不存在` — mock 返回少于输入 ID 数的结果；验证只返回存在的用户（Review 修复：文档化此行为）
  </action>
  <read_first>
    gateway/src/main/kotlin/com/nebula/gateway/handler/PingHandler.kt（Handler 接口模式参考）
  </read_first>
  <acceptance_criteria>
    - GetProfileHandler 使用 userRepository.findById() 查询
    - BatchGetUserHandler 使用 userRepository.findAllById() 批量查询
    - BatchGetUserHandler KDoc 明确说明缺失 ID 静默跳过的设计权衡（Review 修复）
    - 编译通过：./gradlew :gateway:compileKotlin BUILD SUCCESSFUL
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/linyu/project/personal/Nebula/nebula_server && ./gradlew :gateway:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GetProfileHandler.kt 存在，实现 Handler&lt;GetProfileReq, GetProfileResp&gt;
    - BatchGetUserHandler.kt 存在，缺失 ID 文档化（Review 修复）
    - 测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: BatchGetStatusHandler（MGET 批量隐私过滤修复）+ SetPrivacyHandler（best-effort 文档化）+ GetPrivacyHandler</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/handler/user/BatchGetStatusHandler.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/user/SetPrivacyHandler.kt
    gateway/src/main/kotlin/com/nebula/gateway/handler/user/GetPrivacyHandler.kt
  </files>
  <action>
    2.1 **创建 BatchGetStatusHandler** — method = "user/batchGetStatus"（BIZ-USER-04, D-10）—— **Review 修复：MGET 批量隐私查询，非 N+1**：

    ```kotlin
    class BatchGetStatusHandler(
        private val onlineStatusRepository: OnlineStatusRepository,
        private val privacyRepository: PrivacyRepository
    ) : Handler<BatchIdRequest, BatchGetStatusResp> {
        override val method: String = "user/batchGetStatus"

        override suspend fun handle(req: BatchIdRequest): BatchGetStatusResp {
            // Review 修复：使用 batchGetHideOnlineStatus() 一次 MGET 批量查询
            // 原方案对每个用户单独调用 getHideOnlineStatus(uid) 产生 N 次 Redis GET
            // 新方案使用 Redis MGET 一次查询所有用户的隐私设置
            val hiddenUserIds = privacyRepository.batchGetHideOnlineStatus(req.uidsList)

            val builder = BatchGetStatusResp.newBuilder()
            for (uid in req.uidsList) {
                if (uid in hiddenUserIds) continue  // D-10: 跳过隐藏用户
                val isOnline = onlineStatusRepository.isOnline(uid)
                builder.addStatuses(UserOnlineStatus.newBuilder()
                    .setUid(uid)
                    .setStatus(if (isOnline) 1 else 0)  // 1=在线, 0=离线
                    .build())
            }
            return builder.build()
        }
    }
    ```

    **KDoc 中说明（Review 修复）：**
    - 使用 batchGetHideOnlineStatus()（Redis MGET）批量查询隐私设置，避免 N+1 性能问题
    - D-10: hide_online_status=true 的用户不在结果中返回

    2.2 **创建 SetPrivacyHandler** — method = "user/setPrivacy"（BIZ-USER-05, D-09, D-11）：

    ```kotlin
    class SetPrivacyHandler(
        private val privacyRepository: PrivacyRepository
    ) : Handler<SetPrivacyReq, Response> {
        override val method: String = "user/setPrivacy"

        override suspend fun handle(req: SetPrivacyReq): Response {
            val session = coroutineContext.requireSession()
            privacyRepository.setHideOnlineStatus(session.userId, req.hideOnlineStatus)
            return Response.newBuilder()
                .setCode(BizCode.OK.code)
                .setMsg("ok")
                .setMethod(method)
                .build()
        }
    }
    ```

    **KDoc 中明确记录（Review 修复#best-effort 文档化）：**
    - 写 Redis 立即生效 + 异步 CoroutineScope(Dispatchers.IO).launch 刷 MySQL
    - **异步 MySQL 刷写是 best-effort 模式（Pitfall 4）：**
      - 服务器 crash 在 Redis 写完后、MySQL 刷完前，最后一次隐私设置丢失
      - 重启后首次 getPrivacy 从 MySQL 回退读取，可恢复持久化状态
      - 此权衡已被接受：隐私设置变更频率低，Redis 为主要存储，MySQL 为持久化备份
    - 使用通用 Response 作为返回类型（与 PingHandler 同类模式），仅包含状态码

    2.3 **创建 GetPrivacyHandler** — method = "user/getPrivacy"（BIZ-USER-06, D-11）：

    ```kotlin
    class GetPrivacyHandler(
        private val privacyRepository: PrivacyRepository
    ) : Handler<GetPrivacyReq, GetPrivacyResp> {
        override val method: String = "user/getPrivacy"

        override suspend fun handle(req: GetPrivacyReq): GetPrivacyResp {
            val session = coroutineContext.requireSession()
            val hideOnlineStatus = privacyRepository.getHideOnlineStatus(session.userId)
            return GetPrivacyResp.newBuilder()
                .setHideOnlineStatus(hideOnlineStatus)
                .build()
        }
    }
    ```

    KDoc 说明：
    - 读取策略（D-11）：优先读 Redis，未命中则从 MySQL 读取并写回 Redis（Pitfall 4 的恢复机制）
    - 使用空的 GetPrivacyReq 入参，userId 从 Session 获取
  </action>
  <read_first>
    gateway/src/main/kotlin/com/nebula/gateway/handler/PingHandler.kt（Handler 接口和 CoroutineContext Session 模式参考）
  </read_first>
  <acceptance_criteria>
    - BatchGetStatusHandler 使用 privacyRepository.batchGetHideOnlineStatus()（一次 MGET）而非逐个调用 getHideOnlineStatus()（Review 修复）
    - BatchGetStatusHandler KDoc 说明 MGET 批量查询设计
    - SetPrivacyHandler KDoc 说明异步 MySQL best-effort 和 Pitfall 4（Review 修复）
    - GetPrivacyHandler 使用 GetPrivacyReq 空消息
    - 编译通过：./gradlew :gateway:compileKotlin BUILD SUCCESSFUL
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/linyu/project/personal/Nebula/nebula_server && ./gradlew :gateway:compileKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - BatchGetStatusHandler.kt 存在，使用 batchGetHideOnlineStatus() 批量过滤（Review 修复 N+1）
    - SetPrivacyHandler.kt 存在，best-effort 模式已文档化（Review 修复）
    - GetPrivacyHandler.kt 存在，使用 GetPrivacyReq 空消息
    - ./gradlew :gateway:compileKotlin BUILD SUCCESSFUL
  </done>
</task>

<task type="auto">
  <name>Task 3: Handler 单元测试 — Status + Privacy</name>
  <files>
    gateway/src/test/kotlin/com/nebula/gateway/handler/user/BatchGetStatusHandlerTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/user/SetPrivacyHandlerTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/user/GetPrivacyHandlerTest.kt
  </files>
  <action>
    使用 JUnit5 + MockK + runTest 创建单元测试：

    **BatchGetStatusHandlerTest.kt：**
    - `查询在线状态 — 无隐藏用户` — mock batchGetHideOnlineStatus() → emptySet()；mock isOnline(uid=1) → true；验证返回 1 条 status，status=1
    - `隐藏用户被过滤` — mock batchGetHideOnlineStatus([1,2]) → setOf(1)；验证 uid=1 不在结果中，uid=2 在结果中
    - `混合状态` — 3 个用户：1 隐藏 + 1 在线 + 1 离线；mock batchGetHideOnlineStatus() → setOf(hiddenUid)；验证结果数量为 2
    - `空列表` — 传入空 uidsList；验证返回空结果

    **SetPrivacyHandlerTest.kt：**
    - `设置隐藏在线状态` — mock Session 通过 `withContext(SessionKey(session))` 注入；mock privacyRepository.setHideOnlineStatus()；验证被调用
    - `设置可见` — mock setHideOnlineStatus(userId, false)；验证被调用

    Session 注入方式：
    ```kotlin
    val session = Session(1001L, "token-x", "MOBILE", "dev-1", "conn-1")
    val handler = SetPrivacyHandler(privacyRepository)
    val result = withContext(SessionKey(session)) {
        handler.handle(req)
    }
    ```

    **GetPrivacyHandlerTest.kt：**
    - `读取隐藏状态` — mock getHideOnlineStatus() → true；验证 GetPrivacyResp.hideOnlineStatus = true
    - `读取可见状态` — mock getHideOnlineStatus() → false；验证 hideOnlineStatus = false

    **Session 注入说明：** 所有需要 Session 的 Handler 测试中，使用 `withContext(SessionKey(session))` 包裹 handle() 调用。
  </action>
  <read_first>
    gateway/src/test/kotlin/com/nebula/gateway/handler/PingHandlerTest.kt（测试模式参考）
  </read_first>
  <acceptance_criteria>
    - BatchGetStatusHandlerTest 包含 4+ 测试用例
    - SetPrivacyHandlerTest 包含 2+ 测试用例
    - GetPrivacyHandlerTest 包含 2+ 测试用例
    - 所有测试通过：./gradlew :gateway:test BUILD SUCCESSFUL
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/linyu/project/personal/Nebula/nebula_server && ./gradlew :gateway:test 2>&1 | tail -15</automated>
  </verify>
  <done>
    - BatchGetStatusHandlerTest.kt 含 4+ 测试用例
    - SetPrivacyHandlerTest.kt 含 2+ 测试用例
    - GetPrivacyHandlerTest.kt 含 2+ 测试用例
    - ./gradlew :gateway:test BUILD SUCCESSFUL（所有测试通过）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → GetProfileHandler | 目标用户 UID 来自客户端 |
| 客户端 → PrivacyRepository | 隐私设置写入来自客户端请求 |
| 客户端 → BatchGetStatusHandler | 查询 UID 列表来自客户端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | BatchGetStatusHandler | mitigate | D-10 隐私过滤：batchGetHideOnlineStatus() 使用 Redis MGET 批量查询隐藏用户，不在结果中返回 |
| T-05-10 | Tampering | SetPrivacyHandler | mitigate | 只允许修改当前用户的隐私设置（userId 从 Session 获取，非请求参数） |
| T-05-11 | Information Disclosure | GetProfileHandler | accept | 当前所有用户资料公开；Phase 8 可增加好友/非好友可见范围 |
</threat_model>

<verification>
**编译验证：**
```bash
./gradlew :gateway:compileKotlin
```

**N+1 修复验证（Review 重点）：**
```bash
grep "batchGetHideOnlineStatus" gateway/src/main/kotlin/.../BatchGetStatusHandler.kt
# 预期输出：使用 batchGetHideOnlineStatus() 批量查询隐私设置
```

**测试验证：**
```bash
./gradlew :gateway:test
```

**内容验证：**
- grep "GetProfileHandler" gateway/src/main/kotlin/.../GetProfileHandler.kt → 文件存在
- grep "batchGetHideOnlineStatus" gateway/src/main/kotlin/.../BatchGetStatusHandler.kt → MGET 批量查询
- grep "SetPrivacyHandler" gateway/src/main/kotlin/.../SetPrivacyHandler.kt → best-effort 文档化
- grep "GetPrivacyHandler" gateway/src/main/kotlin/.../GetPrivacyHandler.kt → 文件存在
</verification>

<success_criteria>
1. 所有 5 个 Handler 创建完成，编译通过
2. BatchGetStatusHandler 使用 batchGetHideOnlineStatus() MGET 批量过滤（Review 修复 N+1）
3. BatchGetUserHandler KDoc 文档化缺失 ID 静默跳过行为（Review 修复）
4. SetPrivacyHandler KDoc 标注异步 MySQL best-effort 和 Pitfall 4（Review 修复）
5. SetPrivacyHandler 只允许修改当前用户的隐私设置
6. GetPrivacyHandler 使用 GetPrivacyReq 空消息 + Session 获取 userId
7. 所有 8+ 单元测试通过
8. ./gradlew :gateway:test BUILD SUCCESSFUL
</success_criteria>

<output>
Create `.planning/phases/05-user-authentication/05-03-SUMMARY.md` when done
</output>

---
phase: 04-handler-framework
plan: 02
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-03"
files_modified:
  - gateway/src/main/kotlin/com/nebula/gateway/interceptor/Interceptor.kt
  - gateway/src/main/kotlin/com/nebula/gateway/interceptor/AuthInterceptor.kt
  - gateway/src/main/kotlin/com/nebula/gateway/interceptor/LogInterceptor.kt
  - gateway/src/main/kotlin/com/nebula/gateway/interceptor/RateLimitInterceptor.kt
  - gateway/src/main/kotlin/com/nebula/gateway/interceptor/ExceptionInterceptor.kt
  - gateway/src/test/kotlin/com/nebula/gateway/interceptor/AuthInterceptorTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/interceptor/LogInterceptorTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/interceptor/ExceptionInterceptorTest.kt
autonomous: true
requirements:
  - HNDL-04
  - HNDL-05
user_setup: []

must_haves:
  truths:
    - D-05: "Interceptor/Chain suspend 接口定义在 com.nebula.gateway.interceptor 包"
    - D-06: "拦截器通过 Koin List<Interceptor> 注入的模式"
    - D-07: "拦截器执行顺序 Auth → Log → RateLimit → Exception"
    - D-08: "AuthInterceptor.kt, LogInterceptor.kt, RateLimitInterceptor.kt, ExceptionInterceptor.kt 全部创建"
    - D-09: "AuthInterceptor 验证 session 并注入 CoroutineContext，跳过 skipMethods 白名单"
    - D-30: "AuthInterceptor 跳过 system/ping 方法"
    - D-08: "LogInterceptor 记录请求 method + 耗时"
    - D-08: "RateLimitInterceptor 骨架可编译，Travis CI 不报错"
    - D-10: "ExceptionInterceptor 三态异常映射：BizException→业务码、IAE→BAD_REQUEST、其他→9000"
    - D-03: "AuthInterceptor 通过 withContext(SessionKey) 注入 Session 到协程上下文"
    - D-23: "测试框架使用 JUnit5 + MockK"
    - D-24: "4 个拦截器的单元测试通过"
    - D-25: "suspend 函数测试使用 runTest + MockK coEvery/coVerify"
    - D-26: "Mock 全链路测试验证拦截器编排顺序"
  artifacts:
    - path: "gateway/src/main/kotlin/com/nebula/gateway/interceptor/Interceptor.kt"
      provides: "Interceptor + Chain suspend 接口"
      contains: "interface Interceptor"
    - path: "gateway/src/main/kotlin/com/nebula/gateway/interceptor/AuthInterceptor.kt"
      provides: "认证拦截器，skipMethods + CoroutineContext Session 注入"
      contains: "class AuthInterceptor"
    - path: "gateway/src/main/kotlin/com/nebula/gateway/interceptor/LogInterceptor.kt"
      provides: "日志拦截器，method + 耗时"
      contains: "class LogInterceptor"
    - path: "gateway/src/main/kotlin/com/nebula/gateway/interceptor/RateLimitInterceptor.kt"
      provides: "限流拦截器骨架"
      contains: "class RateLimitInterceptor"
    - path: "gateway/src/main/kotlin/com/nebula/gateway/interceptor/ExceptionInterceptor.kt"
      provides: "三态异常映射拦截器"
      contains: "class ExceptionInterceptor"
    - path: "gateway/src/test/kotlin/com/nebula/gateway/interceptor/AuthInterceptorTest.kt"
      provides: "AuthInterceptor 单元测试"
      contains: "skip auth for system-ping"
  key_links:
    - from: "AuthInterceptor.kt"
      to: "SessionRegistry.kt"
      via: "sessionRegistry.validate(token)"
      pattern: "sessionRegistry\\.validate"
    - from: "AuthInterceptor.kt"
      to: "SessionKey"
      via: "withContext(SessionKey(session))"
      pattern: "SessionKey"
    - from: "ExceptionInterceptor.kt"
      to: "BizException.kt"
      via: "catch (e: BizException)"
      pattern: "BizException"
    - from: "ExceptionInterceptor.kt"
      to: "BizCode.kt"
      via: "BizCode.INTERNAL_ERROR"
      pattern: "BizCode"
---

<objective>
构建完整的 Interceptor Pipeline — Interceptor/Chain 接口 + 4 个拦截器实现（Auth、Log、RateLimit、Exception）。

**Purpose:** 实现请求处理管道的认证、日志、限流和异常兜底能力。拦截器通过 Koin `List<Interceptor>` 注入（D-06），执行顺序 Auth → Log → RateLimit → Exception（D-07）。AuthInterceptor 验证 Session 并注入 CoroutineContext，LogInterceptor 记录请求耗时，RateLimitInterceptor 为骨架占位，ExceptionInterceptor 三态映射异常（D-10）。

**Output:** 5 个接口/实现 .kt 文件 + 3 个测试 .kt 文件
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-handler-framework/04-CONTEXT.md
@.planning/phases/04-handler-framework/04-RESEARCH.md
@.planning/phases/04-handler-framework/04-PATTERNS.md

# Plan 01 created interfaces (depends_on)
@.planning/phases/04-handler-framework/04-01-SUMMARY.md

# Existing code
@proto/src/main/proto/nebula/envelope.proto
@common/src/main/kotlin/com/nebula/common/BizCode.kt
@common/src/main/kotlin/com/nebula/common/exception/BizException.kt
@common/src/main/kotlin/com/nebula/common/config/ConfigLoader.kt

<interfaces>
<!-- Types created by Plan 01 that this plan imports -->

From Handler.kt (Plan 01):
```kotlin
interface Handler<Req : Any, Resp : Any> {
    val method: String
    suspend fun handle(req: Req): Resp
}
```

From SessionKey.kt (Plan 01):
```kotlin
data object SessionKey : CoroutineContext.Key<Session>
suspend fun CoroutineContext.requireSession(): Session
```

From HandlerRegistry.kt (Plan 01):
```kotlin
class HandlerRegistry {
    fun register(entry: HandlerEntry)
    fun get(method: String): HandlerEntry?
}
```

<!-- Types from Plan 03 that this plan imports -->

From Session.kt (Plan 03):
```kotlin
data class Session(
    val userId: Long,
    val token: String,
    val deviceType: String,
    val deviceId: String,
    val connectionId: String
)
```

From SessionRegistry.kt (Plan 03):
```kotlin
class SessionRegistry(
    private val sessionRepository: SessionRepository
) {
    suspend fun validate(token: String): Session?
    suspend fun register(session: Session)
    suspend fun unregister(token: String)
}
```

Logging pattern (from ConfigLoader.kt):
```kotlin
import io.github.oshai.kotlinlogging.KotlinLogging
private val log = KotlinLogging.logger {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interceptor/Chain 接口定义</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/interceptor/Interceptor.kt
  </files>
  <behavior>
    - Interceptor 接口定义 suspend fun intercept(request, chain): Response
    - Chain 内嵌接口定义 val request 和 suspend fun proceed(request): Response
  </behavior>
  <action>
    创建 `com.nebula.gateway.interceptor.Interceptor` 接口文件（D-05, D-06）：

    ```kotlin
    package com.nebula.gateway.interceptor

    import com.nebula.chat.Request
    import com.nebula.chat.Response

    /**
     * 拦截器接口 — suspend 版本的责任链模式（GoF Chain of Responsibility）。
     *
     * 通过 Koin 以 List<Interceptor> 方式注入，Dispatcher 组装为链。
     * 执行顺序（D-07）：
     *   AuthInterceptor → LogInterceptor → RateLimitInterceptor → ExceptionInterceptor
     *
     * KDoc 注明 suspend 适配（D-05）、拦截器顺序（D-07）
     */
    interface Interceptor {
        /** 拦截处理请求 */
        suspend fun intercept(request: Request, chain: Chain): Response

        /** 责任链接口 */
        interface Chain {
            /** 当前 Request 对象 */
            val request: Request

            /** 继续执行下一个拦截器（或最终 Handler） */
            suspend fun proceed(request: Request): Response
        }
    }
    ```
    - KDoc 标注 D-05（suspend 适配）、D-06（Chain of Responsibility）、D-07（执行顺序）
    - 无其他实现逻辑，纯接口定义
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    Interceptor.kt 编译通过，包结构符合 D-21
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AuthInterceptor + LogInterceptor + RateLimitInterceptor</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/interceptor/AuthInterceptor.kt
    gateway/src/main/kotlin/com/nebula/gateway/interceptor/LogInterceptor.kt
    gateway/src/main/kotlin/com/nebula/gateway/interceptor/RateLimitInterceptor.kt
  </files>
  <behavior>
    - AuthInterceptor: skipMethods 白名单跳过认证 → 否则 validate token → withContext 注入 Session
    - LogInterceptor: 记录 method + 耗时 + code/msg
    - RateLimitInterceptor: 骨架占位，直接 chain.proceed()
  </behavior>
  <action>
    创建 3 个拦截器实现文件。

    **AuthInterceptor.kt** (D-03, D-08, D-09, D-30):
    - `class AuthInterceptor(private val sessionRegistry: SessionRegistry, private val skipMethods: Set<String> = setOf("system/ping")) : Interceptor`
    - `intercept()` 逻辑：
      1. `method in skipMethods` → 直接 `chain.proceed(request)`（D-30）
      2. `extractToken(request)` 返回 null → 返回 `UNAUTHORIZED` Response
      3. `sessionRegistry.validate(token)` 返回 null → 返回 `TOKEN_INVALID` Response
      4. `withContext(SessionKey(session)) { chain.proceed(request) }` 注入 Session 到协程上下文（D-03）
    - `private fun extractToken(request: Request): String?` — TODO 方法，返回 null（Phase 5 确定 token 传递方式后实现）
    - companion object 持有 `KotlinLogging.logger {}`

    **LogInterceptor.kt** (D-08):
    - `class LogInterceptor : Interceptor`
    - `intercept()` 逻辑：
      1. 记录 `val start = System.currentTimeMillis()`
      2. `val resp = chain.proceed(request)`
      3. `val elapsed = System.currentTimeMillis() - start`
      4. `resp.code != 200` → `log.warn { "[$method] failed: code=${resp.code} msg=${resp.msg} (${elapsed}ms)" }`
      5. `resp.code == 200` → `log.info { "[$method] success (${elapsed}ms)" }`
      6. 返回 resp
    - 使用 `kotlin.system.measureTimeMillis` 也可以，但注意正确获取响应对象

    **RateLimitInterceptor.kt** (D-08):
    - `class RateLimitInterceptor : Interceptor`
    - `intercept()` 直接 `return chain.proceed(request)` — 骨架占位实现
    - KDoc 注明 "Token Bucket 算法占位，Phase 11 精细化调优"
    - TODO 注释标注 Phase 11 实现限流逻辑
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    3 个拦截器编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExceptionInterceptor 三态异常映射</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/interceptor/ExceptionInterceptor.kt
  </files>
  <behavior>
    - BizException → 使用 e.bizCode.code 作为响应 code
    - IllegalArgumentException → BAD_REQUEST(1000)
    - 其他 Exception → INTERNAL_ERROR(9000)，不暴露堆栈
  </behavior>
  <action>
    创建 `com.nebula.gateway.interceptor.ExceptionInterceptor`（D-08, D-10）：

    ```kotlin
    package com.nebula.gateway.interceptor

    import com.nebula.chat.Request
    import com.nebula.chat.Response
    import com.nebula.common.BizCode
    import com.nebula.common.exception.BizException
    import io.github.oshai.kotlinlogging.KotlinLogging

    /**
     * 异常拦截器 — 三态异常映射（D-10）。
     *
     * 作为 Pipeline 链尾包裹 Handler，捕获所有异常并映射为结构化 Response。
     * 不向客户端暴露堆栈细节。
     */
    class ExceptionInterceptor : Interceptor {

        override suspend fun intercept(request: Request, chain: Interceptor.Chain): Response {
            return try {
                chain.proceed(request)
            } catch (e: BizException) {
                // D-10: BizException → 业务状态码
                Response.newBuilder()
                    .setCode(e.bizCode.code)
                    .setMsg(e.message ?: e.bizCode.msg)
                    .setMethod(request.method)
                    .build()
            } catch (e: IllegalArgumentException) {
                // D-10: 参数异常 → BAD_REQUEST
                logger.warn(e) { "Illegal argument for method ${request.method}" }
                Response.newBuilder()
                    .setCode(BizCode.INVALID_PARAM.code)
                    .setMsg(e.message ?: BizCode.INVALID_PARAM.msg)
                    .setMethod(request.method)
                    .build()
            } catch (e: Exception) {
                // D-10: 未预期异常 → INTERNAL_ERROR(9000)，不暴露堆栈
                logger.error(e) { "Unhandled exception for method ${request.method}" }
                Response.newBuilder()
                    .setCode(BizCode.INTERNAL_ERROR.code)
                    .setMsg(BizCode.INTERNAL_ERROR.msg)
                    .setMethod(request.method)
                    .build()
            }
        }

        companion object {
            private val logger = KotlinLogging.logger {}
        }
    }
    ```
    - 注意 catch 顺序：BizException（最具体）→ IllegalArgumentException → Exception（最通用）
    - KDoc 注明三态映射（D-10）
    - 未预期异常：仅 `logger.error` 写堆栈，Response 只返回 9000 + "internal error"
  </action>
  <verify>
    <automated>./gradlew :gateway:compileKotlin 2>&1 | tail -10</automated>
  </verify>
  <done>
    ExceptionInterceptor 编译通过，三态 catch 结构正确
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 拦截器单元测试</name>
  <files>
    gateway/src/test/kotlin/com/nebula/gateway/interceptor/AuthInterceptorTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/interceptor/LogInterceptorTest.kt
    gateway/src/test/kotlin/com/nebula/gateway/interceptor/ExceptionInterceptorTest.kt
  </files>
  <behavior>
    - AuthInterceptor: skipMethods 跳过 / token null 返回 UNAUTHORIZED / token invalid 返回 TOKEN_INVALID
    - LogInterceptor: 正常执行返回原响应
    - ExceptionInterceptor: BizException 映射 / IAE 映射 / 未预期异常 映射
  </behavior>
  <action>
    创建 3 个测试文件，使用 JUnit5 + MockK + runTest（D-23, D-24, D-25）。

    **AuthInterceptorTest.kt:**
    - `skip auth for system-ping` — skipMethods 包含 system/ping，验证 `sessionRegistry.validate(any())` 未被调用
    - `reject when token missing` — extractToken 无法提取 token 时返回 UNAUTHORIZED
    - `reject when token invalid` — validate 返回 null 时返回 TOKEN_INVALID
    - 使用 `coEvery { }` / `coVerify { }` / `coVerify(inverse = true) { }`

    **LogInterceptorTest.kt:**
    - `log success request` — chain.proceed 返回 code=200 的 Response，验证拦截器返回相同 Response
    - 使用 `mockk<Interceptor.Chain>()` + `coEvery { chain.proceed(any()) } returns successResponse`

    **ExceptionInterceptorTest.kt:**
    - `handle BizException` — chain.proceed 抛出 BizException(USER_NOT_FOUND)，验证返回 code=1200
    - `handle IllegalArgumentException` — chain.proceed 抛出 IAE，验证返回 code=1000
    - `handle unexpected exception` — chain.proceed 抛出 RuntimeException，验证返回 code=9000 + msg="internal error"
    - 三种异常测试用例各一个，验证 code 和 msg 正确
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.interceptor.*" 2>&1 | tail -30</automated>
  </verify>
  <done>
    3 个测试类全部通过，验证 skipMethods/session验证/异常三态映射 正确
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AuthInterceptor 入口 | 未认证请求在此边界被拦截 |
| ExceptionInterceptor 出口 | 异常信息在此边界被脱敏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | AuthInterceptor | mitigate | 每个请求验证 Session token，skipMethods 严格控制白名单仅 system/ping（D-09） |
| T-04-06 | Information Disclosure | ExceptionInterceptor | mitigate | 未预期异常仅返回 9000 + "internal error"，堆栈写入日志不返回客户端（D-10） |
| T-04-07 | Tampering | AuthInterceptor.skipMethods | mitigate | skipMethods 通过 Koin 注入注入的构造函数参数配置，运行时不可修改；当前仅包含 system/ping |
| T-04-08 | Spoofing | AuthInterceptor CoroutineContext 注入 | mitigate | AuthInterceptor 通过 `withContext(SessionKey(session))` 注入，子协程使用结构化并发继承 |
| T-04-09 | Denial of Service | RateLimitInterceptor | accept | 当前为骨架占位，Phase 11 才实现限流逻辑 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本计划无包管理器安装操作 |
</threat_model>

<verification>
1. `./gradlew :gateway:compileKotlin` 通过 — 4 个拦截器 + 接口编译无错误
2. `./gradlew :gateway:test --tests "com.nebula.gateway.interceptor.*"` 通过
3. AuthInterceptor 正确跳过 system/ping、拒绝无效 token、注入 Session 到 CoroutineContext
4. ExceptionInterceptor 三态映射正确
</verification>

<success_criteria>
- Interceptor/Chain suspend 接口编译通过
- AuthInterceptor 验证 session 并注入 CoroutineContext，skipMethods 白名单生效
- LogInterceptor 记录 method + 耗时，区分成功/失败日志级别
- RateLimitInterceptor 骨架可编译
- ExceptionInterceptor 三态异常映射：BizException→业务码、IAE→1000、其他→9000
- 所有拦截器单元测试通过
</success_criteria>

<output>
Create `.planning/phases/04-handler-framework/04-02-SUMMARY.md` when done
</output>

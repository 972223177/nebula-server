---
phase: 04-handler-framework
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - gateway/src/main/kotlin/com/nebula/gateway/session/SessionRegistry.kt
  - proto/src/main/proto/nebula/envelope.proto
  - gateway/src/main/kotlin/com/nebula/gateway/handler/PingHandler.kt
  - server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
  - gateway/src/test/kotlin/com/nebula/gateway/session/SessionRegistryTest.kt
  - gateway/src/test/kotlin/com/nebula/gateway/handler/PingHandlerTest.kt
autonomous: true
requirements:
  - HNDL-01
user_setup: []

must_haves:
  truths:
    - D-17: "SessionRegistry 放在 :gateway 模块，依赖 :repository 模块的 SessionRepository"
    - D-18: "SessionRegistry 支持 L1(ConcurrentHashMap) + L2(Redis SessionRepository) 双级缓存"
    - D-19: "SessionRegistry 提供 validate/register/unregister 组合方法 + 细粒度 API"
    - D-20: "SessionRegistry 提供 eviction 回调注册点，连接清理时通知关闭 StreamObserver"
    - "SessionRegistry L2 Redis 使用 500ms 超时保护，Redis 不可用时降级为纯 L1 缓存"
    - D-31: "envelope.proto Direction 枚举包含 PING(4)/PONG(5) 有效值，非 reserved"
    - D-28: "PingHandler 通过 method=system/ping 路由，走标准 Dispatcher 返回 pong 响应"
    - D-30: "AuthInterceptor 和 LogInterceptor 跳过 system/ping"
    - D-27: "ChatServer.kt keepalive 配置使用双重心跳策略注释 + 业界标准参数"
    - D-29: "gRPC keepaliveTime=30~45s (per-connection randomized), minTime=10~20s, maxConnectionIdle=10~30min; 应用层 90s 无 PING 断开"
    - D-32: "所有时间参数加入随机 Jitter，防惊群效应"
    - D-23: "测试框架使用 JUnit5 + MockK"
    - D-24: "单元测试覆盖 SessionRegistry + PingHandler"
    - D-25: "suspend 函数测试使用 runTest + MockK coEvery/coVerify"
  artifacts:
    - path: "gateway/src/main/kotlin/com/nebula/gateway/session/SessionRegistry.kt"
      provides: "L1+L2 Session 注册中心"
      contains: "class SessionRegistry"
    - path: "proto/src/main/proto/nebula/envelope.proto"
      provides: "Direction 枚举含 PING(4)/PONG(5)"
      contains: "PING = 4"
    - path: "gateway/src/main/kotlin/com/nebula/gateway/handler/PingHandler.kt"
      provides: "应用层心跳 Handler"
      contains: "class PingHandler"
    - path: "server/src/main/kotlin/com/nebula/server/server/ChatServer.kt"
      provides: "双重心跳 keepalive 配置"
      contains: "双重心跳策略"
  key_links:
    - from: "SessionRegistry.kt"
      to: "SessionRepository.kt"
      via: "sessionRepository.findByToken()"
      pattern: "sessionRepository\\."
    - from: "SessionRegistry.kt"
      to: "Session.kt"
      via: "localCache 的值类型"
      pattern: "ConcurrentHashMap.*Session"
    - from: "PingHandler.kt"
      to: "Handler.kt"
      via: "implements Handler<Request, Response>"
      pattern: "Handler<"
---

<objective>
构建 Session 管理与应用层心跳 — Session 数据模型、SessionRegistry L1/L2 双级缓存、envelope.proto Direction PING/PONG、PingHandler、ChatServer.kt keepalive 配置升级。

**Purpose:** Session 是认证和用户上下文的核心数据结构（D-16），SessionRegistry 提供两级缓存加速读取（D-18）。应用层心跳（D-27~D-32）补充 gRPC keepalive 无法覆盖的 NAT/代理半开连接检测场景。PingHandler 作为首个 Handler 实现验证整个 Dispatcher Pipeline。

**关键时间参数（D-29, D-32）：**
- gRPC keepalive: keepaliveTime=30~45s (per-connection randomized) + timeout=10s
- 服务端 EnforcementPolicy: minTime=10~20s (per-connection randomized)
- 服务端 MaxConnectionIdle: 10~30min (per-connection randomized)
- 应用层 PING: 客户端 30~60s 发送，服务端 90s 无 PING 即断开
- 所有时间参数加入随机 Jitter，防惊群效应

**Output:** 2 个新 .kt 文件 + 2 个修改文件 + 2 个测试文件
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-handler-framework/04-CONTEXT.md
@.planning/phases/04-handler-framework/04-RESEARCH.md
@.planning/phases/04-handler-framework/04-PATTERNS.md

# Plan 01 created interfaces
@.planning/phases/04-handler-framework/04-01-SUMMARY.md

# Existing code
@proto/src/main/proto/nebula/envelope.proto
@server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
@repository/src/main/kotlin/com/nebula/repository/redis/SessionRepository.kt

<interfaces>
<!-- From Plan 01 -->
From Handler.kt (Plan 01):
```kotlin
interface Handler<Req : Any, Resp : Any> {
    val method: String
    suspend fun handle(req: Req): Resp
}
```

From SessionKey.kt (Plan 01):
```kotlin
data object SessionKey : CoroutineContext.Key<Session>
suspend fun CoroutineContext.requireSession(): Session
```

From Session.kt (Plan 01):
```kotlin
data class Session(
    val userId: Long,
    val token: String,
    val deviceType: String,
    val deviceId: String,
    val connectionId: String
)
```

<!-- Existing module dependency -->
From SessionRepository.kt:
```kotlin
class SessionRepository(
    private val connection: StatefulRedisConnection<String, String>
) {
    suspend fun save(token: String, userData: String, ttlSeconds: Long)
    suspend fun findByToken(token: String): String?
    suspend fun delete(token: String)
}
```

Current envelope.proto Direction (simplified):
```protobuf
enum Direction {
  DIRECTION_UNSPECIFIED = 0;
  REQUEST = 1;
  RESPONSE = 2;
  PUSH = 3;
  PING = 4;
  PONG = 5;
}
```

Current ChatServer.kt keepalive:
```kotlin
.keepAliveTime(60, TimeUnit.SECONDS)
.keepAliveTimeout(20, TimeUnit.SECONDS)
.permitKeepAliveWithoutCalls(true)
.maxConnectionIdle(300, TimeUnit.SECONDS)
.maxConnectionAge(1800, TimeUnit.SECONDS)
.maxConnectionAgeGrace(10, TimeUnit.SECONDS)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionRegistry L1/L2 双级缓存</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/session/SessionRegistry.kt
    gateway/src/test/kotlin/com/nebula/gateway/session/SessionRegistryTest.kt
  </files>
  <behavior>
    - SessionRegistry 使用 Plan 01 创建的 Session data class（D-16）
    - SessionRegistry validate(): L1 命中直接返回 → L2 查询后写入 L1 → 返回
    - SessionRegistry register(): 写入 L1 + L2
    - SessionRegistry unregister(): 移除 L1 + L2 + 触发回调
    - SessionRegistry L2 Redis 调用使用 500ms 超时保护，超时降级不阻塞流程
  </behavior>
  <read_first>
    04-CONTEXT.md — Session 双级缓存设计（D-17~D-20）
    04-RESEARCH.md — Pitfall 6（Redis 超时降级）
    Plan 01 输出: Session.kt（Session 数据类）
    repository/.../redis/SessionRepository.kt — Redis Session 操作接口
  </read_first>
  <action>
    创建 `com.nebula.gateway.session` 包下的 SessionRegistry.kt 文件。

    **SessionRegistry.kt** (D-17, D-18, D-19, D-20):
    - Session data class 由 Plan 01 提供，位于 `com.nebula.gateway.session` 包
    - `class SessionRegistry(private val sessionRepository: SessionRepository)` — D-17
    - private 字段：
      - `localCache = ConcurrentHashMap<String, Session>()` — token → Session（L1）
      - `userIdIndex = ConcurrentHashMap<Long, MutableSet<String>>()` — userId → tokens（用于多设备管理，D-18）
      - `evictionCallbacks = CopyOnWriteArrayList<(String) -> Unit>()` — 回调列表（D-20）
    - `fun onEviction(callback: (token: String) -> Unit)` — 注册驱逐回调（D-20）
    - 细粒度方法（D-19）：
      - `addToLocalCache(session: Session)` — 写入 L1 + 更新 userIdIndex
      - `removeFromLocalCache(token: String): Session?` — 移除 L1 + 更新 userIdIndex
      - `getFromLocalCache(token: String): Session?`
      - 对应 Redis 方法（suspend）：
        - `suspend fun saveToRedis(session: Session)` — 通过 sessionRepository.save() 将 token→连接信息的 JSON 写入 Redis
        - `suspend fun removeFromRedis(token: String)` — 通过 sessionRepository.delete() 删除
        - `suspend fun queryFromRedis(token: String): Session?` — 通过 sessionRepository.findByToken() 查询并反序列化为 Session
    - 组合方法（D-19）：
      - `suspend fun validate(token: String): Session?` — `getFromLocalCache(token) ?: queryFromRedis(token)?.also { addToLocalCache(it) }`
      - `suspend fun register(session: Session)` — `addToLocalCache(session); saveToRedis(session)`
      - `suspend fun unregister(token: String)` — `removeFromLocalCache(token); removeFromRedis(token); evictionCallbacks.forEach { it(token) }`
    - KDoc 注明 "L1(ConcurrentHashMap) + L2(Redis SessionRepository) 二级缓存（D-18）"
    - ⚠️ L2 Redis 调用超时与降级策略（Review 反馈#6）：SessionRegistry.validate() 的 Redis 调用可能因网络故障挂起。当前阶段：
      - `queryFromRedis()` 使用 `withTimeout(500, TimeUnit.MILLISECONDS)` 包裹 Redis 调用（如果 Redis 挂起超过 500ms 则返回 null）
      - `saveToRedis()` 同样使用 `withTimeout(500, TimeUnit.MILLISECONDS)`，超时后仅日志记录不阻塞注册流程
      - 降级策略：Redis 不可用时，仅使用 L1 本地缓存。已登录用户不受影响（Session 在 L1 中），新登录用户在 Redis 恢复前无法完成认证
      - TODO: Phase 11 添加熔断器（如 Resilience4j）保护 Redis 调用

    **SessionRegistryTest.kt** (D-23, D-24, D-25):
    - 包 `com.nebula.gateway.session`
    - 使用 MockK 模拟 `SessionRepository`，`coEvery {}` 定义 Redis 行为
    - 测试用例：
      - `validate from L1 cache` — 先 register()，再 validate()，验证不走 Redis（coVerify(inverse=true) 确认 findByToken 未被调用）
      - `validate from L2 when L1 miss` — 仅 queryFromRedis 返回 Session，验证 validate 返回 Session 且写入 L1
      - `register stores to L1 and L2` — 验证 register 后 L1 可查到，Redis save 被调用
      - `unregister triggers eviction callbacks` — 注册回调后 unregister，验证回调被调用
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.session.SessionRegistryTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    Session 数据类编译通过，SessionRegistry L1/L2 缓存行为验证通过，单元测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: envelope.proto Direction 更新 + ChatServer.kt keepalive 优化</name>
  <files>
    proto/src/main/proto/nebula/envelope.proto
    server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
  </files>
  <behavior>
    - envelope.proto Direction 包含 PING(4)/PONG(5) 非 reserved 值
    - ChatServer.kt keepalive 注释说明双重心跳策略
  </behavior>
  <read_first>
    04-CONTEXT.md — 双重心跳策略（D-27~D-32）、时间参数随机 Jitter（D-29/D-32）
    proto/src/main/proto/nebula/envelope.proto — 当前 Direction 枚举定义
    server/.../ChatServer.kt — 当前 keepalive 配置
  </read_first>
  <action>
    修改两个现有文件。

    **envelope.proto** (D-31):
    - 当前 Direction 枚举应已包含 PING=4/PONG=5（Phase 1 定义时已纳入）
    - 确保枚举值存在且 NOT reserved
    - 更新 Direction 上方注释：添加双重心跳策略说明（从"心跳采用双重心跳策略"开始，说明传输层 gRPC keepalive 和应用层 PING/PONG 的区别）
    - 用 Edit 工具检查并更新注释块

    **ChatServer.kt** (D-27, D-29, D-32):
    - 当前配置（Phase 2 遗留）: keepAliveTime=60s, keepAliveTimeout=20s, maxConnectionIdle=300s
    - **更新为公开推荐配置（D-29）:**
      ```
      // 双重心跳策略（D-27）
      // 传输层 gRPC keepalive → 连接存活检测（TCP/HTTP2 帧层面）
      // 应用层 system/ping → 服务健康检测（业务请求链路层面）
      
      // 客户端 side keepalive（通过 NettyServerBuilder 的服务端配置影响客户端行为）
      // 服务端 EnforcementPolicy：
      .keepAliveTime(30, TimeUnit.SECONDS)      // baseline，实际值会被 jitter 覆盖
      .keepAliveTimeout(10, TimeUnit.SECONDS)    // PING 超时 10s
      .permitKeepAliveWithoutCalls(true)         // 无活动流也发 PING
      
      // 服务端 MaxConnectionIdle — 主动回收僵尸连接
      .maxConnectionIdle(10, TimeUnit.MINUTES)   // baseline，实际值会被 jitter 覆盖
      
      // 后续实现中，keepAliveTime/maxConnectionIdle 在创建 NettyServerBuilder 时随机化（D-32）
      ```
    - 注释中注明 keepaliveTime 和 maxConnectionIdle 需要在创建时随机化（D-32 Jitter 策略）
    - 保留 `maxConnectionAge(30, TimeUnit.MINUTES)` 和 `maxConnectionAgeGrace` 不变（安全边界）
  </action>
  <verify>
    <automated>
      # 验证 PING/PONG 存在且非 reserved
      grep -c "PING = 4" proto/src/main/proto/nebula/envelope.proto
      grep -c "PONG = 5" proto/src/main/proto/nebula/envelope.proto
      # 验证双重心跳注释 + Jitter 策略说明
      grep -c "双重心跳" server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
      grep -c "jitter\|随机化" server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
      # 验证关键参数存在
      grep -c "keepAliveTime" server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
      grep -c "maxConnectionIdle" server/src/main/kotlin/com/nebula/server/server/ChatServer.kt
      ./gradlew :proto:generateProto 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    envelope.proto Direction 包含 PING(4)/PONG(5) 有效值，ChatServer.kt 注释反映双重心跳策略
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PingHandler 应用层心跳 Handler</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/handler/PingHandler.kt
    gateway/src/test/kotlin/com/nebula/gateway/handler/PingHandlerTest.kt
  </files>
  <behavior>
    - PingHandler.method = "system/ping"
    - handle() 返回 code=200 msg="pong" 的 Response
  </behavior>
  <read_first>
    04-CONTEXT.md — 应用层 PING/PONG 心跳策略（D-28~D-30）
    Plan 01 输出: Handler.kt（Handler 接口定义）
    common/.../BizCode.kt — BizCode.OK
    proto/src/main/proto/nebula/envelope.proto — Request/Response 消息定义
  </read_first>
  <action>
    创建 `com.nebula.gateway.handler` 包下的文件。

    **PingHandler.kt** (D-28, D-30):
    - `class PingHandler : Handler<Request, Response>` — 实现标准 Handler 接口
    - `override val method: String = "system/ping"` — D-28 指定的路由
    - `override suspend fun handle(req: Request): Response`：
      ```kotlin
      return Response.newBuilder()
          .setCode(BizCode.OK.code)
          .setMsg("pong")
          .setMethod(method)
          .build()
      ```
    - KDoc 注明：
      - 双重心跳策略的应用层组件（D-27）：gRPC keepalive 解决"连接存活"问题，应用层 PING 解决"服务健康"问题
      - AuthInterceptor 和 LogInterceptor 通过 skipMethods 跳过此 Handler（D-30）
      - 应用层 PING 超时：客户端 30~60s 发送，服务端 90s 无 PING 断开（D-29），加入随机 Jitter（D-32）
    - ⚠️ Pitfall 警告：PingHandler 内部若使用 `launch { }` 或 `withContext(Dispatchers.Default)` 启动新协程，Session（通过 CoroutineContext 传递）在新协程中不可见。如需异步操作，使用 `coroutineScope { }` / `supervisorScope { }` 结构化并发。（RESEARCH.md Pitfall 1）

    **PingHandlerTest.kt** (D-23, D-24, D-25, Claude's Discretion: 心跳 Handler 单元测试):
    - 包 `com.nebula.gateway.handler`
    - 使用 `runTest { }`
    - 测试用例：`ping returns pong response` — handle(emptyRequest) 返回 code=200 msg="pong"
    - 测试用例：`method is system/ping` — 验证 `handler.method == "system/ping"`
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.handler.PingHandlerTest" 2>&1 | tail -10</automated>
  </verify>
  <done>
    PingHandler 编译通过，method=system/ping 返回 pong 响应，单元测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionRegistry L1 缓存 | 本地内存中的数据一致性和并发安全性 |
| SessionRegistry L2 到 Redis | 跨节点 Session 持久化和失效处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | SessionRegistry L1/L2 一致性 | mitigate | L1 只读缓存（validate 写入），L2 持久化（Redis TTL）。L1 故障不影响全局 Session（L2 兜底） |
| T-04-10b | Denial of Service | SessionRegistry L2 Redis 超时 | mitigate | `queryFromRedis()` 和 `saveToRedis()` 使用 `withTimeout(500ms)` 保护，Redis 不可用时降级为纯 L1 缓存，不影响已登录用户（Review 反馈#6） |
| T-04-11 | Elevation of Privilege | PingHandler | mitigate | D-30: AuthInterceptor 跳过 system/ping，但 PingHandler 只返回固定 pong 响应，无业务逻辑暴露 |
| T-04-12 | Denial of Service | PingHandler | mitigate | 客户端 PING 间隔 30~60s + 随机 Jitter（D-32），服务端 EnforcementPolicy minTime=10~20s 拒绝更频繁的 PING |
| T-04-14 | Denial of Service | Jitter 缺失 | mitigate | D-32 强制所有时间参数随机化，防止惊群效应。ChatServer.kt 在创建时对 keepaliveTime、maxConnectionIdle 做 per-connection 随机化 |
| T-04-13 | Elevation of Privilege | SessionRegistry.onEviction | mitigate | 回调仅通知关闭 StreamObserver，不涉及认证或授权操作（D-20） |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 本计划无包管理器安装操作 |
</threat_model>

<verification>
1. `./gradlew :gateway:compileKotlin` 通过
2. `./gradlew :proto:generateProto` 通过 — PING/PONG 枚举值有效
3. `./gradlew :gateway:test --tests "com.nebula.gateway.session.*"` 通过
4. `./gradlew :gateway:test --tests "com.nebula.gateway.handler.PingHandlerTest"` 通过
5. ChatServer.kt 注释反映双重心跳策略
</verification>

<success_criteria>
- SessionRegistry L1/L2 双级缓存可正常工作：L1 写入/读取/Evict 回调、L2 Redis 查询/写入/删除
- envelope.proto Direction 包含 PING(4)/PONG(5)
- PingHandler 通过 method=system/ping 返回 code=200 msg="pong"
- ChatServer.kt keepalive 配置注释反映双重心跳策略
</success_criteria>

<output>
Create `.planning/phases/04-handler-framework/04-03-SUMMARY.md` when done
</output>

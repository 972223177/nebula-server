---
phase: 04-handler-framework
plan: 04
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt
  - gradle/libs.versions.toml
  - gateway/build.gradle.kts
  - server/build.gradle.kts
  - server/src/main/kotlin/com/nebula/server/NebulaServer.kt
  - gateway/src/test/kotlin/com/nebula/gateway/di/GatewayModuleTest.kt
autonomous: true
requirements:
  - HNDL-03
user_setup: []

must_haves:
  truths:
    - D-06: "Koin module 注册所有基础设施组件（HandlerRegistry, ProtoCodec, SessionRegistry, Dispatcher, 4 个拦截器）"
    - D-07: "拦截器以 List<Interceptor> 注入，顺序决定执行顺序"
    - "libs.versions.toml 包含 koin-core 4.1.0 和 test 依赖"
    - "gateway/build.gradle.kts 包含 koin-core 和 test 依赖"
    - "server/build.gradle.kts 包含 koin-core 依赖"
    - D-03: "NebulaServer.kt 在 gRPC 启动前初始化 Koin，加载 gateway 模块"
    - D-23: "测试框架使用 JUnit5 + MockK + KoinTestExtension"
    - D-24: "集成测试验证 Dispatcher → Pipeline → Handler 完整路径"
    - D-26: "全链路 Mock 测试验证编排顺序"
  artifacts:
    - path: "gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt"
      provides: "框架 + Handler Koin 模块"
      contains: "frameworkModule"
    - path: "gradle/libs.versions.toml"
      provides: "Koin 和 MockK 版本声明"
      contains: "koin = \"4.1.0\""
    - path: "gateway/build.gradle.kts"
      provides: "gateway 模块 Koin 依赖"
      contains: "libs.koin.core"
    - path: "server/build.gradle.kts"
      provides: "server 模块 Koin 依赖"
      contains: "libs.koin.core"
    - path: "server/src/main/kotlin/com/nebula/server/NebulaServer.kt"
      provides: "Koin 初始化入口"
      contains: "startKoin"
  key_links:
    - from: "GatewayModule.kt"
      to: "HandlerRegistry.kt"
      via: "single { HandlerRegistry() }"
      pattern: "HandlerRegistry"
    - from: "GatewayModule.kt"
      to: "AuthInterceptor.kt / LogInterceptor.kt / RateLimitInterceptor.kt / ExceptionInterceptor.kt"
      via: "single<Interceptor> { ... }"
      pattern: "single<Interceptor>"
    - from: "GatewayModule.kt"
      to: "SessionRegistry.kt"
      via: "single { SessionRegistry(get()) }"
      pattern: "SessionRegistry"
    - from: "NebulaServer.kt"
      to: "GatewayModule.kt"
      via: "modules(frameworkModule, handlerModule)"
      pattern: "modules\\(.*Module"
---

<objective>
构建 Koin 依赖注入 + 全链路集成 — GatewayModule Koin 模块定义、libs.versions.toml/build.gradle.kts 依赖声明、NebulaServer.kt Koin 初始化集成、端到端集成测试。

**Purpose:** 将 Plan 01~03 的所有组件通过 Koin DI 进行装配（D-06），NebulaServer 启动时初始化 Koin 并加载模块（D-03）。集成测试验证 gRPC Request → Dispatcher.dispatch() → Pipeline → Handler → Response 完整路径。

**Output:** 1 个新 .kt 文件 + 3 个修改文件 + 1 个测试文件
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-handler-framework/04-CONTEXT.md
@.planning/phases/04-handler-framework/04-RESEARCH.md
@.planning/phases/04-handler-framework/04-PATTERNS.md

# All prior plans
@.planning/phases/04-handler-framework/04-01-SUMMARY.md
@.planning/phases/04-handler-framework/04-02-SUMMARY.md
@.planning/phases/04-handler-framework/04-03-SUMMARY.md

# Existing code
@gradle/libs.versions.toml
@gateway/build.gradle.kts
@server/build.gradle.kts
@server/src/main/kotlin/com/nebula/server/NebulaServer.kt

<interfaces>
<!-- All types Koin must wire together -->

From Plan 01:
```kotlin
// Handler.kt
interface Handler<Req : Any, Resp : Any> {
    val method: String
    suspend fun handle(req: Req): Resp
}

// HandlerRegistry.kt
class HandlerRegistry {
    fun register(entry: HandlerEntry)
    fun get(method: String): HandlerEntry?
}

// ProtoCodec.kt
object ProtoCodec {
    fun buildCodec(protoClass: KClass<*>): CodecPair
}

// Dispatcher.kt
class Dispatcher(
    private val handlerRegistry: HandlerRegistry,
    private val interceptors: List<Interceptor>,
    private val protoCodec: ProtoCodec
) {
    suspend fun dispatch(envelopeRequest: Request): Response
}
```

From Plan 02:
```kotlin
// Interceptor.kt
interface Interceptor {
    suspend fun intercept(request: Request, chain: Chain): Response
    interface Chain { ... }
}

// AuthInterceptor.kt
class AuthInterceptor(
    private val sessionRegistry: SessionRegistry,
    private val skipMethods: Set<String> = setOf("system/ping")
) : Interceptor

// LogInterceptor.kt
class LogInterceptor : Interceptor

// RateLimitInterceptor.kt
class RateLimitInterceptor : Interceptor

// ExceptionInterceptor.kt
class ExceptionInterceptor : Interceptor
```

From Plan 03:
```kotlin
// Session.kt
data class Session(...)

// SessionRegistry.kt
class SessionRegistry(
    private val sessionRepository: SessionRepository
) { ... }

// PingHandler.kt
class PingHandler : Handler<Request, Response>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依赖声明 + build.gradle.kts 更新</name>
  <files>
    gradle/libs.versions.toml
    gateway/build.gradle.kts
    server/build.gradle.kts
  </files>
  <action>
    修改三个构建文件以添加 Koin 和测试依赖。

    **libs.versions.toml** (RESEARCH.md Standard Stack):
    - 在 `[versions]` 节添加：
      ```toml
      koin = "4.1.0"
      mockk = "1.13.14"
      ```
    - 在 `[libraries]` 节添加：
      ```toml
      koin-core = { module = "io.insert-koin:koin-core", version.ref = "koin" }
      koin-test = { module = "io.insert-koin:koin-test", version.ref = "koin" }
      koin-test-junit5 = { module = "io.insert-koin:koin-test-junit5", version.ref = "koin" }
      mockk = { module = "io.mockk:mockk", version.ref = "mockk" }
      kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "kotlinx-coroutines" }
      ```

    **gateway/build.gradle.kts** (RESEARCH.md lines 150-165):
    - 当前内容：
      ```kotlin
      dependencies {
          implementation(project(":service"))
          implementation(project(":proto"))
      }
      ```
    - 修改为：
      ```kotlin
      dependencies {
          implementation(project(":service"))
          implementation(project(":proto"))
          implementation(project(":common"))

          // Phase 4: Handler Framework
          implementation(libs.koin.core)
          implementation(libs.kotlin.reflect)

          // Test
          testImplementation(libs.koin.test)
          testImplementation(libs.koin.test.junit5)
          testImplementation(libs.mockk)
          testImplementation(libs.kotlinx.coroutines.test)
      }
      ```
    - 注意：gateway 需要依赖 `:common` 模块（引用 BizCode, BizException）

    **server/build.gradle.kts** (RESEARCH.md lines 168-177):
    - 在 dependencies 末尾添加：
      ```kotlin
      // Phase 4: Koin 启动
      implementation(libs.koin.core)
      ```
  </action>
  <verify>
    <automated>./gradlew :gateway:dependencies --configuration runtimeClasspath 2>&1 | grep -E "koin|mockk|coroutines-test" | head -10</automated>
  </verify>
  <done>
    libs.versions.toml 包含 Koin/MockK 声明，gateway 和 server 模块 build.gradle.kts 包含正确的依赖项
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GatewayModule Koin 模块</name>
  <files>
    gateway/src/main/kotlin/com/nebula/gateway/di/GatewayModule.kt
    gateway/src/test/kotlin/com/nebula/gateway/di/GatewayModuleTest.kt
  </files>
  <behavior>
    - frameworkModule 注册 HandlerRegistry, ProtoCodec, SessionRegistry, Dispatcher, 4 interceptors
    - handlerModule 注册 PingHandler
    - KoinTestExtension 验证所有组件可解析
  </behavior>
  <action>
    创建 `com.nebula.gateway.di` 包下的 Koin 模块文件和测试。

    **GatewayModule.kt** (D-06, RESEARCH.md Pattern 6):
    ```kotlin
    package com.nebula.gateway.di

    import com.nebula.gateway.codec.ProtoCodec
    import com.nebula.gateway.dispatcher.Dispatcher
    import com.nebula.gateway.dispatcher.HandlerRegistry
    import com.nebula.gateway.handler.PingHandler
    import com.nebula.gateway.interceptor.AuthInterceptor
    import com.nebula.gateway.interceptor.ExceptionInterceptor
    import com.nebula.gateway.interceptor.Interceptor
    import com.nebula.gateway.interceptor.LogInterceptor
    import com.nebula.gateway.interceptor.RateLimitInterceptor
    import com.nebula.gateway.session.SessionRegistry
    import org.koin.dsl.module

    /**
     * 框架级 Koin 模块 — 注册所有基础设施组件。
     *
     * 设计决策引用：
     * - D-06: 拦截器通过 Koin List<Interceptor> 注入
     * - D-07: 拦截器顺序 Auth → Log → RateLimit → Exception
     */
    val frameworkModule = module {
        single { HandlerRegistry() }
        single { ProtoCodec }
        single { SessionRegistry(get()) }  // SessionRepository 从 Koin 注入

        // D-07: 拦截器以 List<Interceptor> 注入，顺序决定执行顺序
        single<Interceptor> { AuthInterceptor(get(), skipMethods = setOf("system/ping")) }
        single<Interceptor> { LogInterceptor() }
        single<Interceptor> { RateLimitInterceptor() }
        single<Interceptor> { ExceptionInterceptor() }
    }

    /**
     * 业务 Handler Koin 模块 — 注册业务 Handler。
     */
    val handlerModule = module {
        single { PingHandler() }
    }
    ```

    **GatewayModuleTest.kt** (D-23, D-24):
    - 包 `com.nebula.gateway.di`
    - 使用 `KoinTestExtension` + `KoinTest` interface
    - 注入 `SessionRepository` 的 mock（因为 SessionRegistry 依赖它）
    - 测试用例：
      - `frameworkModule resolves HandlerRegistry` — 验证 `get<HandlerRegistry>()` 非 null
      - `frameworkModule resolves Dispatcher` — 构造 Dispatcher 需要 HandlerRegistry + List<Interceptor> + ProtoCodec，通过 Koin 解析和 `get()` 手动构建
      - `handlerModule resolves PingHandler` — 验证 `get<PingHandler>()` 非 null
    - 注意：Dispatcher 在 Koin 模块中依赖 `List<Interceptor>`（Koin 自动提供所有 `single<Interceptor>` 的列表），所以模块测试需要验证 Koin 可正确装配
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.di.GatewayModuleTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    GatewayModule Koin 模块文件注册所有组件，GatewayModuleTest 验证 Koin 装配正确
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NebulaServer.kt Koin 初始化 + 全链路集成测试</name>
  <files>
    server/src/main/kotlin/com/nebula/server/NebulaServer.kt
    gateway/src/test/kotlin/com/nebula/gateway/dispatcher/PipelineIntegrationTest.kt
  </files>
  <behavior>
    - NebulaServer.kt 在 gRPC 启动前加载 Koin modules
    - 集成测试验证完整 Pipeline 执行
  </behavior>
  <action>
    修改 NebulaServer.kt 并创建集成测试。

    **NebulaServer.kt** (D-03):
    - 在文件头部添加 import：
      ```kotlin
      import com.nebula.gateway.di.frameworkModule
      import com.nebula.gateway.di.handlerModule
      import org.koin.core.context.startKoin
      ```
    - 在 Step 4.5（Repository 初始化后）和 Step 5（启动 gRPC）之间插入 Koin 初始化：
      ```kotlin
      // Step 4.75: Phase 4 — 初始化 Koin DI 容器
      // 在 gRPC Server 启动前注册所有基础设施组件
      // 注册顺序：框架组件（HandlerRegistry/Interceptor/SessionRegistry）→ 业务 Handler
      startKoin {
          modules(frameworkModule, handlerModule)
      }
      ```
    - 位置：在 `messageWriteRepo.startFlushTimer()` 之后、`val chatServer = ChatServer(config)` 之前

    **PipelineIntegrationTest.kt** (D-24, D-26):
    - 包 `com.nebula.gateway.dispatcher`
    - 使用 `KoinTestExtension` + MockK 模拟 SessionRepository
    - Koin module：注册所有 frameworkModule + handlerModule 组件
    - 测试用例：`full pipeline processes ping request`：
      1. `get<HandlerRegistry>()` — 获取注册中心
      2. `get<Dispatcher>()` — 获取分发器
      3. 手动将 PingHandler 注册到 HandlerRegistry（使用 HandlerEntry）
      4. 构建 `Request.newBuilder().setMethod("system/ping").build()`
      5. `dispatcher.dispatch(request)` 在 `runTest { }` 中执行
      6. 验证：Response.code == 200, Response.msg == "pong"
    - 验证拦截器链执行顺序（通过 MockK 的 coVerify 确认各拦截器按 D-07 顺序调用）
    - 这验证了从 Dispatcher → HandlerRegistry → ProtoCodec → Interceptor Pipeline → Handler 的完整路径
  </action>
  <verify>
    <automated>./gradlew :gateway:test --tests "com.nebula.gateway.dispatcher.PipelineIntegrationTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    NebulaServer.kt 在 gRPC 启动前初始化 Koin，集成测试验证完整 Pipeline 执行
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Koin module 加载 | 组件注册和依赖解析入口 |
| NebulaServer.kt Koin 初始化 | 应用级启动流程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | Koin module 重复注册 | mitigate | HandlerRegistry.register() 的 putIfAbsent 检查（已在 Plan 01 实现）防止 method 重复 |
| T-04-15 | Denial of Service | NebulaServer Koin init 失败 | mitigate | startKoin 在 gRPC 启动前执行，若 DI 装配失败则应用不启动，避免运行在半初始化状态 |
| T-04-SC | Tampering | Maven Central 依赖验证 | mitigate | 所有包（koin-core 4.1.0, mockk 1.13.14）在 RESEARCH.md Package Legitimacy Audit 中标记为 [VERIFIED] |
</threat_model>

<verification>
1. `./gradlew :gateway:compileKotlin` 通过
2. `./gradlew :server:compileKotlin` 通过 — NebulaServer.kt Koin 初始化编译通过
3. `./gradlew :gateway:test` 通过 — GatewayModuleTest + PipelineIntegrationTest 通过
4. Koin 模块正确注册所有 9+ 组件（HandlerRegistry, ProtoCodec, SessionRegistry, 4 Interceptors, Dispatcher, PingHandler）
</verification>

<success_criteria>
- libs.versions.toml 包含 koin-core 4.1.0 版本声明
- gateway/build.gradle.kts 包含 koin-core 和 test 依赖
- server/build.gradle.kts 包含 koin-core 依赖
- GatewayModule.kt 定义 frameworkModule + handlerModule 两个 Koin 模块
- NebulaServer.kt 在 gRPC 启动前调用 startKoin { modules(frameworkModule, handlerModule) }
- PipelineIntegrationTest 验证 PingHandler 通过完整 Dispatcher+Pipeline 返回 pong
</success_criteria>

<output>
Create `.planning/phases/04-handler-framework/04-04-SUMMARY.md` when done
</output>

# Nebula 内置敏感词基线词库（一行一词，UTF-8 编码）
#
# 格式约定：
#   - 每行一个敏感词
#   - 以 # 开头的行视为注释，加载时跳过
#   - 首尾空白会被自动去除，重复词自动去重
#
# 该文件随 jar 打包发布，服务端启动时优先加载此基线（始终可用，不依赖外网）。
# 如需扩充词库：直接在此文件追加词条后调用 admin/sensitive_word_reload 热更新；
# 或在 config 中配置敏感的 source-url 指向可达的远程词库（启动时作为可选覆盖）。
#
# 以下为占位种子词，请按业务需要替换为实际词表。
傻逼
操你妈
fuck
shit
bitch
damn
垃圾
废物
滚蛋
去死
核弹
炸弹
强奸

<configuration>
    <!--
        日志目录注入（D-18）:
        通过 -Dlog.dir=... JVM 参数传入，默认 logs/。
        例如 Java 启动参数 -Dlog.dir=/var/log/nebula
    -->
    <property name="LOG_DIR" value="${log.dir:-logs}"/>

    <!-- CQ-10: 审计日志文件 Appender — 仅 AUDIT_LOGIN Marker 事件写入 audit.log -->
    <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/audit.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/audit.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] - %msg%n</pattern>
        </encoder>
        <!-- 仅接受 AUDIT_LOGIN Marker 标记的日志事件 -->
        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                <marker>AUDIT_LOGIN</marker>
            </evaluator>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.JsonEncoder"/>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
        <appender-ref ref="AUDIT_FILE"/>
    </root>
</configuration>
